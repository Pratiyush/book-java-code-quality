# fig07_1.sources.md — per-label source trace

**Figure:** Fig 07.1 — The typography / meaning axis  
**Chapter:** 6 (dossier key 07, folds 17 + 34) — Three Things You Should Never Argue About Twice  
**Draft:** `03-drafts/07_naming_structure_formatting/07_naming_structure_formatting_v1.md`  
**Rendered:** `05-figures/07_naming_structure_formatting/fig07_1.png`

---

## Per-label source trace

| Label / atom in diagram | Claim made | Pinned source path |
|---|---|---|
| Axis head "Tool-decidable (typography) / Human-only (semantics)" | The chapter's spine: one axis splits all three layers. "The one idea worth holding is the axis." | Draft §"Overview" + §"Layer 1" CONCEPT callout |
| Axis sublabel "Regex / reflow → one correct answer" | Formatting is "purely mechanical and fully decidable": under a given style there is exactly one in-format rendering. | Draft §"Layer 3 — Formatting" |
| Axis sublabel "Domain truth → no correct answer from syntax" | "A linter confirms `data` matches `lowerCamelCase`; it cannot flag `data` as a uselessly vague name for the customer's unsettled invoices." | Draft §"Layer 1 — Naming: the settled part and the hard part" |
| Layer 1 — Naming: `lowerCamelCase` for methods, fields, params, locals | Google Java Style §5 naming table: methods, non-constant fields, parameters, local variables → `lowerCamelCase`. | Draft §"Layer 1" table; Google Java Style Guide §5.2 |
| Layer 1 — Naming: `UpperCamelCase` for types | Google Java Style §5.2.1: class names are `UpperCamelCase`. | Draft §"Layer 1" table; Google Java Style Guide §5.2.1 |
| Layer 1 — Naming: `CONSTANT_CASE` for deeply-immutable `static final` | Google Java Style §5.2.4: a constant is a `static final` field "whose contents are deeply immutable and whose methods have no detectable side effects." | Draft §"Layer 1" — Google Java Style §5.2.4 trap paragraph |
| `Checkstyle ConstantName ^[A-Z][A-Z0-9]*(_[A-Z0-9]+)*$` | Checkstyle 13.6.0 `ConstantName` rule default regex. | Draft §"Layer 1" tool table row "Checkstyle / ConstantName / verified" |
| `Checkstyle MethodName ^[a-z][a-zA-Z0-9]*$` | Checkstyle 13.6.0 `MethodName` rule default regex. | Draft §"Layer 1" tool table row "Checkstyle / MethodName / verified" |
| `PMD ClassNamingConventions.classPattern [A-Z][a-zA-Z0-9]*` | PMD 7.25.0 `ClassNamingConventions.classPattern` default. | Draft §"Layer 1" tool table row "PMD / ClassNamingConventions.classPattern / verified" |
| `SonarQube java:S100 · S101 · S115 · S116 · S117` | SonarQube 2026.1 LTA rule keys for method, class, constant, field, local/param naming. | Draft §"Layer 1" tool table row "SonarQube / java:S100/S101/S115/S116/S117 / ⚠ default @pin" |
| `data` → `outstandingInvoices` rename example | The hook and §"Layer 1": "`data` — data about what? This holds the customer's unsettled invoices." | Draft §"Hook" + §"Layer 1 — the hard part" |
| Layer 2 — Structure: package → imports → one top-level class | Google Java Style §3 source-file structure: license/copyright if present, `package`, imports, then exactly one top-level class. | Draft §"Layer 2 — Structure: order with a reason"; Google Java Style Guide §3 |
| No wildcard imports; static imports in one group; ASCII-sorted | Google Java Style §3.3 import discipline. | Draft §"Layer 2" — import discipline paragraph |
| Overloaded methods in a single contiguous group | Google Java Style §3.4.2: "methods sharing a name appear in a single contiguous group with no other members in between." | Draft §"Layer 2" — overload contiguity paragraph |
| §3.4.2 "some logical order… maintainer could explain" quote | Google Java Style §3.4.2 verbatim: "what is important is that each class uses *some* logical order, which its maintainer could explain if asked." | Draft §"Layer 2" — "here the guide explicitly declines" paragraph |
| Layer 3 — Formatting: +2 spaces (§4.2) | Google Java Style §4.2: block indentation is +2 spaces. | Draft §"Layer 3 — Formatting" |
| Layer 3 — Formatting: 100-char column limit (§4.4) | Google Java Style §4.4: column limit is 100. | Draft §"Layer 3" |
| Layer 3 — Formatting: K&R braces (§4.1) | Google Java Style §4.1: Egyptian-style ("K&R") braces. | Draft §"Layer 3" |
| Layer 3 — Formatting: one statement per line (§4.3) | Google Java Style §4.3: one statement per line. | Draft §"Layer 3" |
| `google-java-format 1.35.0 — no configurability by design` | google-java-format README: "There is no configurability as to the formatter's algorithm for formatting. This is a deliberate design decision…" | Draft §"Layer 3" — google-java-format paragraph (verbatim README quote) |
| `palantir-java-format — 120-char, chain-tuned` | palantir-java-format README: "modern, lambda-friendly, 120 character Java formatter," "based on the excellent google-java-format." | Draft §"Layer 3" — palantir-java-format paragraph (verbatim README quote) |
| `Spotless 3.6.0 — spotless:check / spotless:apply` | Spotless 3.6.0 Maven goals `spotless:check` / `spotless:apply`. | Draft §"Layer 3" — Spotless paragraph |
| `EditorConfig — indent_style, indent_size, end_of_line, charset` | EditorConfig spec core properties. `max_line_length` intentionally omitted as it is not in the core spec's listed properties (draft back-matter note). | Draft §"Layer 3" — EditorConfig paragraph + back-matter sources |
| Style value comparison: Google 2-space/100-col, AOSP 4-space, palantir 120-col, Checkstyle LineLength default 80 | "Two-space vs four-space, 80 vs 100 vs 120 columns: Google uses 2/100, AOSP 4, palantir 120, Checkstyle `LineLength` defaults to 80." | Draft §"Limitations & when NOT to reach for it" — "Style values are choices, not truths" |
| Format/lint split CONCEPT callout | Draft CONCEPT callout verbatim: "A linter *detects* a deviation and asks a human to fix it; a formatter *computes* the canonical rendering and writes it." | Draft §"Layer 3" — CONCEPT callout |
| Convention vs meaning CONCEPT callout | Draft CONCEPT callout: "Every naming/style decision splits into a typographical part (a tool checks it with a regex) and a semantic part (only a person can judge it)." | Draft §"Layer 1" — CONCEPT callout |
| `spotless:check` fails the build on whitespace drift | Spotless `check` goal is the CI gate; draft §"Layer 3" and §"When to use what". | Draft §"When to use what" |
| `-Xdoclint all,-missing` | JDK `-Xdoclint` flag; "documented middle path is `<doclint>all,-missing</doclint>`." | Draft §"The fourth thing: comments" — Javadoc paragraph |
| Human enforcer items (naming truth, member order explainability, why-comment, rename-before-comment) | The hard part of each layer as described in the draft's §"Layer 1", §"Layer 2", and §"The fourth thing" sections. | Draft §§ "Layer 1 — the hard part", "Layer 2 — Member ordering", "The fourth thing — comments (a contested practice)" |

---

## Atoms not included / intentionally out of scope

- School A (Clean Code) vs School B (APoSD) comment debate: the figure carries the existence of two schools and the "rename before commenting" resolution, but does not adjudicate between them — consistent with the draft's two-schools-no-crown presentation.
- SonarQube `java:S125` (commented-out code): applies to the comments section, not the axis diagram; editorial choice to keep the figure single-idea.
- JEP 467 (`///` Markdown comments, JDK 23): marked AHEAD-OF-PIN in draft; excluded from the figure.
- `{@snippet}` (JEP 413): Javadoc tooling detail, not structural to the axis diagram.

---

Source-trace status: **YES** — every label, rule ID, regex, tool flag, section number, and quoted claim in the diagram traces to a line in the chapter draft, which itself cites the pinned authority sources (Checkstyle 13.6.0, PMD 7.25.0, SonarQube 2026.1 LTA, Spotless 3.6.0, google-java-format 1.35.0, palantir-java-format, Google Java Style Guide §§3–5, EditorConfig spec).

Verified against the pinned authority set (00-strategy/SOURCE-PIN.md) at the pins in SOURCE-PIN.md.  
Draft pin date: 2026-06-20.
