# Fig 101.1 sources — The performance discipline loop

Chapter 43 (dossier key 101, folds 102, 103, 51, 104) · draft v1 · `03-drafts/101_performance_profiling_memory_benchmarking/101_performance_profiling_memory_benchmarking_v1.md`

| Label / element in diagram | Claim / atom | Pinned source path |
|---|---|---|
| Title "Fig 101.1 — The performance discipline loop" | Identifying label | Draft v1 dossier comment line 6: "Fig 101.1 — the performance discipline loop: set target → profile → benchmark the fix (JMH) → gate against regression"; research dossier §6 |
| Subtitle "Measure, don't guess — the four-step loop, governed by a standing constraint." | Framing | Draft v1 §Overview: "performance is a quality attribute to measure, not guess at — set a real target, profile…" |
| **Governing constraint — "Most code does not enter this loop"** | Claim: premature optimization dominant failure | Draft v1 §"Performance as a measured requirement": "When it does *not* matter describes most code"; dossier key 101 §"Evidence AGAINST": "Premature/misplaced optimization is the dominant failure" |
| Governing constraint — "trades readability for imaginary gains" | Claim | Draft v1 §"Performance as a measured requirement": "adds complexity and bugs for no gain — a quality anti-pattern that trades the readability of Chapter 2 for imaginary speed" |
| Governing constraint — "(Knuth…)" attribution | Knuth premature-optimization citation | Draft v1 §"Performance as a measured requirement": "Knuth's premature optimization names the dominant failure"; dossier key 101 §"Mechanism": "Premature optimization (Knuth)"; ⚠ verbatim at pin |
| **Step 1 — Set a real target** | Loop step label | Draft v1 CONCEPT: "set the target"; dossier key 101 §Mechanism: "define targets…tied to actual needs" |
| p99 latency | Target metric | Draft v1 §"Performance as a measured requirement": "p99 latency, throughput, memory/RSS, startup time"; dossier key 101 §Mechanism |
| Throughput (req/s) | Target metric | Same source as above |
| Memory / RSS | Target metric | Same source as above |
| Startup time | Target metric | Same source as above |
| "'Make it fast' is unmeasurable." | Claim | Draft v1 §"Performance as a measured requirement": "'Make it fast' is unmeasurable and ungateable" |
| **Step 2 — Profile — find the real hotspot** | Loop step label | Draft v1 §"Profiling: find the real hotspot" heading; dossier key 102 |
| `JFR` — built-in, ~1% overhead, production-safe | Tool + overhead claim | Draft v1 §"Profiling…": "JFR is built into the JVM, runs at roughly 1% overhead (verify at the pin), and is designed for production profiling"; dossier key 102: "JFR (built-in, ~1% overhead, production)" — ⚠ verify at pin |
| CPU/alloc/locks/GC/IO (JFR event categories) | JFR capability | Draft v1 §"Profiling…": "records events (CPU, allocation, locks, GC, I/O)"; dossier key 102 |
| `async-profiler` — flame graphs; avoids safepoint bias | Tool + capability | Draft v1 §"Profiling…": "async-profiler is a low-overhead sampling profiler that produces flame graphs…avoids the safepoint bias of older samplers"; dossier key 102 |
| async-profiler "JFR-compatible output" | Capability claim | Draft v1 §"Profiling…": "Its output is JFR-compatible"; dossier key 102 |
| "Read flame graph top-down: widest frame = where time actually goes" | Flame graph reading instruction | Draft v1 CONCEPT: "A flame graph's width is time (or samples) in a call path; read it top-down and look for the widest frames — those are where the cycles actually go" |
| "Profile under realistic load" | Discipline | Draft v1 CONCEPT: "profile under realistic load"; dossier key 102: "Profile under REALISTIC load" |
| "Profiling shows where — not why or the fix." | Honest limitation | Draft v1 §"Profiling…": "profiling shows where, not why or the fix"; dossier key 102 §LIMITS |
| **Step 3 — Benchmark the fix — JMH** | Loop step label | Draft v1 §"Honest benchmarking: JMH and the lies it defeats"; dossier key 104 |
| `@Benchmark` + `@Warmup` + `@Fork` | JMH annotations | Draft v1 §"Honest benchmarking…": "warmup iterations let the JIT compile to steady state; forking runs each benchmark in a fresh JVM"; dossier key 104: "@Benchmark/Warmup/Fork" |
| `@State` non-final inputs (defeats constant folding) | JMH idiom | Draft v1 §"Honest benchmarking…": "read inputs from non-final @State fields so they cannot be constant-folded"; dossier key 104: "@State-non-final" |
| `Blackhole.consume` / return result (defeats DCE) | JMH idiom | Draft v1 §"Honest benchmarking…": "return it (or sink it through a Blackhole) so it cannot be eliminated"; dossier key 104: "Blackhole-or-return" |
| "Run offline; report error + environment (JDK, hardware)" | JMH best practice | Draft v1 §"Honest benchmarking…": "Benchmarks belong in a separate run"; §"Micro is not macro…": "state the hardware and JDK (results do not transfer across machines or versions)" |
| "JMH 1.37 (OpenJDK)" | Version number | SOURCE-PIN.md §3 table: "JMH (benchmarking) 1.37 github.com/openjdk/jmh ✅ pinned"; dossier key 104: "JMH OpenJDK live-line 1.37" |
| "Naive `System.nanoTime()` loops measure the optimizer, not the code." | Claim — naive benchmark lies | Draft v1 §"Honest benchmarking…": "a hand-rolled timer measures the optimizer, not the algorithm (the hook's phantom 10×)"; dossier key 104: "naive benchmarks LIE (JIT DCE…)" |
| JMH samples 08/09/10 references | JMH canonical pitfall samples | Draft v1 §"Honest benchmarking…": "The pitfalls are well-documented (JMH ships them as canonical samples): returning nothing (dead code) [08], using constant inputs [09 Blackholes / 10 ConstantFold]"; dossier key 104: "samples 08 DeadCode/09 Blackholes/10 ConstantFold" |
| **Step 4 — Gate against regression** | Loop step label | Draft v1 §"Hand-off to the next chapter": "the performance-regression gate"; dossier key 101 §Mechanism: "gate against regression (key 105)" |
| "Benchmark threshold in CI baseline" | Regression gate mechanism | Draft v1 §"Hand-off to the next chapter": "benchmark and latency thresholds gated against a baseline"; dossier key 101 §Mechanism: "gate against regression (key 105)" |
| "Load / macro testing for system-level truth" | Load test role | Draft v1 §"Micro is not macro…": "Production profiling and load testing are the truth"; §"Hand-off…": "load and macro testing for system-level behavior" |
| "Environment control — same JDK + hardware" | Gate discipline | Draft v1 §"Micro is not macro…": "state the hardware and JDK (results do not transfer across machines or versions)"; dossier key 104: "hardware/JVM-specific" |
| "Micro is not macro" caveat in Step 4 | Loud caveat | Draft v1 CONCEPT heading: "Micro is not macro — the loudest caveat"; dossier key 104 §LIMITS: "micro≠macro" |
| **Return arrow — "regression caught → reset target & re-profile"** | Loop feedback path | Draft v1 dossier comment line 6 / §Mechanism: "Loop: set-target → profile (§B) → benchmark (§D) → gate against regression (Ch 44 key 105)" — the loop is cyclic by definition |
| **Instrument #1 — "the guessed hotspot"** | Recurring villain named in draft | Draft v1 §Deep dive: "the unifying difficulty is the instrument that lies…Developer intuition about where time goes is an instrument, and it lies (confidently, specifically, and almost always)" |
| "Developer intuition…is reliably wrong" | Claim | Draft v1 §"Profiling…": "Profiling answers 'where does the time and allocation actually go?' — and the answer is reliably not where intuition points"; dossier key 102 |
| **Instrument #2 — "the naive benchmark"** | Recurring villain named in draft | Draft v1 §Hook: "the unguarded benchmark"; §Deep dive: "A System.nanoTime() loop is an instrument, and it lies" |
| "dead-code elimination" + "constant folding" + no warmup | JIT lie mechanisms | Draft v1 §"Honest benchmarking…" CONCEPT: "The JIT eliminates code whose result is never used (dead-code elimination), folds constant inputs to a precomputed answer (constant folding), and runs cold, un-compiled code if there is no warmup"; dossier key 104 |
| JMH "warmup + fork + Blackhole defeat each lie" | Claim | Draft v1 §"Honest benchmarking…": "JMH (the OpenJDK Java Microbenchmark Harness) defends against exactly these: warmup iterations…forking…the result is made observable" |
| **Allocation hygiene caveat cell** | Memory hygiene concept | Draft v1 §"Memory and allocation hygiene"; dossier key 103 |
| "Churn in hot paths (autoboxing, string concat in loops, unnecessary stream materialization)" | Allocation hygiene atoms | Draft v1 §"Memory and allocation hygiene": "autoboxing in a tight loop, string concatenation in a loop (use StringBuilder), unnecessary stream materialization, per-request object churn" |
| "Escape analysis may already handle it" | Claim | Draft v1 CONCEPT: "Escape analysis means 'allocation' is not always heap allocation — another reason to measure"; "an allocation in source code may cost nothing at runtime" |
| "Object pooling is usually counterproductive on modern JVMs" | Limitation | Draft v1 CONCEPT: "object pooling is usually counterproductive on modern JVMs: allocation is cheap and the GC is good"; dossier key 103 §LIMITS |
| Caption — JFR overhead ~1% | Metric | Draft v1 §"Profiling…": "roughly 1% overhead (verify at the pin)"; dossier key 102 — ⚠ verify at pin |
| Caption — JMH 1.37 | Version | SOURCE-PIN.md §3 "JMH (benchmarking) 1.37 ✅ pinned" |
| Caption — JMH samples 08/09/10 | Canonical sample numbers | Dossier key 104: "samples 08 DeadCode/09 Blackholes/10 ConstantFold"; draft v1 §"Honest benchmarking…" |
| Caption — ISO 25010 Performance Efficiency | Standard citation | SOURCE-PIN.md §1: "ISO/IEC 25010 (quality model) 2023"; draft v1 §"Overview": "ISO 25010 names Performance Efficiency" |
| Caption — Knuth premature-optimization | Attribution | Dossier key 101 §Mechanism; draft v1 §"Performance as a measured requirement" — ⚠ verbatim at pin |

---

Source-trace status: YES

Verified against the pinned authority set (00-strategy/SOURCE-PIN.md) at the pins in SOURCE-PIN.md.

Every label, step name, tool name, annotation, metric, version number, and caveat in Fig 101.1 traces to an explicit passage in `03-drafts/101_performance_profiling_memory_benchmarking/101_performance_profiling_memory_benchmarking_v1.md` (the draft) or its research dossier `02-research/101_performance_quality_attribute/101_performance_quality_attribute_RESEARCH.md`, which is itself verified at SOURCE-PIN.md. No atom was introduced by the figure that is not present in the draft or dossier.

Atoms marked ⚠: JFR ~1% overhead (verify at pin — stated in draft as "(verify at the pin)"), Knuth premature-optimization (verbatim attribution flagged ⚠ in dossier §7 gap-filling), JMH version 1.37 (SOURCE-PIN.md §3 ✅ pinned). No ahead-of-pin claims (Valhalla / value types deliberately excluded as AHEAD-OF-PIN).
