# Figure source-trace — Diff-scoping: one discipline across coverage, the platform check, and PR feedback (Ch 34)

- Files: fig80_1.html → fig80_1.png. Family: Concept-map / comparison-contrast (three facets × two scopes). Method: HTML → render.mjs → crop (#figure, scale 3).
- One idea: One discipline — DIFF-SCOPING (focus everything on the code this change touches) — runs through all three facets of the developer-facing gate: gate coverage on NEW code (ratchet + mutation backstop), run the platform's required check against the PR's changes, and comment only on the CHANGED lines. Whole-repo scope fails identically in every facet (unreachable / a stale verdict / muted noise → the gate gets disabled); diff scope makes the gate fair, adoptable, and actionable.
- Chapter: key 80 (folds 77 + 78) = Ch 34, Part IX (01-index/FINAL_INDEX.md line 123).
- Verified against Java Code Quality pinned authority set (00-strategy/SOURCE-PIN.md, pinned 2026-06-20).

| Label in diagram | Claim | Pinned source (SOURCE-PIN.md) |
|---|---|---|
| Title — "Diff-scoping: one discipline across coverage, the platform check, and PR feedback" | The chapter's single unifying idea is diff-scoping running through all three facets | Draft 80_..._v1.md §"Deep dive: diff-scoping" lines 80–82 ("the same discipline runs through all three: diff-scoping"); Overview one-idea line 38 |
| Subtitle — "Focus everything on the code this change touches" | Definition of diff-scoping | Draft v1 line 80 ("diff-scoping, focusing everything on the code this change actually touches") |
| Subtitle — "a failed gate gets disabled" | A gate that fails (unreachable/noisy) gets routed around / disabled | Draft v1 line 82 ("both get the gate disabled"); fig73_1 precedent "gate fatigue" |
| Scope column — "Whole-repo scope — measures everything — fails" | Whole-repo scope fails in all three forms | Draft v1 line 82 ("Whole-repo scope fails identically in all three forms") |
| Scope column — "Diff scope — only what this change touched — works" | Diff-scope is the working discipline | Draft v1 line 82 ("Scope to the diff and the gate becomes fair, adoptable, and actionable") |
| FACET 1 — Coverage gate · "JaCoCo 0.8.15 · Sonar gate" | The coverage facet is implemented with JaCoCo + a Sonar new-code gate | Draft v1 line 21 (research foot: "JaCoCo + Sonar new-code coverage gate"); SOURCE-PIN §3 "JaCoCo (coverage) — 0.8.15"; §2 "SonarQube Server 2026.1 LTA" |
| JaCoCo 0.8.15 | Coverage tool version | SOURCE-PIN §3: "JaCoCo (coverage) — 0.8.15" (pinned 2026-06-20) |
| Sonar gate | Sonar quality gate / new-code coverage condition ("clean as you code") | SOURCE-PIN §2 "SonarQube / SonarLint / Sonar rules — Server 2026.1 LTA"; draft v1 line 46 + dossier 80 §2 (Sonar new-code gate, key 76). ⚠ exact Sonar new-code-coverage condition wording carries the draft's verify-at-pin flag (draft line 10) |
| FACET 1 whole-repo cell — "Absolute whole-repo threshold (e.g. 80% of all lines)" | An absolute whole-repo coverage gate is the failing pattern | Draft v1 lines 44, 88 ("an 80% gate on a legacy codebase is unreachable … or gamed"); dossier 80 §2 |
| FACET 1 whole-repo cell — "Unreachable on a legacy codebase / gamed with assertion-free tests on getters" | Whole-repo coverage gate is unreachable on legacy or gamed | Draft v1 lines 19, 23, 44 ("instantiate an object, call its getters, and assert nothing"); Hook lines 19–23 |
| FACET 1 whole-repo verdict — "unreachable / gamed" | The whole-repo coverage outcome | Draft v1 line 44 ("fail both ways: … unreachable … and … gamed") |
| FACET 1 diff cell — "New-code coverage + ratchet" | The working coverage strategy is new-code focus + ratcheting | Draft v1 lines 46–47 ("New-code focus … Ratcheting … overall coverage only goes up"); dossier 80 §2 |
| FACET 1 diff cell — "Mutation backstop on new code" | Mutation testing backs coverage to close the assertion-free loophole | Draft v1 line 48 ("A mutation backstop … gate on the mutation score of new code"); dossier 80 §2 (key 47) |
| PITest 1.25.3 | Mutation testing tool version | SOURCE-PIN §3: "PITest (mutation) — 1.25.3" (pinned 2026-06-20); draft v1 line 128 ("optional PITest new-code mutation threshold") |
| FACET 1 diff cell — "Adoptable from day one; improves where work happens" | New-code focus makes the gate adoptable and improves where work happens | Draft v1 line 46 ("adoptable on any codebase immediately and steadily improves quality exactly where work is happening") |
| FACET 1 diff verdict — "adoptable, converges up" | Ratchet bends the curve upward | Draft v1 line 47 ("bends the curve upward without a big-bang test-writing project") |
| FACET 2 — Required check · "runs on the CI platform" | The platform's required check is the policy facet | Draft v1 line 62 ("required status checks feeding branch protection"); dossier 77 §2 |
| FACET 2 whole-repo cell — "A green check read as 'the code is good'" | Mistaking a green check for good code is the failing pattern | Draft v1 lines 84, 95 ("Treat the platform's green check as proof of quality … mistaken 'the configured checks passed' for 'the code is good'") |
| FACET 2 whole-repo cell — "Green means only that the configured checks passed — not that the design is sound" | A green check is not good code | Draft v1 line 95 ("A green PR check is not good code … It means the configured checks passed on the diff") |
| FACET 2 whole-repo verdict — "stale verdict" | Whole-repo verdict is slow / not the PR's job | Draft v1 line 82 (whole-repo scope fails); line 84 ("the gate measures and enforces; it does not, by itself, create quality") |
| FACET 2 diff cell — "Required check runs against the PR's changes, JDK 21 + 25 matrix, with caching" | The platform check runs per-PR with a JDK matrix and caching | Draft v1 lines 62, 82 ("required checks run against the PR's changes"; "JDK matrix builds (testing on Java 21 and 25) … caching"); dossier 77 §2 |
| JDK 21 + 25 matrix | Test on Java 21 (anchor LTS) AND 25 (forward LTS) | SOURCE-PIN runtime baseline: "Java 21 (LTS)" anchor + "Java 25 (LTS)" forward; OpenJDK 21.0.11 + 25.0.3; draft v1 line 62 |
| FACET 2 diff cell — "the same gate design on any platform" | The gate design ports across platforms; only syntax differs | Draft v1 lines 54, 62 ("the gate design ports across all of them … the gates themselves are identical"); dossier 77 §1 |
| GitHub Actions · GitLab CI · Jenkins — "design ports; syntax differs" | The three dominant Java CI platforms, presented neutrally, no crown | Draft v1 lines 56–60 (platform table); dossier 77 §2; SOURCE-PIN §5 "GitHub Actions / GitLab CI / Jenkins" (docs as of 2026-06) |
| FACET 2 diff verdict — "per-PR verdict" | The required check reports a per-PR verdict | Draft v1 line 82 ("report a per-PR verdict") |
| FACET 3 — PR automation · "Checks API / MR widgets" | PR feedback rendered via platform Checks API (GitHub) / MR widgets (GitLab) | Draft v1 line 68 ("The platform Checks API (GitHub) and MR widgets (GitLab) render findings on the diff"); dossier 78 §2 |
| FACET 3 whole-repo cell — "Whole-repo findings posted as PR comments, or buried in a CI log nobody reads" | The failing feedback patterns | Draft v1 lines 66, 74 ("A gate verdict buried in a CI log gets ignored … Whole-repo findings posted as PR comments are noise") |
| FACET 3 whole-repo cell — "Comment overload → developers mute the bot, and a muted bot is a disabled gate" | Comment overload gets bots muted | Draft v1 lines 74, 76, 93 ("a muted bot is a disabled gate … comment overload is the killer"); dossier 78 §4 (key 39) |
| FACET 3 whole-repo verdict — "muted noise" | The whole-repo feedback outcome | Draft v1 line 82 ("a whole-repo finding report is noise") |
| FACET 3 diff cell — "Inline, only on the changed lines. De-dupe by niche — each tool owns one job" | Diff-scoped inline annotation + de-dupe responsibilities | Draft v1 lines 66, 76 ("inline on the pull request, on the changed line … de-dupe responsibilities"); dossier 78 §2/§4 |
| reviewdog (lint, diff-filtered) | reviewdog wraps any linter and comments only on changed lines | Draft v1 line 70 ("reviewdog wraps any linter's output and posts comments only on changed lines, diff-filtered"); dossier 78 (github.com/reviewdog/reviewdog). ⚠ reviewdog version carries the dossier's verify-at-pin flag (dossier 78 §7); named without a version per pin |
| Sonar PR decoration (the gate) | SonarQube PR decoration runs the Sonar gate on the PR and annotates new issues | Draft v1 line 72 ("SonarQube PR decoration runs the Sonar quality gate on the PR and annotates the new issues plus the gate status"); dossier 78 §2 (key 35); SOURCE-PIN §2 SonarQube Server 2026.1 LTA |
| Danger (process rules) | Danger runs per-PR rule scripts for process checks | Draft v1 line 71 ("Danger runs rule scripts per PR for process checks — description, changelog, size, tests touched"); dossier 78 (danger.systems). ⚠ Danger version carries the dossier's verify-at-pin flag (dossier 78 §7); named without a version per pin |
| FACET 3 diff verdict — "gets fixed" | Findings on the diff get fixed | Draft v1 lines 66, 68 ("the same finding shown inline … gets fixed") |
| Summary (whole-repo) — "the gate gets disabled … a net negative" | All three whole-repo failures end in a disabled gate | Draft v1 line 82 ("both get the gate disabled"); fig73_1 precedent ("A gate the team routes around is a net negative") |
| Summary (diff) — "fair · adoptable · actionable" | Diff-scope makes the gate fair, adoptable, actionable | Draft v1 line 82 ("the gate becomes fair … adoptable … and actionable … all three properties reduce to scoping to the change") |
| Note — "A measure that becomes a target stops being a good measure (Goodhart)" | Goodhart's law underlies the coverage trap | Draft v1 lines 23, 84 ("Goodhart's law in a CI config … gating on a proxy incentivizes optimizing the proxy"); folklore keys 04/48 (Ch 1/23) |
| Note — "Coverage is a floor, not a goal" | Coverage = floor; reach for mutation + review for quality | Draft v1 line 50 (CONCEPT: "Coverage is a floor, not a goal … reaches for mutation testing and review (Chapter 84) for quality") |
| Note — "Bots are not review" | Bots catch mechanical issues, not design/correctness/right-change | Draft v1 lines 74, 94 ("Bots are not review … not whether the design is sound or the change is the right one") |
| Note — "a green check is not good code" | A green check means configured checks passed, not good code | Draft v1 line 95 ("A green PR check is not good code") |
| Note — "human judgment (Ch 84) is where quality actually lives" | Human review is the irreplaceable layer the bots free up | Draft v1 lines 84, 103 ("the human judgment it frees up (Chapter 84) is where quality actually lives") |
| Caption cross-refs — Ch 23 (keys 47, 48); Ch 35 (key 81); Ch 84; Ch 1/23 (keys 04, 48) | Routing to the coverage/mutation tools, required checks/merge queue, human review, and the folklore | Draft v1 lines 36, 99–103, 119, 124–126 (Routes / Alternatives / Hand-off); dossier 80 §"Learnings" cross-refs |

Source-trace status: YES (every label/atom/verdict traces to draft v1 prose or to a SOURCE-PIN.md pinned version. Two PR-tool names — reviewdog, Danger — are deliberately shown WITHOUT a version number because the dossier (78 §7) and draft (line 10) carry an explicit ⚠ verify-at-pin flag on their versions and SOURCE-PIN §5 does not pin a fixed version for either; naming-without-version is the on-pin-honest choice. The Sonar new-code-coverage condition wording likewise carries the draft's verify-at-pin flag and is depicted only as "Sonar gate"/"new-code coverage," not as a quoted condition string.)

Verified against the pinned authority set (00-strategy/SOURCE-PIN.md) at pins dated 2026-06-20.

## Accessibility

- Alt-text: A grid showing diff-scoping make coverage, the platform check, and PR feedback work where whole-repo scope fails.
- Long-description: The figure is a grid read top-to-bottom in three facet rows, each split into two columns by scope. A header band labels the two columns: the left column "Whole-repo scope" is drawn with a dashed border and light fill and tagged (in stop-red) "measures everything — fails"; the right column "Diff scope" is drawn with a strong fill and an orange accent bar and tagged "only what this change touched — works." The corner cell reads "The three facets the developer meets / Each enforced against one of two scopes." Row 1, Coverage gate (left stub names JaCoCo 0.8.15 and the Sonar gate): the whole-repo cell describes an absolute whole-repo threshold (e.g. 80% of all lines) that is unreachable on a legacy codebase or gamed with assertion-free tests on getters, with a dashed stop-red verdict pill "unreachable / gamed"; the diff cell describes new-code coverage plus a ratchet (overall coverage may only go up) with a mutation backstop on new code via PITest 1.25.3, adoptable from day one, with a green verdict pill "adoptable, converges up." Row 2, Required check (left stub "runs on the CI platform"): the whole-repo cell describes a green check read as "the code is good" — green means only that the configured checks passed — with a dashed verdict "stale verdict"; the diff cell describes a required check run against the PR's changes on a JDK 21 + 25 matrix with caching, the same gate design ported across GitHub Actions, GitLab CI, and Jenkins (syntax differs), with a green verdict "per-PR verdict." Row 3, PR automation (left stub "Checks API / MR widgets"): the whole-repo cell describes whole-repo findings posted as PR comments or buried in a CI log, producing comment overload so developers mute the bot (a muted bot is a disabled gate), with a dashed verdict "muted noise"; the diff cell describes inline annotations only on the changed lines, de-duped by niche across reviewdog (lint, diff-filtered), Sonar PR decoration (the gate), and Danger (process rules), with a green verdict "gets fixed." Below the rows, two spanning summary strips: under whole-repo (dashed) "Whole-repo scope → the gate gets disabled" — unreachable, a stale verdict, muted noise all end the same way, a gate developers route around, a net negative; under diff (accent bar) "Diff scope → fair · adoptable · actionable." A legend distinguishes the dashed whole-repo swatch (the scope that fails) from the accent-barred diff swatch (the scope that works). A closing note (orange left border) states Goodhart's law — a measure that becomes a target stops being a good measure — and that coverage is a floor not a goal (reach for mutation and human review for quality), bots are not review, a green check is not good code, and the gate is scoped to the diff so it is fair, delivered to the developer so it is actionable, and understood as a floor so it is not gamed, with human judgment (Chapter 84) where quality actually lives. A caption cites key 80 (Ch 34, folding 77 + 78), the draft sections, the pinned tool versions, and the sources sidecar.
- Grayscale / contrast / legibility: Reads in grayscale. The two scope columns are told apart by a dashed border and light fill (whole-repo) versus a solid border with a left accent bar (diff), and each column header repeats the verdict in words ("measures everything — fails" / "only what this change touched — works"), so the contrast survives without color; the legend restates both swatches in text. The per-cell verdict pills are labeled ("unreachable / gamed", "adoptable, converges up", "stale verdict", "per-PR verdict", "muted noise", "gets fixed") rather than coded only by color, and the three facet rows are fixed by their left-stub labels and position. Dark ink on light tints holds ample contrast; tool and platform names (JaCoCo 0.8.15, PITest 1.25.3, GitHub Actions, GitLab CI, Jenkins, reviewdog, Danger) are real selectable text, not a screenshot, and no meaning rests on hue alone.
