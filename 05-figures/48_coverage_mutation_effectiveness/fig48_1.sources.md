# Fig 48.1 sources — Coverage vs mutation: the assertion gap on the same covered line

Chapter 23 (dossier key 48, folds 47, Part V) · draft v1 · `03-drafts/48_coverage_mutation_effectiveness/48_coverage_mutation_effectiveness_v1.md`
Dossier: `02-research/48_coverage_mutation_effectiveness/` (inline in draft header comment)

| Label / element in diagram | Claim / atom | Pinned source path |
|---|---|---|
| Title "Fig 48.1 — Coverage vs mutation: the assertion gap on the same covered line" | Identifying label | Draft v1 §Deep dive heading + chapter thesis sentence |
| Subtitle "100% line coverage does not move when assertions are added — mutation score does" | Central invariant of the deep-dive section | Draft v1 §Deep dive: "while line coverage stays exactly 100%. That invariance is the whole point: coverage did not move because the code executed was identical; what changed was whether the tests checked it." |
| "Weak test — assertion-free" / BEFORE column | Weak-test scenario | Draft v1 §Deep dive: "Write a weak test that calls it and asserts only that the result is non-null." |
| Code: `assertNotNull(result)` — only assertion | Weak-test assertion pattern | Draft v1 §Deep dive: "a weak test that calls it and asserts only that the result is non-null" |
| Code: `Discount.apply(10, 100.0, 0.10)` — method signature and arguments | Companion module spec: boundary `qty >= 10`, `price * (1 - rate)` | Draft v1 §Companion module spec: "a Discount.apply method (boundary qty >= 10 + price*(1-rate) + early return)" |
| JaCoCo LINE = 100%, PASS | Weak test earns full line coverage | Draft v1 §Deep dive: "JaCoCo reports ~100% line coverage: every line ran." |
| JaCoCo BRANCH < 100%, FAIL gate | Weak test does not exercise both sides of boundary | Draft v1 §Companion module spec: "weak test … JaCoCo ~100% line coverage yet BRANCH < 100%" |
| Mutation score = low, FAIL gate | Weak test fails mutation gate | Draft v1 §Companion module spec: "PITest mutants SURVIVED (low mutation score, fails mutationThreshold)" |
| CONDITIONALS_BOUNDARY mutator: `>=` → `>` on `qty >= 10`, SURVIVED (weak) | PITest DEFAULT mutator; surviving mutant on covered line | Draft v1 §Deep dive: "the CONDITIONALS_BOUNDARY mutant (>= → >) survives"; PITest 1.25.3 docs (pitest.org, DEFAULTS mutator list) — SOURCE-PIN §3 row PITest 1.25.3 |
| MATH mutator: `*` → `/` in `price * (1 - rate)`, SURVIVED (weak) | PITest DEFAULT mutator; surviving mutant | Draft v1 §Deep dive: "the MATH mutant (* → /) survives"; PITest 1.25.3 docs (pitest.org) — SOURCE-PIN §3 row PITest 1.25.3 |
| VOID_METHOD_CALLS mutator: delete void call (early return), SURVIVED (weak) | PITest DEFAULT mutator; surviving mutant | Draft v1 §Deep dive: "a deleted method call goes undetected"; PITest 1.25.3 docs (pitest.org) — SOURCE-PIN §3 row PITest 1.25.3 |
| NULL_RETURNS mutator: return null instead of result, SURVIVED (weak) | PITest DEFAULT mutator (NULL_RETURNS is in DEFAULTS) | PITest 1.25.3 docs (pitest.org, DEFAULTS: NULL_RETURNS listed) — SOURCE-PIN §3 row PITest 1.25.3; Draft v1 §Back matter: "EMPTY\|FALSE\|TRUE\|NULL\|PRIMITIVE_RETURNS" |
| Verdict (weak): PITest verbatim "does not check that your tests are actually able to detect faults in the executed code" | PITest self-statement (verbatim) | Draft v1 §Mutation testing: "PITest states it plainly: coverage 'does not check that your tests are actually able to detect faults in the executed code.'"; pitest.org (basic-concepts) — SOURCE-PIN §3 row PITest 1.25.3 |
| "Strong test — boundary + value assertions" / AFTER column | Strong-test scenario | Draft v1 §Deep dive: "Add the strong test — AssertJ assertions on the boundary (qty == 9 versus qty == 10) and the computed price" |
| Code: `assertThat(...).isCloseTo(90.0, within(0.001))` and boundary check `qty=9` | AssertJ value + boundary assertion pattern | Draft v1 §Deep dive: "AssertJ assertions on the boundary (qty == 9 versus qty == 10) and the computed price"; AssertJ 3.27.7 — SOURCE-PIN §3 row AssertJ 3.27.7 |
| JaCoCo LINE = 100%, PASS (strong) | Line coverage unchanged after adding assertions | Draft v1 §Deep dive: "while line coverage stays exactly 100%" |
| JaCoCo BRANCH = 100%, PASS gate (strong) | Strong test exercises both sides of boundary | Draft v1 §Companion module spec: "strong AssertJ boundary/value test → mutants KILLED, score climbs, line coverage unchanged at 100%" |
| Mutation score = high, PASS gate (strong) | Mutants killed after strong assertions | Draft v1 §Companion module spec: "strong AssertJ boundary/value test → mutants KILLED, score climbs" |
| CONDITIONALS_BOUNDARY: KILLED (strong) | Boundary assertion kills this mutant | Draft v1 §Deep dive: "Add the strong test … and the mutants die, the score climbs" |
| MATH: KILLED (strong) | Value assertion `isCloseTo(90.0)` kills this mutant | Draft v1 §Deep dive: same |
| VOID_METHOD_CALLS: KILLED (strong) | Early-return deletion caught by value assertion | Draft v1 §Deep dive: same |
| NULL_RETURNS: KILLED (strong) | Null return caught by `isCloseTo` (not `assertNotNull`) | Draft v1 §Deep dive: same |
| Verdict (strong): "LINE coverage stayed at 100% — the same lines ran. BRANCH rose to 100%…" | Invariant explained | Draft v1 §Deep dive: "coverage did not move because the code executed was identical; what changed was whether the tests checked it" |
| Synthesis — JaCoCo tool tag `org.jacoco:jacoco-maven-plugin` | Maven GAV coordinate | Draft v1 §Back matter: "Maven org.jacoco:jacoco-maven-plugin goals prepare-agent/report/check"; SOURCE-PIN §3 row JaCoCo 0.8.15 |
| JaCoCo "Question: what did the tests touch?" | Coverage semantics | Draft v1 hook + overview: "Coverage (JaCoCo) answers what did the tests touch?" |
| "Gate every commit — cheap (single instrumented run, Java agent)" | Coverage cost model | Draft v1 §CONCEPT: "Coverage answers 'what executed?': a necessary lower bound, cheap enough to gate every commit" |
| "Read BRANCH (C1), not only LINE" | Counter guidance | Draft v1 §CONCEPT: "Read BRANCH, not only LINE…BRANCH coverage catches the untested decision path that LINE hides" |
| Check goal: element BUNDLE, counter BRANCH, value COVEREDRATIO, haltOnFailure default true | JaCoCo check rule model (verbatim elements) | Draft v1 §Gate / Back matter: check rule model verbatim from check-mojo.html; JaCoCo 0.8.15 docs — SOURCE-PIN §3 row JaCoCo 0.8.15 |
| "JaCoCo 0.8.15 (Java 21 support: 0.8.11; Java 25: 0.8.14)" | Version + JDK-support mapping | Draft v1 §Back matter + header comment: "Java21→0.8.11, Java25→0.8.14 (0.8.13 experimental)"; SOURCE-PIN §3 row JaCoCo 0.8.15 pinned 2026-06-20 |
| Synthesis — PITest tool tag `org.pitest:pitest-maven` goal `mutationCoverage` | Maven GAV + goal name | Draft v1 §Back matter: "org.pitest:pitest-maven goal mutationCoverage"; pitest.org — SOURCE-PIN §3 row PITest 1.25.3 |
| PITest "Question: would the tests catch a bug?" | Mutation testing semantics | Draft v1 §CONCEPT: "Mutation score answers 'would a break be caught?'" |
| "Run periodically — expensive (covering tests once per mutant)" | Mutation cost model | Draft v1 §CONCEPT + §Limitations: "a separate, scoped CI stage (nightly, or on changed modules)" |
| 7 statuses: KILLED / SURVIVED / NO_COVERAGE / TIMED_OUT / MEMORY_ERROR / RUN_ERROR / NON_VIABLE | PITest statuses (verbatim, basic-concepts) | Draft v1 §Mutation testing: "seven statuses — KILLED, SURVIVED, NO_COVERAGE, TIMED_OUT, MEMORY_ERROR, RUN_ERROR, NON_VIABLE"; pitest.org basic-concepts — SOURCE-PIN §3 row PITest 1.25.3 |
| "Mutation score = killed ÷ total. Test strength = killed ÷ mutants-with-coverage." | Metric formulae | Draft v1 §Mutation testing: "mutation score is the percentage killed … test strength (killed ÷ mutants-with-coverage)"; pitest.org — SOURCE-PIN §3 row PITest 1.25.3 |
| "pitest-junit5-plugin REQUIRED for JUnit 5 (Jupiter) — absent = silent no-coverage" | Setup trap | Draft v1 §Mutation testing: "running JUnit 5 (Jupiter) tests requires the pitest-junit5-plugin; without it, the tests silently report no coverage"; pitest.org — SOURCE-PIN §3 row PITest 1.25.3 |
| "PITest 1.25.3" | Pinned version | SOURCE-PIN §3 row: PITest 1.25.3 pinned at pitest.org |
| Invariant callout: "LINE coverage did not move … coverage did not move; assertion strength did" | Central thesis verbatim | Draft v1 §Deep dive invariant passage (verbatim) |
| Invariant callout: "coverage is a gap-finder (necessary floor); mutation score is a fault-detection gauge (stronger signal, higher cost). Run both; crown neither." | Complementary pair framing (neutrality) | Draft v1 §CONCEPT: "Coverage and mutation score are complementary, not ranked… Neither is crowned" |
| Routing row: "Gate policy → CI part (Ch 38) | Flakiness corrupts score → Ch 20 | Over-mocking flatters score → Ch 21" | Chapter routing | Draft v1 §Routes: "gate POLICY … → CI part (80); over-mocking flatters score → Ch21(44); flakiness corrupts score → Ch20(49)" |
| DEFAULTS mutator list in routing row: CONDITIONALS_BOUNDARY / INCREMENTS / INVERT_NEGS / MATH / NEGATE_CONDITIONALS / VOID_METHOD_CALLS / EMPTY|FALSE|TRUE|NULL|PRIMITIVE_RETURNS | PITest DEFAULTS (complete verbatim list) | Draft v1 §Back matter + header: "Mutators DEFAULTS (CONDITIONALS_BOUNDARY/INCREMENTS/INVERT_NEGS/MATH/NEGATE_CONDITIONALS/VOID_METHOD_CALLS/EMPTY\|FALSE\|TRUE\|NULL\|PRIMITIVE_RETURNS)"; pitest.org — SOURCE-PIN §3 PITest 1.25.3 |
| Caption — Fowler verbatim: "high coverage numbers are too easy to reach with low quality testing" | Fowler folklore (verbatim) | Draft v1 §Coverage folklore: Fowler "TestCoverage" bliki verbatim; SOURCE-PIN §7 named-book canon (bliki = Bucket-i, secondary, acknowledged in draft header) |
| Caption — Fowler verbatim: "useful tool for finding untested parts of a codebase" | Fowler folklore (verbatim) | Draft v1 §Coverage folklore: same Fowler bliki source |
| Caption — AssertJ 3.27.7 | AssertJ pinned version | SOURCE-PIN §3 row: AssertJ 3.27.7 pinned 2026-06-20 |
| Caption — "Companion module spec: 08-companion-code/48_coverage_mutation_effectiveness/ (EXAMPLE-BUILD pending)" | Companion module status | Draft v1 §Companion module spec: "⚠ EXAMPLE-BUILD = PENDING" |

---

Source-trace status: YES

**Notes on coverage:**
- The Fowler "TestCoverage" bliki is cited as a secondary (Bucket-i per draft header). Draft v1 explicitly marks it "Cohn/Fowler bliki Bucket-i" in the SOURCE-PIN gaps note. Used for the folklore section only; no hard technical claim rests solely on it.
- RETURN_VALS mutator is deprecated per pitest.org and is correctly absent from the DEFAULTS list in this figure.
- "AHEAD-OF-PIN" Java 25 preview-construct mutation is not depicted (out of scope).
- The BRANCH < 100% result for the weak test and BRANCH = 100% for the strong test trace to the companion module spec in the draft (which exercises `qty=9` vs `qty=10`, both sides of the `qty >= 10` boundary).

Verified against the pinned authority set (00-strategy/SOURCE-PIN.md) at the pins in SOURCE-PIN.md.
All tool names, GAV coordinates, mutator names, status names, metric formulae, version numbers
(JaCoCo 0.8.15; JaCoCo JDK mapping 0.8.11/0.8.14; PITest 1.25.3; AssertJ 3.27.7), and verbatim
quotes trace to draft v1 §Back matter + header comment and to SOURCE-PIN §3 rows (JaCoCo 0.8.15,
PITest 1.25.3, AssertJ 3.27.7). No atom was introduced by the figure that is not present in the draft.
