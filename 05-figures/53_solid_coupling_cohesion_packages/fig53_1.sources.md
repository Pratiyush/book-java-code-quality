# Fig 53.1 sources — SOLID: intent and over-application trap

Chapter 25 (dossier key 53, folds 54+57) · draft v1 · `03-drafts/53_solid_coupling_cohesion_packages/53_solid_coupling_cohesion_packages_v1.md`

| Label / element in diagram | Claim / atom | Pinned source path |
|---|---|---|
| Title "Fig 53.1 — SOLID: intent and over-application trap" | Identifying label | Draft v1 §"SOLID: useful vocabulary, not a law to maximize" (lines ~51–63); figure placement after the five-principle list |
| Subtitle "Five heuristics for low coupling + high cohesion — useful vocabulary, not a law to maximize. No principle is crowned." | Framing / neutrality | Draft v1 line ~53: "they are a useful *vocabulary and set of heuristics* — not a law to push to its limit"; line ~38: "none is a target to maximize"; §"Two schools, crown neither" CONCEPT (line ~61) |
| School pill "SOLID (Martin)" — "SRP · OCP · LSP · ISP · DIP — principles that aim at low coupling, high cohesion, safe change. Widely taught shared vocabulary; maps to measurable structure." | Attribution + intent | Draft v1 line ~53: "five object-oriented design principles popularized by Robert C. Martin, aimed at the same target as maintainability: low coupling, high cohesion, and safe change"; back-matter line ~137 (⚠ §7 canon row, Martin not a pinned row — verbatim defs @pin) |
| School pill "CUPID (North) & 'simple over SOLID'" — "Composable · Unix-philosophy · Predictable · Idiomatic · Domain-based. Argues dogmatic SOLID drives premature abstraction that hurts readability. Legitimate alternative lens — neither crowned." | Named alternative (neutral) | Draft v1 §"Two schools, crown neither" CONCEPT (line ~61): "Dan North's **CUPID** (Composable, Unix-philosophy, Predictable, Idiomatic, Domain-based)…a broader 'simple over SOLID' current"; back-matter line ~137 ("dannorth.net", ⚠ verbatim @pin) |
| "vs" divider between the two schools | Neutrality marker | Draft v1 line ~36: "No principle, lens, or structure is crowned"; CONCEPT title "Two schools, crown neither" (line ~61) |
| Table header "Principle / Rule of thumb / Smell it fights (Java) / Over-application trap" | Column labels | Draft v1 §"SOLID" list structure (lines ~55–59): each principle stated with intent + over-application trap; dossier header line 7 |
| SRP — "Single Responsibility" / "A class should have one reason to change." | Principle + rule | Draft v1 line ~55: "**SRP — Single Responsibility.** A class should have one reason to change." |
| SRP smell "God service — 2 000-line class that does everything" | Smell fought | Draft v1 line ~55: "It fights the God service, the 2,000-line class that does everything (Chapter 12)"; dossier line 7: "fights God service" |
| SRP trap "Cloud of one-method classes — a single behaviour scattered across a dozen files; hurts readability." | Over-application trap | Draft v1 line ~55: "it explodes into a cloud of one-method classes that scatter a single behaviour across a dozen files, the over-decomposition that hurts readability (Chapter 2)"; dossier line 7: "trap = cloud of one-method classes" |
| OCP — "Open/Closed" / "Open for extension; closed for modification — new behaviour is a new class, not an edit." | Principle + rule | Draft v1 line ~56: "**OCP — Open/Closed.** Open for extension, closed for modification — in Java, via interfaces, polymorphism, or the strategy pattern, so new behaviour is a new class rather than an edit to a tested one." |
| OCP smell "Shotgun surgery — One change forcing edits to many existing classes" | Smell fought | Draft v1 line ~70: "shotgun surgery (one change forcing edits across many classes)"; OCP is the principle that fights it via extension-not-edit (line ~56) |
| OCP trap "Speculative abstraction — an extension point added for a variation that never comes (YAGNI tension)." | Over-application trap | Draft v1 line ~56: "speculative abstraction for a change that never comes (the YAGNI tension); every premature extension point is indirection"; dossier line 7: "trap = speculative abstraction/YAGNI" |
| LSP — "Liskov Substitution" / "A subtype must be substitutable for its supertype without breaking callers' expectations." | Principle + rule | Draft v1 line ~57: "**LSP — Liskov Substitution.** A subtype must be substitutable for its supertype without breaking callers' expectations." |
| LSP note "Nearest to a hard correctness rule — ties to equals/hashCode contract and sealed types." | Character claim | Draft v1 line ~57: "the one principle closest to a hard *correctness* rule…tied directly to the `equals`/`hashCode` contract (Chapter 7) and well served by sealed types that make the permitted subtypes explicit (Chapter 5)"; dossier line 7 |
| LSP smell "Broken contract — Subclass that strengthens preconditions or weakens the contract" | Smell fought | Draft v1 line ~57: "a subclass that strengthens preconditions or weakens the contract is a latent bug" |
| LSP trap "Less of an over-application risk; violation is a latent bug. Sealed types make permitted subtypes explicit." | Over-application note | Draft v1 line ~57: "a subclass that strengthens preconditions or weakens the contract is a latent bug…well served by sealed types that make the permitted subtypes explicit"; Java 21/25 sealed types per `00-strategy/SOURCE-PIN.md` Runtime baseline + §1 (JLS SE 21/25) |
| ISP — "Interface Segregation" / "Many focused role interfaces over one fat interface — a client depends only on methods it uses." | Principle + rule | Draft v1 line ~58: "**ISP — Interface Segregation.** Many focused role interfaces over one fat interface, so a client depends only on the methods it uses." |
| ISP smell "Fat interface — Callers forced to implement methods they never use" | Smell fought | Draft v1 line ~58 (fat interface implied: "Many focused role interfaces over one fat interface, so a client depends only on the methods it uses"); dossier line 7: "ISP (focused role interfaces over fat…)" |
| ISP trap "Interface proliferation — too many single-method interfaces adds indirection with no real variation." | Over-application trap | Draft v1 §Limitations line ~104 + §"Two schools" line ~61 (over-abstraction/indirection theme); dossier line 7 (ISP) + hook line ~23 ("an interface for every class") |
| DIP — "Dependency Inversion" / "Depend on abstractions, not concretions — program to interfaces; inject implementations." | Principle + rule | Draft v1 line ~59: "**DIP — Dependency Inversion.** Depend on abstractions, not concretions. In Java: program to interfaces and inject implementations." |
| DIP smell "Hard dependency — Class instantiates its own collaborators; untestable" | Smell fought | Draft v1 line ~59: "a collaborator declared by interface is one that can be substituted" → enables testability; dossier line 7: "DIP (depend on abstractions; DI; enables testability + module boundaries)" |
| DIP trap "Five layers of DI wiring to add one field — interface-with-one-implementation satisfies DIP on paper, adds indirection that makes change harder." | Over-application trap | Draft v1 line ~99: "every interface-with-one-implementation satisfies DIP on paper while adding indirection that makes change harder, not easier"; hook line ~23: "dependency injection wiring five layers deep, and adding one field means touching seven files" |
| Bottom note "Heuristics, not metrics. SOLID-ness cannot be gated — 'one reason to change' is subjective; the measurable, enforceable subset is coupling direction and cycles (next chapter)." | Through-line claim | Draft v1 line ~63: "SOLID is principles, not metrics. Gating 'SOLID-ness' as a score is the vanity trap. SRP's 'one reason to change' is genuinely subjective…The parts of this domain that *can* be measured and enforced are coupling, cohesion, and cycles"; §Limitations line ~104 |
| Bottom note "Records, sealed types, and pattern matching (Ch 15) sometimes reach a SOLID goal more directly than a classic refactor." | Java-version subtlety | Draft v1 §"Two schools" CONCEPT line ~61: "records, sealed types, and pattern matching (Chapter 5) sometimes achieve a SOLID goal more directly than a classic refactor"; `00-strategy/SOURCE-PIN.md` §1 (JLS SE 21/25 + JEP index for records/sealed/pattern matching) — ⚠ chapter cross-ref reads "Ch 15" vs draft "Chapter 5" |
| Caption — Robert C. Martin "Agile Software Development / Clean Architecture (SOLID definitions; dossier §2 verbatim ⚠)" | Source attribution | Draft v1 back-matter line ~137: "Robert C. Martin (*Agile Software Development*, *Clean Architecture*; ⚠ §7 canon row, verbatim definitions @pin)"; `00-strategy/SOURCE-PIN.md` §7 (Martin not a pinned row — ⚠ @pin) |
| Caption — Dan North "CUPID for joyful coding (dannorth.net; dossier §4 named alt)" | Source attribution | Draft v1 back-matter line ~137: "Dan North **CUPID**…`dannorth.net`, ⚠ verbatim @pin"; `00-strategy/SOURCE-PIN.md` §7 (North CUPID not a pinned row — ⚠ @pin) |
| Caption — "LSP origin: Liskov & Wing — 'A behavioral notion of subtyping' (⚠ formal statement @pin)" | Source attribution | Draft v1 back-matter line ~137: "LSP origin Liskov & Wing (⚠ formal statement @pin)"; `00-strategy/SOURCE-PIN.md` §7 (Liskov/Wing not a pinned row — ⚠ @pin) |

---

Source-trace status: YES (with ⚠ canon-gap caveats below)

Verified against the pinned authority set (00-strategy/SOURCE-PIN.md) at the pins in SOURCE-PIN.md.

Every principle name, rule of thumb, smell, and over-application trap in Fig 53.1 traces to an explicit passage in `03-drafts/53_solid_coupling_cohesion_packages/53_solid_coupling_cohesion_packages_v1.md` (§"SOLID: useful vocabulary, not a law to maximize", the "Two schools, crown neither" CONCEPT callout, §Limitations, and the back-matter sources block). No principle, smell, or trap was invented by the diagram. Neutrality respected: SOLID and CUPID/simple-design each get their own pill with their own case; no principle and no lens is crowned. Source caveats: the SOLID definitions (Martin), CUPID (Dan North), and the LSP formal statement (Liskov & Wing) are attributed to named authorities that are NOT pinned rows in `00-strategy/SOURCE-PIN.md` §7 — the draft and figure both mark these `⚠ @pin` (verbatim definitions to be confirmed when those canon rows are pinned). The figure's caption already carries those ⚠ flags, matching the draft.
