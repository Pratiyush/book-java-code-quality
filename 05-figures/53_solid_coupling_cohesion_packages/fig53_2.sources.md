# Fig 53.2 sources — The module-strength ladder

Chapter 25 (dossier key 53, folds 54+57) · draft v1 · `03-drafts/53_solid_coupling_cohesion_packages/53_solid_coupling_cohesion_packages_v1.md`

| Label / element in diagram | Claim / atom | Pinned source path |
|---|---|---|
| Title "Fig 53.2 — The module-strength ladder" | Identifying label | Draft v1 §"Package structure: where the lines fall" — "The module-strength ladder" CONCEPT callout (line ~87) |
| Subtitle "Encapsulation is not binary — climb only as far as the boundary warrants. Each rung trades ceremony for stronger guarantees." | Framing | Draft v1 line ~87: "Encapsulation is not binary; it is a ladder of increasing enforcement…Each rung trades ceremony for stronger guarantees; the right choice is the lowest rung that actually holds the boundary in question" |
| Axis label "Enforcement strength" (↑) | Axis claim | Draft v1 line ~87: "a ladder of increasing enforcement, and teams climb it as a boundary matters more" |
| Column headers "Rung / mechanism · Guarantee · Cost / ceremony" | Column labels | Draft v1 line ~87 (each rung named with its mechanism + guarantee + ceremony); dossier line 9: "Module-strength ladder: package convention → package-private → ArchUnit slices → JPMS module-info (compiler-enforced) → separate build modules" |
| Rung 1 "Package convention — names only — discipline" / "Agreed naming signals intent; nothing enforces it — any class can reach any other." / "Zero tooling cost; zero enforcement — breaks silently under deadline pressure." | Rung + guarantee + cost | Draft v1 line ~87: "a *package convention* (names only — discipline)"; erosion-under-pressure theme line ~85, ~89; dossier line 9 (convention rung) |
| Rung 2 "package-private access — Java default visibility" / "Compiler hides internals within a package; classes outside the package cannot reach them." / "No build changes; discipline to omit `public` keyword. Java's most under-used encapsulation tool." | Rung + guarantee + cost | Draft v1 line ~87: "*package-private* access (the compiler hides internals within a package)"; line ~85: "Java's package-private default is an under-used encapsulation tool, and using it keeps internals internal"; dossier line 9 |
| Rung 3 "ArchUnit-enforced slices — slices().should().beFreeOfCycles()" / "A test fails on a forbidden dependency or cycle — structure is continuously checked in CI." / "ArchUnit test dependency; rules authored once; catches violations before merge. Enforcement chapter's primary tool." | Rung + mechanism + guarantee + cost | Draft v1 line ~87: "*ArchUnit-enforced slices* (a test fails on a forbidden dependency)"; line ~74: "ArchUnit's `slices().should().beFreeOfCycles()`"; ArchUnit pinned **1.4.2** in `00-strategy/SOURCE-PIN.md` §2; enforcement is "next chapter" (line ~74, Ch 26) |
| `slices().should().beFreeOfCycles()` (rung 3 mechanism) | ArchUnit DSL identifier | Draft v1 line ~74: "ArchUnit's `slices().should().beFreeOfCycles()`"; back-matter line ~138; `00-strategy/SOURCE-PIN.md` §2 ArchUnit 1.4.2 (archunit.org) |
| Rung 4 "JPMS module-info — JEP 261 · compiler-enforced" / "Compiler forbids access across module boundaries; only explicitly exports-ed packages are visible." / "module-info.java per module; dependency graph declared; reflection restrictions. Ceremony meaningful for libraries or large systems." | Rung + mechanism + guarantee + cost | Draft v1 line ~87: "*JPMS `module-info`* (the compiler itself forbids access across module boundaries)"; back-matter line ~139: "JPMS `module-info` (JEP 261, compiler-enforced; ⚠ exports semantics @pin)" |
| "JEP 261" (rung 4 mechanism label) | JEP identifier | Draft v1 back-matter line ~139: "JPMS `module-info` (JEP 261, compiler-enforced)"; `00-strategy/SOURCE-PIN.md` §1 "JEP index — per-feature (confirm each JEP # at use)" — ⚠ exports semantics @pin per draft |
| `module-info.java` / `exports` (rung 4) | Config / keyword identifiers | Draft v1 line ~87 + back-matter line ~139 (JPMS module-info, exports semantics ⚠ @pin); `00-strategy/SOURCE-PIN.md` §1 JEP index |
| Rung 5 "Separate build modules — Maven/Gradle multi-module" / "Depending on an undeclared module is physically impossible — the build fails if the dependency is not wired." / "Separate build unit per module; build aggregator; physical separation. Highest ceremony — right only where the boundary truly warrants it." | Rung + mechanism + guarantee + cost | Draft v1 line ~87: "*separate build modules* (depending on an undeclared dependency is physically impossible)"; dossier line 9 (build modules rung); Maven 3.9.16 / Gradle 9.6.0 multi-module per `00-strategy/SOURCE-PIN.md` §4 |
| "Maven/Gradle multi-module" (rung 5 mechanism) | Build-tool labels | `00-strategy/SOURCE-PIN.md` §4: "Apache Maven 3.9.16", "Gradle 9.6.0"; Runtime baseline row "Build toolchains: Maven + Gradle (both)"; draft routing line ~140 ("build modules → later (62)") |
| Strategy box "By-layer (controller · service · repository)" — "Familiar; maps to layered architecture; easy entry point for new teams." / "Risk: a feature's code scatters across packages; layer packages become low-cohesion buckets." | Strategy + strength + risk | Draft v1 §"Package structure" table (lines ~80–82): "**By-layer** (`controller`, `service`, `repository`)…familiar; maps to layered architecture | a feature's code scatters across packages; layer packages become low-cohesion buckets"; dossier line 9 |
| Strategy box "By-feature (orders · billing)" — "High cohesion per feature; change stays local; natural fit for modular monoliths." / "Risk: cross-feature sharing needs deliberate API packages; more up-front planning." | Strategy + strength + risk | Draft v1 §"Package structure" table (lines ~80–83): "**By-feature** (`orders`, `billing`)…high cohesion per feature; change stays local | cross-feature sharing needs deliberate API packages"; line ~85: "by-feature is increasingly favoured for modular monoliths" |
| "vs" divider between strategies | Neutrality marker | Draft v1 line ~85: "Neither is crowned"; figure rule-of-thumb note "Neither by-layer nor by-feature is crowned — context decides" |
| Rule of thumb "Use package-private by default (rung 2); add ArchUnit slices when cycle discipline matters (rung 3); reach for JPMS or build modules only when the boundary is load-bearing and team-scale warrants the ceremony. Neither by-layer nor by-feature is crowned — context decides." | Synthesis claim (neutral) | Draft v1 line ~87: "the right choice is the lowest rung that actually holds the boundary in question"; line ~126–127 §"When to use what": "package-private by default…climb the module-strength ladder only as far as the boundary warrants"; line ~85: "Neither is crowned…the right answer depends on team size, domain, and whether the project is an app or a library" |
| Caption — "module-strength ladder CONCEPT callout (convention → package-private → ArchUnit slices → JPMS module-info → build modules)" | Source attribution | Draft v1 §"Package structure" CONCEPT callout (line ~87); dossier line 9 |
| Caption — "ArchUnit slices().should().beFreeOfCycles() — draft v1 §'Coupling and cohesion' (cycles paragraph)" | Source attribution | Draft v1 line ~74 (cycles paragraph); `00-strategy/SOURCE-PIN.md` §2 ArchUnit 1.4.2 |
| Caption — "JEP 261 (JPMS; ⚠ exports semantics @pin)" | Source attribution | Draft v1 back-matter line ~139; `00-strategy/SOURCE-PIN.md` §1 JEP index (confirm JEP # at use — ⚠ @pin) |
| Caption — "by-layer vs by-feature trade-off table — draft v1 §'Package structure' table" | Source attribution | Draft v1 §"Package structure" table (lines ~80–83) |

---

Source-trace status: YES (with ⚠ JPMS-semantics caveat below)

Verified against the pinned authority set (00-strategy/SOURCE-PIN.md) at the pins in SOURCE-PIN.md.

Every rung name, mechanism, guarantee, cost, and package strategy in Fig 53.2 traces to an explicit passage in `03-drafts/53_solid_coupling_cohesion_packages/53_solid_coupling_cohesion_packages_v1.md` (the "module-strength ladder" CONCEPT callout in §"Package structure: where the lines fall", the §"Coupling and cohesion" cycles paragraph, and the by-layer/by-feature trade-off table) and to `00-strategy/SOURCE-PIN.md` (§2 ArchUnit 1.4.2, §4 Maven 3.9.16 / Gradle 9.6.0, §1 JEP index). No rung or strategy was invented by the diagram. The five rungs render bottom-to-top (weakest → strongest) by a grayscale lightness ramp, not by hue. Neutrality respected: by-layer and by-feature each get their own strength and their own risk; neither is crowned. Source caveat: the JPMS `exports` semantics under JEP 261 are marked `⚠ @pin` in both the draft and the figure caption — the JEP index is pinned per-feature in `00-strategy/SOURCE-PIN.md` §1, with the exact `exports`/`requires` behaviour to be confirmed at use.
