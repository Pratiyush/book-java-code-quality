# fig91_1.sources.md — Source-trace sidecar for Fig 91.1

**Figure:** Fig 91.1 — The safe-change loop: one invariant at four scales  
**Chapter:** 39 — Changing Code Without Breaking It (dossier key 91, folds 92/93/95)  
**HTML source:** `05-figures/91_refactoring_legacy_modernization/fig91_1.html`  
**Rendered artifact:** `05-figures/91_refactoring_legacy_modernization/fig91_1.png`

---

## Per-label source-trace table

| Label / atom in diagram | Claim asserted | Pinned source (SOURCE-PIN.md) |
|---|---|---|
| Figure subtitle: "Preserve behavior · verify with tests · move in small reversible steps · keep the system working throughout" | The one invariant governing all four scales | Draft v1 §"Deep dive: one invariant, four scales" — dossier key 91 (leads), statement: "One invariant governs all four scales" |
| Step 0 heading: "Precondition — ensure a test net exists" | Tests must cover the area before refactoring; build the net first on untested legacy | Fowler *Refactoring* 2e (2018), SOURCE-PIN §7; Draft §"Refactoring: small behavior-preserving steps" step (1): "ensure tests cover the area (or characterize first)" |
| Step 0 body: "characterization tests + seams" | Characterization tests and seams are the mechanism for getting legacy code under test | Feathers *WELC* (2004), SOURCE-PIN §7; Draft §"Legacy code: characterize, then create a seam" |
| Step 0 body: "pins current behavior — including bugs — as a net, not a specification; review what is captured" | Characterization tests lock what-IS (including bugs), not what-should-be; must be reviewed | Draft §"Legacy code": "Characterization tests pin current behavior including its bugs…review the captured behavior, not blindly accepted"; Feathers *WELC* 2004, SOURCE-PIN §7 |
| Step 0 tag: `Feathers WELC 2004 — "legacy code is code without tests"` | Feathers' working definition | Feathers *Working Effectively with Legacy Code* (2004), SOURCE-PIN §7; Draft §"Legacy code: characterize, then create a seam": "Michael Feathers' working definition is deliberately stark: legacy code is code without tests" |
| Step 1 heading: "Make ONE small behavior-preserving transformation" | A refactoring is a single, behavior-preserving transformation | Fowler *Refactoring* 2e (2018), SOURCE-PIN §7; Draft §"Refactoring": "make one small refactoring" |
| Step 1 body: "Extract Method · Rename · Move · Replace Conditional with Polymorphism · Introduce Parameter Object" | Named catalog refactorings from Fowler | Fowler *Refactoring* 2e (2018), SOURCE-PIN §7; Draft §"Refactoring: small behavior-preserving steps": "(Extract Method, Rename, Move, Replace Conditional with Polymorphism, Introduce Parameter Object)" |
| Step 1 body: "IDE-automated refactorings — behavior-preserving by construction" | IDE refactorings are behavior-preserving by construction | Draft §"Refactoring": "IDE automated refactorings (IntelliJ/Eclipse) are behavior-preserving by construction"; Fowler *Refactoring* 2e (2018), SOURCE-PIN §7 |
| Step 1 tag: `Fowler Refactoring 2e (2018) — catalog entries` | Attribution of the refactoring catalog | Fowler *Refactoring* 2e (2018), SOURCE-PIN §7 |
| NEVER box: "Never: refactor + behavior change in one step" | Cardinal rule of refactoring discipline | Fowler *Refactoring* 2e (2018), SOURCE-PIN §7; Draft §"Refactoring": "The cardinal rule: never refactor and change behavior in the same step" |
| NEVER box body: "Fowler 'two hats'" | Fowler's named concept | Fowler *Refactoring* 2e (2018), SOURCE-PIN §7; Draft §"Refactoring": "Fowler's 'two hats' names it — at any moment the developer is either refactoring…or adding a feature…never both" |
| NEVER box body: "refactor (structure fixed, behavior preserved) OR add feature (behavior changes, structure fixed)" | Two-hats definition | Draft §"Refactoring" CONCEPT callout; Fowler *Refactoring* 2e (2018), SOURCE-PIN §7 |
| NEVER box body: "a failing test cannot localize which change broke it" | Consequence of mixing hats | Draft §"Refactoring": "mixing them means a test failure cannot identify which change broke it"; Fowler *Refactoring* 2e (2018), SOURCE-PIN §7 |
| Step 2 heading: "Run the test suite — must stay green" | After each refactoring, the suite must pass | Fowler *Refactoring* 2e (2018), SOURCE-PIN §7; Draft §"Refactoring": "(3) run the tests — green" |
| Step 2 tag: `Fowler Refactoring 2e — "suite runs after every refactoring"` | Attribution | Fowler *Refactoring* 2e (2018), SOURCE-PIN §7 |
| Step 3 heading: "Commit — one commit per step" | One commit per refactoring step | Draft §"Refactoring" loop: "(4) commit; repeat"; Fowler *Refactoring* 2e (2018), SOURCE-PIN §7 |
| Step 3 body: "Each commit is independently reversible. The codebase is never more than one small step away from a known-good state." | Core safety of incremental change | Draft §"Deep dive": "each small step is independently verifiable and reversible, so the system is never more than one step away from working" |
| Repeat arrow: "Repeat — return to step 1" | The loop is cyclic | Fowler *Refactoring* 2e (2018), SOURCE-PIN §7; Draft §"Refactoring": "repeat" |
| Note: "Preparatory refactoring" | Refactor before the feature to make the change easy | Draft §"Refactoring": "preparatory refactoring (refactor before a change to make it easy)"; Fowler *Refactoring* 2e (2018), SOURCE-PIN §7 |
| Note: "Boy Scout Rule, Ch 1" | Clean up as code is touched; chapter cross-reference | Draft §"Refactoring": "opportunistic (Boy Scout Rule, Chapter 1)"; dossier key 06 (Ch 1) |
| Scale A heading: "Method / class — Refactoring" | Scale A of the four-scale ladder | Draft §"Deep dive: one invariant, four scales": "Refactoring applies it to a method or class"; dossier key 91 |
| Scale A: "records, sealed + pattern matching (Ch 5)" | Modern Java language features that supersede some catalog entries; chapter cross-reference | Draft §"Refactoring": "some 2018 manual refactorings are now language features — Replace Constructor with Factory is often just a record, some Visitor hierarchies become sealed types with pattern matching — Chapter 5"; JDK 21.0.11 + 25.0.3, SOURCE-PIN §1 |
| Scale A limit: "refactoring without tests is just editing" | Primary risk of untested refactoring | Draft §"Limitations": "Refactoring without tests is just editing"; Fowler *Refactoring* 2e (2018), SOURCE-PIN §7 |
| Scale B heading: "Untested legacy — Characterize & seam" | Scale B (getting code under test) | Draft §"Legacy code: characterize, then create a seam"; Feathers *WELC* 2004, SOURCE-PIN §7 |
| Scale B: "object / interface / link" | Three seam types from Feathers | Feathers *WELC* (2004), SOURCE-PIN §7; Draft §"Legacy code": "Object seams (inject a collaborator, or subclass-and-override), interface seams, and link seams" |
| Scale B: "Extract Interface · Parameterize Constructor · Subclass-and-Override" | Named seam-creating refactorings that can be applied without tests via IDE | Feathers *WELC* (2004), SOURCE-PIN §7; Draft §"Legacy code" CONCEPT: "the seam-creating refactorings (Extract Method, Extract Interface, Parameterize Constructor, Subclass-and-Override) are exactly the IDE's behavior-preserving ones" |
| Scale B loop: "find change point → seam → characterize → change → refactor green" | The legacy-code loop from Feathers | Feathers *WELC* (2004), SOURCE-PIN §7; Draft §"Legacy code" CONCEPT: "The loop: find the change point → break dependencies to reach a seam → characterize the current behavior → make the change → refactor under the now-green tests" |
| Scale B limit: "characterization pins current behavior including bugs — review the captured output; it is a net, not a spec" | Characterization pins bugs; must review | Draft §"Legacy code": "Characterization tests pin current behavior including its bugs…must be reviewed, not blindly accepted"; Feathers *WELC* 2004, SOURCE-PIN §7 |
| Scale C heading: "Whole system — Strangler fig" | Scale C; strangler-fig pattern | Fowler StranglerFigApplication (2004), SOURCE-PIN §7; Draft §"Strangler fig: grow the new around the old" |
| Scale C: "Façade / routing layer in front of old system" | Strangler mechanics — routing layer | Draft §"Strangler fig": "put a façade or interception layer (a gateway, proxy, or adapter) in front of the old system"; Fowler (2004), SOURCE-PIN §7 |
| Scale C: "Feature flags toggle each slice" | Feature flags for per-slice routing/rollback | Draft §"Strangler fig": "Feature flags (Chapter 35) toggle and roll back each migrated slice independently"; dossier key 81 (Ch 35) |
| Scale C: "Contract tests + characterization at the seam. (Fowler 2004)" | Contract and characterization tests at the strangler seam | Draft §"Strangler fig": "characterization tests (previous section) and contract tests (Chapter 24) at the seam"; Fowler (2004), SOURCE-PIN §7 |
| Scale C limit: "half-strangled stall is the failure mode — dual-running cost + shared data/state is the hard part; needs org stamina to finish" | Primary strangler failure mode | Draft §"Strangler fig" CONCEPT: "The half-strangled stall is the failure mode…the team maintains the old system, the new system, and the routing layer simultaneously, and the migration can stall half-done"; dossier key 93 |
| Scale D heading: "Platform — Java version migration" | Scale D; platform-level change | Draft §"Java version migration: the platform scale"; dossier key 95 |
| Scale D: "LTS-to-LTS path: 8 → 11 → 17 → 21 → 25" | Named LTS stepping path | Draft §"Java version migration": "The path is LTS-to-LTS — 8 → 11 → 17 → 21 → 25 (this book anchors 21, notes 25)"; JDK 21.0.11 + 25.0.3, SOURCE-PIN §1 |
| Scale D: "dependencies first (usual blocker: old lib/agent)" | Deps-first process step; named usual blocker | Draft §"Java version migration" CONCEPT: "bump dependencies first — the usual blocker is an old library or agent that does not run on the new JDK (Chapter 27)"; dossier key 64 (Ch 27) |
| Scale D: "UpgradeToJava21 recipe (OpenRewrite 8.81.0)" | Named OpenRewrite recipe and pinned version | Draft §"Java version migration" CONCEPT: "OpenRewrite's rewrite-migrate-java recipes (UpgradeToJava17/21/25, composite so 25 includes 21)"; OpenRewrite 8.81.0, SOURCE-PIN §6 |
| Scale D: "JPMS, removed APIs" | Named categories of per-hop breaking changes | Draft §"Java version migration": "breaking changes (removed APIs, JPMS strong encapsulation since 17, removed internals, deprecations-for-removal)"; JDK 21.0.11, SOURCE-PIN §1 |
| Scale D: "records / sealed / pattern matching" | Named modern Java features to adopt after migration, as separate step | Draft §"Java version migration": "adopt new features opportunistically — as separate steps from the bump"; Ch 5, dossier key 13; JDK 21.0.11, SOURCE-PIN §1 |
| Scale D limit: "migration ≠ modernization — bumping the version does not refactor old idioms; recipes do not cover everything; preview at 25 is not a migration target" | Three named limits of migration | Draft §"Limitations": "Migration is not modernization. Bumping the version does not refactor old idioms…"; Draft §"Java version migration": "Migration is not modernization"; draft: "Preview features at 25 are not migration targets — AHEAD-OF-PIN"; dossier key 95 |
| Caption: "OpenRewrite pin: 8.81.0" | Pinned OpenRewrite version | SOURCE-PIN §6: "OpenRewrite 8.81.0 (rewrite-maven-plugin 6.38.0; rewrite-gradle-plugin 7.32.0)" |
| Caption: "JDK 21.0.11 / 25.0.3" | Pinned JDK versions | SOURCE-PIN §1: "JDK 21.0.11 (LTS) + 25.0.3 (LTS) GA" |

---

## Source-trace status

**Source-trace status: YES**

Every label, arrow, atom, and number in Fig 91.1 traces to either:
- The pinned chapter draft `03-drafts/91_refactoring_legacy_modernization/91_refactoring_legacy_modernization_v1.md` (which itself cites the authority set), or
- A named pinned authority in SOURCE-PIN.md:
  - Fowler *Refactoring* 2e (2018) — SOURCE-PIN §7
  - Feathers *Working Effectively with Legacy Code* (2004) — SOURCE-PIN §7
  - Fowler StranglerFigApplication (2004) — SOURCE-PIN §7
  - OpenRewrite 8.81.0 — SOURCE-PIN §6
  - JDK 21.0.11 / 25.0.3 — SOURCE-PIN §1

No label, claim, or rule ID was invented. All technical atoms (refactoring names, seam types, LTS versions, recipe name, OpenRewrite version, Java feature names) match the draft text which was itself verified against the pin.

**Verified against the pinned authority set (00-strategy/SOURCE-PIN.md) at the pins in SOURCE-PIN.md — 2026-06-20.**
