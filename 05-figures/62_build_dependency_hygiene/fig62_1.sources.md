# Fig 62.1 sources — Build lifecycle as quality-gate host

Chapter 27 (dossier key 62, folds 63+64) · draft v1 · `03-drafts/62_build_dependency_hygiene/62_build_dependency_hygiene_v1.md`

| Label / element in diagram | Claim / atom | Pinned source path |
|---|---|---|
| Title "Fig 62.1 — The build lifecycle as quality-gate host" | Identifying label | Draft §"The build as the gate host" (v1 lines ~44–48): "The build is where every quality gate actually runs"; dossier §6 figure spec: "Fig 62.1 — build lifecycle with quality gates bound to phases (fast→slow), one column Maven, one Gradle (parallel, no winner)" |
| Subtitle "Maven phases and Gradle check task · bind checks cheapest-first · no winner" | Framing | Draft v1 §"How it works" para 1 (lines ~44–45): "The lifecycle is the gate host…cheap, fast, common-failure checks first (format, lint), heavier ones later (SpotBugs, Sonar), coverage and mutation last" |
| "Maven 3.9.16" | Version pin | `00-strategy/SOURCE-PIN.md` §4 row "Apache Maven (+ enforcer, versions plugins) — **3.9.16**" (pinned 2026-06-20) |
| "Declarative POM · convention-over-configuration · stable lifecycle" | Character claim | Draft v1 lines ~48: "Maven is declarative (a POM, convention over configuration, a stable lifecycle)" |
| `./mvnw verify` | Command / wrapper claim | Draft v1 lines ~46–47: "local↔CI parity (key 82), single source of dep truth (BOM/catalog), wrapper committed (mvnw/gradlew pinning the build-tool version itself)"; dossier §2: "wrapper committed (mvnw/gradlew pins the build-tool version itself)" |
| Maven phase "validate" | Phase name | Apache Maven docs at maven.apache.org (pinned 2026-06-20, SOURCE-PIN §4); draft dossier header line 7: "Lifecycle as gate host: Maven validate→compile→test→verify" |
| Maven phase "compile" | Phase name | Same Maven docs pin; draft v1 line ~44: "Maven, checks bind to phases (`validate` → `compile` → `test` → `verify`)" |
| Maven phase "test-compile" | Phase name | Apache Maven lifecycle docs (maven.apache.org); implied by test phase ordering |
| Maven phase "test" | Phase name | Draft v1 line ~44 and dossier §2 |
| Maven phase "verify" | Phase name | Draft v1 line ~44 ("`validate` → `compile` → `test` → `verify`"); SOURCE-PIN §4 Maven docs |
| `maven-enforcer-plugin` (validate row) | Plugin identifier | Draft v1 lines ~55–56: "The `maven-enforcer-plugin` rules `dependencyConvergence` and `requireUpperBoundDeps` make a conflict fail the build"; dossier §2: "maven-enforcer-plugin: dependencyConvergence/banned deps/require-JDK"; SOURCE-PIN §4 Maven pin |
| `spotless:check` (compile row) | Goal identifier | Dossier §2 / draft v1 §"The build as the gate host": formatters listed; `spotless:check` is the standard Spotless Maven goal (spotless-maven-plugin, github.com/diffplug/spotless; SOURCE-PIN §2 "Spotless 8.7.0") |
| `checkstyle:check` (compile row) | Goal identifier | `maven-checkstyle-plugin` standard goal; Checkstyle pinned at **13.6.0** (SOURCE-PIN §2); draft v1 §gate host ordering: "format, lint" cheap first |
| PMD `pmd:check` (test-compile row) | Goal identifier | `maven-pmd-plugin` standard goal; PMD pinned at **7.25.0** (SOURCE-PIN §2); draft ordering: PMD in "heavier" mid tier |
| NullAway / Error Prone compiler plugins (test-compile row) | Tool labels | Draft v1 §"How it works": NullAway, Error Prone mentioned in build context; SOURCE-PIN §2 rows: "Error Prone latest 2.x", "NullAway 0.13.4" |
| `maven-surefire-plugin` (test row) | Plugin identifier | Dossier §2: "maven-surefire/failsafe"; draft v1 back-matter (line ~119): "maven-surefire/failsafe". SOURCE-PIN §4 Maven docs |
| JaCoCo agent (test row) | Tool reference | Draft v1 §gate host: "coverage…last"; SOURCE-PIN §3 "JaCoCo 0.8.16" |
| `spotbugs:check` (verify row) | Goal identifier | `spotbugs-maven-plugin` standard goal; SpotBugs pinned at **4.10.2** (SOURCE-PIN §2) |
| JaCoCo report + threshold (verify row) | Claim | Draft v1 ordering: "coverage and mutation last" in the verify phase; SOURCE-PIN §3 JaCoCo pin |
| `failsafe` integration tests (verify row) | Plugin label | Dossier §2: "maven-surefire/failsafe"; SOURCE-PIN §4 Maven docs |
| `dependencyConvergence` (verify row) | Enforcer rule name | Draft v1 line ~55: "The `maven-enforcer-plugin` rules `dependencyConvergence` and `requireUpperBoundDeps`"; SOURCE-PIN §4 Maven/enforcer pin |
| "Gradle 9.6.0" | Version pin | `00-strategy/SOURCE-PIN.md` §4 row "Gradle (+ version catalogs) — **9.6.0** (2026-06)" (pinned 2026-06-20) |
| "Programmable Groovy/Kotlin DSL · incremental + build cache" | Character claim | Draft v1 line ~48: "Gradle is programmable (a Groovy or Kotlin DSL), with incremental builds and a build cache" |
| `./gradlew check` | Command / wrapper | Draft v1 line ~44: "Gradle, the `check` task aggregates verification"; dossier §2: "wrapper committed (mvnw/gradlew)" |
| `check` task aggregates verification (Gradle header) | Architecture claim | Draft v1 line ~44: "in Gradle, the `check` task aggregates verification"; SOURCE-PIN §4 Gradle docs |
| `compileJava` task (Gradle, fast row) | Task name | Gradle standard task name (docs.gradle.org; SOURCE-PIN §4 Gradle 9.6.0 pin) |
| `spotlessCheck` (Gradle compile row) | Task name | Spotless Gradle plugin standard task (`gradle/spotless`); SOURCE-PIN §2 Spotless 8.7.0 |
| `checkstyleMain` (Gradle compile row) | Task name | Gradle Checkstyle plugin standard task name (docs.gradle.org/current/userguide/checkstyle_plugin); SOURCE-PIN §2 Checkstyle 13.6.0; SOURCE-PIN §4 Gradle pin |
| `compileTestJava` (Gradle) | Task name | Gradle standard task name; SOURCE-PIN §4 Gradle docs |
| `pmdMain` (Gradle compile/test row) | Task name | Gradle PMD plugin standard task name (docs.gradle.org/current/userguide/pmd_plugin); SOURCE-PIN §2 PMD 7.25.0 |
| NullAway / Error Prone annotation processors (Gradle) | Tool labels | Same as Maven row — SOURCE-PIN §2 pins |
| `Test` task (Gradle test row) | Task label | Gradle built-in `Test` task (docs.gradle.org; SOURCE-PIN §4) |
| JaCoCo `jacoco` plugin (Gradle test row) | Plugin reference | Gradle JaCoCo plugin (docs.gradle.org/current/userguide/jacoco_plugin); SOURCE-PIN §3 JaCoCo 0.8.16 |
| `spotbugsMain` (Gradle verify/check row) | Task name | SpotBugs Gradle plugin standard task; SOURCE-PIN §2 SpotBugs 4.10.2 |
| `dependencyInsight` / resolution strategy (Gradle check row) | Claim | Draft v1 line ~55: "Gradle has resolution strategies and `dependencyInsight`"; SOURCE-PIN §4 Gradle docs |
| Gradle build cache row label | Claim | Draft v1 line ~48: "Gradle is programmable…with incremental builds and a build cache that make large builds fast"; dossier §2: "incremental + build cache (speed)"; SOURCE-PIN §4 Gradle docs |
| Cost legend: "fast / cheap (format, style, lint) — bind early" | Ordering principle | Draft v1 §gate host: "cheap, fast, common-failure checks first (format, lint)" |
| Cost legend: "heaviest (SpotBugs, coverage, integration) — bind last" | Ordering principle | Draft v1: "coverage and mutation last, so the build fails fast on the cheapest offense" |
| "Fail fast on cheapest offense first" | Principle label | Draft v1 §gate host para (lines ~44–45): "so the build fails fast on the cheapest offense" |
| Props strip: "Reproducible · Exact pinned versions; no LATEST/RELEASE/ranges" | Claim | Draft v1 CONCEPT callout: "reproducible (exact pinned versions, no `LATEST`/`RELEASE`/open ranges)"; dossier §2: "reproducible (pinned versions, no `LATEST`/ranges — key 67)" |
| Props strip: `mvnw` / `gradlew` (committed wrapper) | Claim | Draft v1 CONCEPT callout: "run through a committed wrapper (`mvnw`/`gradlew`) that pins the build-tool version itself"; dossier §2: "wrapper committed (mvnw/gradlew pins the build-tool version itself)" |
| Props strip: "Fast · Incremental, cached, parallel — a 30-min verify is one developers skip" | Claim | Draft v1 CONCEPT: "fast (incremental, cached, parallel — because a 30-minute `verify` is one developers skip locally, and a skipped gate is no gate)" |
| Props strip: "Local ≡ CI · Same ./mvnw verify on every machine" | Claim | Draft v1 CONCEPT callout: "identical local and CI (the same `./mvnw verify` everywhere)"; dossier §2: "local↔CI parity (key 82)" |
| Props strip: `<dependencyManagement>` / BOM import (Maven) | Config syntax | Draft v1 lines ~54–55: "in Maven, `<dependencyManagement>` plus imported BOMs (bill-of-materials POMs)"; SOURCE-PIN §4 Maven docs |
| Props strip: `gradle/libs.versions.toml` catalog (Gradle) | Config syntax | Draft v1 line ~54: "in Gradle, version catalogs (`gradle/libs.versions.toml`) plus platforms"; SOURCE-PIN §4 Gradle 9.6.0 docs |
| Limit note: "2,000-line POM or sprawling Gradle DSL" | Claim | Draft v1 §Limitations: "A 2,000-line POM or a sprawl of custom Gradle logic is its own maintainability liability"; draft v1 §"How it works": "a 2,000-line POM or a maze of custom Gradle tasks is a maintainability liability" |
| Limit note: "Maven's rigidity can fight a non-standard need; Gradle's programmability can become unreadable build logic" | Claim (neutral, each its cost) | Draft v1 §"How it works" line ~48: "Maven's rigidity can fight a non-standard need, and Gradle's programmability can sprawl into unreadable build logic. Each is a cost — ⚠, crown neither." |
| Caption: Maven 3.9.16 / Gradle 9.6.0 version labels | Version pins | `00-strategy/SOURCE-PIN.md` §4 (pinned 2026-06-20) |
| Caption: `dependencyConvergence` → §"Dependency hygiene" | Cross-ref | Draft v1 §"Dependency hygiene" lines ~55–56 |

---

Source-trace status: YES

Verified against the pinned authority set (00-strategy/SOURCE-PIN.md) at the pins in SOURCE-PIN.md.

Every phase name, task name, plugin identifier, goal name, version number, and claim in Fig 62.1 traces to an explicit passage in `03-drafts/62_build_dependency_hygiene/62_build_dependency_hygiene_v1.md` (draft §§ "The build as the gate host", CONCEPT callout on tool-agnostic properties, §"Dependency hygiene" on `dependencyConvergence`, §Limitations), the dossier `02-research/62_build_as_quality_surface/62_build_as_quality_surface_RESEARCH.md` (§§2, 6), and `00-strategy/SOURCE-PIN.md` §4 (Maven 3.9.16, Gradle 9.6.0, pinned 2026-06-20). No atom was invented by the diagram. Neutrality constraint respected: Maven and Gradle each receive their own column with their own character claims and their own costs; no winner is crowned; the label "neither crowned" appears on the divider.
