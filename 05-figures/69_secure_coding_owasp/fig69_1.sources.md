# Source-trace sidecar — Fig 69.1
## Three Java vulnerability classes: design-out hierarchy

Chapter 30 · key 69 (folds 72, 74) · "The Vulnerabilities You Write Yourself"
Family: Concept-map / decision-hierarchy
Placement: §"How it works" — after OWASP map intro, before §Injection deep-dive

---

| Label in diagram | Claim | Pinned source (SOURCE-PIN.md) |
|---|---|---|
| "Injection — SQL · Command · LDAP · EL/SpEL/OGNL" | Named injection sub-classes that share the untrusted-data-as-code root cause | Draft §B (key 72): "SQL/command/LDAP/expression-language injection" |
| "OWASP Top 10:2025 · A03 · CWE-89/78" | Injection maps to A03 in OWASP Top 10; SQL injection = CWE-89, OS command injection = CWE-78 | Draft back-matter (key 72): "OWASP A03 + A08"; OWASP Top 10:2025 A03 description; mitre.org/cwe CWE-89/78 |
| Root cause: "Untrusted input concatenated into an interpreter… that then executes it as code" | Root cause of injection class | Draft §B (key 72): "Injection… happens when input is concatenated into an interpreter that then executes it" |
| "PreparedStatement bind params (SQL)" | Eliminate SQL injection via PreparedStatement parameterization | Draft §B: "SQL → a PreparedStatement with bind parameters, never a string-concatenated query" |
| "JPA/Criteria parameter binding" | JPA as parameterization equivalent | Draft §B: "JPA/criteria parameter binding does the same" |
| "ProcessBuilder with fixed args (command)" | Eliminate command injection via ProcessBuilder with fixed arguments | Draft §B: "avoid Runtime.exec with user input entirely; use an allow-list and ProcessBuilder with fixed arguments" |
| "Encode/parameterize LDAP, XPath, EL/SpEL/OGNL" | Parameterize or encode for LDAP/EL injection classes | Draft §B: "LDAP, XPath, expression languages (EL/SpEL/OGNL) → encode and parameterize" |
| "Parameterize — never concatenate" (Eliminate label) | Core design-out principle for injection | Draft §B: "the principle is parameterize, do not concatenate" |
| "Jakarta Bean Validation (structural input checks)" | Validation as defense-in-depth substrate for injection | Draft §B: "Input validation (Jakarta Bean Validation, Chapter 9) is a useful substrate" |
| "a 'valid' string still injects; parameterization is mandatory" | Validation is not a substitute for parameterization | Draft §B: "validation is not a substitute for parameterization. A string can be perfectly 'valid' by every structural rule and still inject" |
| "SAST taint analysis (untrusted source → dangerous sink)" | SAST taint detects injection | Draft back-matter (key 72): "SAST false negatives"; draft §"Hand-off": "taint analysis to trace untrusted input from a source to a dangerous sink" |
| "FindSecBugs SQL-injection rule" | FindSecBugs flags SQL injection | Draft §C (key 74): "FindSecBugs/Error Prone flag MD5, ECB, and Random-for-keys at build time"; draft header comment: "FindSecBugs flags the bad" |
| "Insecure Deserialization — native Java serialization of untrusted data" | Class name and scope | Draft §B (key 72): "insecure deserialization happens when untrusted bytes are reconstituted into live objects" |
| "OWASP Top 10:2025 · A08 · CWE-502" | Deserialization maps to A08; CWE-502 Unsafe Deserialization | Draft back-matter (key 72): "OWASP A03 + A08"; OWASP Top 10:2025 A08; mitre.org/cwe CWE-502 |
| Root cause: "untrusted bytes reconstituted into live objects via readObject; gadget chains… reach RCE" | Root cause of insecure deserialization | Draft §B: "untrusted bytes are reconstituted into live objects, which, through 'gadget chains' of existing classes, can reach remote code execution" |
| "Avoid native Java serialization for untrusted data entirely" | Eliminate deserialization attack surface | Draft §B: "avoid native Java serialization of untrusted data entirely" |
| "Use JSON or Protobuf without polymorphic typing enabled" | Safe format alternative | Draft §B: "Prefer a safe data format (JSON, Protobuf) with the library configured not to deserialize arbitrary types (polymorphic typing disabled)" |
| "Effective Java, Ch 5: 'prefer alternatives to Java serialization'" | Cited authority for serialization stance | Draft §B: "This is the same hierarchy Effective Java states (Chapter 5): prefer alternatives to Java serialization"; draft back-matter (key 72): "Effective Java (Ch 5) 'prefer alternatives to Java serialization'" |
| "ObjectInputFilter (JEP 290, Java 9+) — allow-list permitted classes" | Mitigation when native serialization unavoidable | Draft §B: "ObjectInputFilter (JEP 290) constrains what classes may be deserialized via an allow-list"; draft back-matter (key 72): "ObjectInputFilter (JEP 290 allow-list classes)" |
| "allow-list is a mitigation of an evolving gadget-chain threat" | Honesty note on ObjectInputFilter | Draft §B: "an allow-list is a mitigation of an evolving threat, not the elimination a safe format provides" |
| "FindSecBugs deserialization rule (readObject on untrusted input)" | Detection tool for deserialization | Draft back-matter (key 72): "SAST taint"; draft §"Hand-off": "the unsafe readObject in a project's own code" |
| "Cryptographic-API Misuse — wrong mode · weak alg · bad randomness · hardcoded keys" | Class name and scope | Draft §C (key 74): "wrong mode/hardcoded key-IV/weak alg/bad randomness" |
| "OWASP Top 10:2025 · A02 · CWE-327/330" | Crypto failure maps to A02; CWE-327 (Broken Crypto), CWE-330 (Insufficient Randomness) | Draft back-matter (key 74): "Maps A02"; OWASP Top 10:2025 A02; mitre.org/cwe CWE-327/330 |
| Root cause: "failure is API misuse, not a broken algorithm — wrong mode, weak choice, predictable IV/key, or bad PRNG" | Core framing of crypto failure class | Draft §C: "the failure is almost never a broken algorithm. The failure is API misuse" |
| "SecureRandom (not Random / Math.random)" | Fix for insecure randomness | Draft §C: "insecure randomness (java.util.Random/Math.random → SecureRandom)" |
| "AES/GCM authenticated mode (not Cipher.getInstance('AES') ECB default)" | Fix for ECB-mode misuse | Draft §C: "ECB mode (Cipher.getInstance('AES') defaults badly) → an authenticated mode (AES/GCM)" |
| "bcrypt / scrypt / Argon2 / PBKDF2 for password hashing (not plain SHA)" | Fix for weak password hashing | Draft §C: "weak password hashing (plain SHA → bcrypt/scrypt/Argon2/PBKDF2 + salt)" |
| "Externalized keys; fresh random IVs per encryption" | Fix for hardcoded/static keys and predictable IVs | Draft §C: "hardcoded keys/IVs + static/predictable IVs → externalized keys; fresh random IVs" |
| "Flagged MD5 may be a legitimate non-security checksum — record a justified suppression (Ch 19)" | FP note and suppression guidance | Draft §C: "a flagged MD5 may be a legitimate non-security checksum, so suppress false positives with a recorded justification (Chapter 19)" |
| "Date algorithm guidance against current OWASP/NIST (key sizes and recommendations evolve)" | Guidance evolution note | Draft §C: "algorithm guidance evolves (key sizes change, post-quantum migration looms), so any specific recommendation must be dated against current OWASP/NIST guidance" |
| "FindSecBugs: weak-algorithm, ECB, predictable-random rules" | Detection tools for crypto misuse | Draft §C: "FindSecBugs (the SpotBugs security plugin, Chapter 16) and Error Prone flag MD5, ECB, and Random-for-keys at build time" |
| "Error Prone: crypto rules (Ch 18)" | Error Prone detection for crypto misuse | Draft back-matter (key 74): "Error Prone (Ch 16/18)"; draft §C same |
| "CryptoGuard / CogniCrypt deeper analysis" | Additional detection tools | Draft back-matter (key 74): "CryptoGuard/CogniCrypt deeper" |
| "Detects misuse patterns — not protocol or key-management design flaws" | Honesty: detection boundary | Draft §C: "Static analysis finds ECB and MD5; it does not find a correct set of primitives composed into an insecure protocol, or a broken key-management design" |
| "Eliminate tier is preferred; mitigate only when elimination is infeasible" (legend) | Hierarchy preference | Draft §"Deep dive": "Moving a vulnerability from 'mitigated' to 'impossible by construction' is always preferable" |
| "Detect catches pattern-matchable signatures; misses design/logic flaws (Ch 31)" (legend) | Detection limitation | Draft §"Deep dive": "automated detection catches patterns, not design" |

---

Source-trace status: YES

Verified against the pinned authority set (00-strategy/SOURCE-PIN.md) at the pins in SOURCE-PIN.md.

Every label, claim, tool name, CWE identifier, OWASP category, API name, and code token in this
diagram traces to draft v1 (key 69/72/74) or its cited authorities (OWASP Top 10:2025, JEP 290,
Effective Java 3rd ed., FindSecBugs, OWASP Cheat Sheet Series, CWE taxonomy). No label is invented.

Notes:
- OWASP Top 10:2025 exact category numbers (A02/A03/A08) are stated as per the draft's own
  `⚠ verify-at-pin` annotation; the draft consistently references these persistent categories.
  Verification against owasp.org at the pin is flagged by the draft itself.
- CWE numbers (89, 78, 502, 327, 330) are well-established identifiers from mitre.org/cwe.
- JEP 290 (ObjectInputFilter) = Java 9+, confirmed in draft back-matter (key 72).
- Effective Java chapter reference (Ch 5 / Ch 12 "Serialization") — draft back-matter (key 72)
  references "Effective Java (Ch 5)" for prefer-alternatives stance; the ⚠ note flags exact item
  number should be verified at pin.
