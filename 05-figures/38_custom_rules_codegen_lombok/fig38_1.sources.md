# Figure source-trace — Fig 38.1: One shape, five artifacts (Ch 18)

- Files: `fig38_1.html` → `fig38_1.png`. Family: Architecture / comparison. Method: HTML → render → crop.
- One idea: Every custom rule is select→predicate→report→register; each tool realizes that skeleton over a different artifact (source tokens / source AST / typed javac AST / bytecode / class graph), and that substrate difference — not a quality ranking — fixes what the rule can see and when it runs.
- Verified against the pinned authority set (00-strategy/SOURCE-PIN.md) at SOURCE-PIN §2 pins dated 2026-06-20.

| Label in diagram | Claim | Pinned source (SOURCE-PIN §2) |
|---|---|---|
| Title "Fig 38.1 — One shape, five artifacts: the custom-rule skeleton" | Identifying label | Draft v1 §"The shared shape of a custom rule" + FINAL_INDEX Ch 18 |
| Subtitle "Every custom rule is select→predicate→report→register…" | Central thesis | Draft v1 §"The shared shape": "Strip away the API differences and every custom rule, in every tool, is the same four steps" |
| Step 1 "Select — program elements of interest" | First step of shared shape | Draft v1 §"The shared shape": "1. Select the program elements of interest" |
| Step 2 "Predicate — does this element violate the invariant?" | Second step | Draft v1 §"The shared shape": "2. Predicate — decide whether a selected element violates the invariant." |
| Step 3 "Report — emit a violation (+ optional suggested fix)" | Third step | Draft v1 §"The shared shape": "3. Report — emit a violation with a message (and, where the tool allows, a suggested fix)." |
| Step 4 "Register & gate — wire into analyzer config; violation fails CI" | Fourth step | Draft v1 §"The shared shape": "4. Register & gate — wire the rule into the analyzer's config and the build so a violation fails CI." |
| Checkstyle column header "13.6.0" | Version | SOURCE-PIN §2 Checkstyle row: 13.6.0 (2026-06) |
| PMD column header "7.25.0 (Java + XPath)" | Version | SOURCE-PIN §2 PMD row: 7.25.0 |
| Error Prone column header "latest 2.x + Refaster" | Version line | SOURCE-PIN §2 Error Prone row: "latest 2.x" |
| SpotBugs column header "4.10.2" | Version | SOURCE-PIN §2 SpotBugs row: 4.10.2 |
| ArchUnit column header "1.4.2" | Version | SOURCE-PIN §2 ArchUnit row: 1.4.2 (2026-04) |
| Checkstyle "Artifact: source token AST" | Substrate | Draft v1 §shared-shape table: "source token AST"; §"Checkstyle (source tokens)." |
| PMD "Artifact: source node AST" | Substrate | Draft v1 §shared-shape table: "source node AST" |
| Error Prone "Artifact: typed javac AST (compile-time)" | Substrate | Draft v1 §shared-shape table: "typed javac AST"; §"Error Prone (typed javac AST, compile-time, with auto-fix)." |
| SpotBugs "Artifact: compiled bytecode (.class)" | Substrate | Draft v1 §shared-shape table: "compiled bytecode"; dossier: "analyzes compiled bytecode, not source" |
| ArchUnit "Artifact: imported class graph" | Substrate | Draft v1 §shared-shape table: "imported class graph"; dossier: ClassFileImporter |
| Checkstyle "Runs at: source pass" | Execution moment | Draft v1 §shared-shape table "Runs at" column |
| PMD "Runs at: source pass" | Execution moment | Draft v1 §shared-shape table |
| Error Prone "Runs at: compile" | Execution moment | Draft v1 §shared-shape table |
| SpotBugs "Runs at: post-compile" | Execution moment | Draft v1 §shared-shape table |
| ArchUnit "Runs at: test phase (JUnit)" | Execution moment | Draft v1 §shared-shape table; §ArchUnit: "runs as a JUnit test" |
| Checkstyle Select: `getDefaultTokens()` / `TokenTypes.*` | API identity | Draft v1 §"Checkstyle (source tokens)": "getDefaultTokens()/getAcceptableTokens()/getRequiredTokens()"; dossier §Primary dependency: "getDefaultTokens()", "TokenTypes.*"; checkstyle.org/writingchecks.html |
| PMD Select: `visit(ASTNode, data)` or XPath expression | API identity | Draft v1 §shared-shape table "Select" column; §ArchUnit prose: "PMD adds a second authoring mode … a declarative XPath expression"; dossier §Primary dependency: "visit(NodeType, Object data)"; docs.pmd-code.org 7.25.0 |
| Error Prone Select: matcher interface e.g. `matchMethodInvocation(tree, state)` | API identity | Draft v1 §"Error Prone": "implements a matcher interface such as MethodInvocationTreeMatcher. Its matchMethodInvocation(tree, state)"; dossier: "matchMethodInvocation(MethodInvocationTree, VisitorState)"; errorprone.info/docs/plugins |
| SpotBugs Select: `OpcodeStackDetector.sawOpcode(int)` | API identity | Draft v1 §shared-shape table: "opcode / annotation visit"; dossier: "OpcodeStackDetector … sawOpcode"; spotbugs.readthedocs.io/implement-plugin.html 4.10.2 |
| ArchUnit Select: `classes().that(DescribedPredicate)` | API identity | Draft v1 §"ArchUnit": "classes().that(myPredicate).should(myCondition)"; dossier: "ArchRuleDefinition.classes()…"; archunit.org/userguide 1.4.2 |
| Checkstyle Report: `log(DetailAST, key, …)` key resolves in `messages.properties` | API identity | Draft v1 §"Checkstyle": "log(DetailAST, messageKey, args), where the key resolves against a messages.properties bundle"; dossier: "log(DetailAST, String key, Object... args)"; checkstyle.org/writingchecks.html |
| PMD Report: `asCtx(data).addViolation(node)` | API identity | Draft v1 §shared-shape table: "asCtx(data).addViolation(…)"; dossier: "asCtx(data).addViolation(node, ...)"; docs.pmd-code.org 7.25.0 |
| Error Prone Report: `describeMatch(tree, SuggestedFix)` | API identity | Draft v1 §"Error Prone": "describeMatch(tree, fix)"; dossier: "describeMatch(…) + SuggestedFix"; errorprone.info/docs/plugins |
| SpotBugs Report: `BugInstance` → `bugReporter.reportBug()` | API identity | Draft v1 §shared-shape table: "BugInstance → reportBug"; dossier: "BugInstance + bugReporter.reportBug(…)"; spotbugs.readthedocs.io/implement-plugin.html 4.10.2 |
| ArchUnit Report: `ArchCondition.check(item, events)` → `SimpleConditionEvent.violated(…)` | API identity | Draft v1 §"ArchUnit": "ArchCondition<JavaClass> (the constraint; override check(item, events) and add SimpleConditionEvent.violated(origin, message))"; dossier; archunit.org/userguide 1.4.2 |
| Checkstyle Register: JAR on Checkstyle classpath; FQN under `Checker → TreeWalker` | Registration mechanism | Draft v1 §"Checkstyle": "Compile it into a JAR … register the check by fully-qualified class name under Checker → TreeWalker"; dossier; checkstyle.org/writingchecks.html |
| PMD Register: ruleset XML `<rule class="…">` | Registration mechanism | Draft v1 §shared-shape table "Register & gate"; dossier: "ruleset XML <rule name=... class=... message=...>"; docs.pmd-code.org 7.25.0 |
| Error Prone Register: `@AutoService(BugChecker.class)`; annotation-processor path | Registration mechanism | Draft v1 §"Error Prone": "discovered off the annotation-processor path via @AutoService(BugChecker.class)"; dossier; errorprone.info/docs/plugins |
| SpotBugs Register: `findbugs.xml <Detector>` + `messages.xml` | Registration mechanism | Draft v1 §shared-shape table; dossier: "findbugs.xml with <Detector class=... reports=... speed=...> and <BugPattern type=... category=...>; messages.xml … ShortDescription / LongDescription / Details"; spotbugs.readthedocs.io/implement-plugin.html 4.10.2 |
| ArchUnit Register: `@AnalyzeClasses` + `@ArchTest` (JUnit 5); throws `AssertionError` | Registration mechanism | Draft v1 §"ArchUnit": "runs as a JUnit test (@AnalyzeClasses + @ArchTest) and throws AssertionError on violation"; dossier; archunit.org/userguide 1.4.2 |
| Error Prone Auto-fix "yes — SuggestedFix; Refaster @BeforeTemplate → @AfterTemplate" | Capability | Draft v1 §"Error Prone": "a Description can carry a SuggestedFix"; "Refaster: write a @BeforeTemplate … and an @AfterTemplate"; dossier: "@BeforeTemplate / @AfterTemplate in com.google.errorprone.refaster.annotation"; errorprone.info/docs/refaster |
| All other tools "Auto-fix: no" | Capability | Draft v1 §shared-shape table Auto-fix column: "no" for Checkstyle, PMD, SpotBugs, ArchUnit |
| Checkstyle "Characteristic limitation: limited type resolution; sees source shape, not behavior" | Limitation | Draft v1 §"Checkstyle": "Its limit is its artifact: token-level source, with limited type resolution." |
| PMD "Characteristic limitation: PMD 7.x reworked AST node names (upgrade-sensitive)" | Limitation | Draft v1 §Limitations: "PMD's 7.x line reworked its AST node names and rule model — custom PMD rules are the most upgrade-sensitive"; dossier: "7.x API churn" |
| Error Prone "Characteristic limitation: requires compile step; @BugPattern zero-arg ctor mandatory" | Limitation | Draft v1 §"Error Prone": "must have a zero-arg constructor"; dossier: "zero-arg constructor required"; errorprone.info/docs/plugins |
| SpotBugs "Characteristic limitation: distance from source; no source-only constructs" | Limitation | Draft v1 §Limitations: "Checkstyle/PMD miss what is only in bytecode; SpotBugs misses source-only constructs" |
| ArchUnit "Characteristic limitation: type-level only; needs compiled classes + test run" | Limitation | Draft v1 §shared-shape table; dossier: "Type-level only, needs compiled classes + test run" |
| Note box "Pick the tool whose artifact the invariant naturally lives in" | Substrate-selection guidance | Draft v1 §shared-shape CONCEPT box: "Pick the tool whose artifact the invariant naturally lives in: a textual/structural convention fits Checkstyle/PMD … type-aware rule fits Error Prone … bytecode-only pattern fits SpotBugs … architectural law fits ArchUnit." |
| Routing note "Which analyzer to run for a given rule type → Ch 17" | Chapter routing | Draft v1 §Overview: "The cross-cutting 'which analyzer to run' verdict is Chapter 17's." |
| Adoption box: "Ship as WARNING first … promote to ERROR after gathering signal" | Adoption guidance | Draft v1 §"Three realizations, briefly": "Ship it as a WARNING first to gather signal, then promote to ERROR" |
| Caption verbatim API identifiers (all tools) | All API identities in caption | Draft v1 §Back matter — sources & traceability; dossier §Primary dependency; SOURCE-PIN §2 respective tool rows |

---

Source-trace status: YES

Verified against the pinned authority set (00-strategy/SOURCE-PIN.md) at the pins in SOURCE-PIN.md (dated 2026-06-20).
All tool names, API class/method identifiers, version numbers, and characterizations trace to draft v1 §"The shared shape of a custom rule," §"Three realizations, briefly," §Limitations, §Back matter, and to dossier 38_writing_custom_rules_RESEARCH.md §Primary dependency.
No atom was introduced by this figure that is not already present in the draft and dossier.
Lombok/AutoValue/Immutables/MapStruct not yet SOURCE-PIN §2 rows (noted in draft as ⚠); those tools do not appear in this figure (they are the subject of fig38_2).

## Accessibility
- Alt-text: Five-column matrix showing the shared select→predicate→report→register rule skeleton as realized by Checkstyle, PMD, Error Prone, SpotBugs, and ArchUnit, with each tool's artifact, execution moment, key APIs, registration mechanism, auto-fix capability, and characteristic limitation.
- Long-description: The figure opens with a four-box strip across the top labelled Step 1 Select, Step 2 Predicate, Step 3 Report, and Step 4 Register & gate, connected left-to-right by arrows, establishing the shared rule shape. Below is a five-column matrix with a row-label column on the left. The header row names the five tools: Checkstyle 13.6.0, PMD 7.25.0, Error Prone latest 2.x + Refaster, SpotBugs 4.10.2, and ArchUnit 1.4.2. Seven data rows follow: (1) Artifact reasoned over — source token AST, source node AST, typed javac AST at compile time, compiled bytecode (.class), imported class graph. (2) Runs at — source pass, source pass, compile, post-compile, test phase (JUnit). (3) Select API — getDefaultTokens()/TokenTypes.*, visit(ASTNode,data) or XPath, matchMethodInvocation(tree,state), OpcodeStackDetector.sawOpcode(int), classes().that(DescribedPredicate). (4) Report API — log(DetailAST,key), asCtx(data).addViolation(node), describeMatch(tree,SuggestedFix), BugInstance→bugReporter.reportBug(), ArchCondition.check(item,events)→SimpleConditionEvent.violated. (5) Register — JAR+FQN under Checker→TreeWalker, ruleset XML <rule class=…>, @AutoService(BugChecker.class) on annotation-processor path, findbugs.xml <Detector>+messages.xml, @AnalyzeClasses+@ArchTest throws AssertionError. (6) Auto-fix — no, no, yes (SuggestedFix; Refaster @BeforeTemplate→@AfterTemplate), no, no. (7) Characteristic limitation — one sentence per tool. Below the matrix, a callout note reads: pick the tool whose artifact the invariant naturally lives in; the choice follows the artifact, not a ranking. A smaller adoption-guidance box advises shipping as WARNING before promoting to ERROR.
