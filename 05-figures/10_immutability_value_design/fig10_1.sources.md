# Figure source-trace — Three instruments of Java immutability (Ch 8)

- Files: fig10_1.html → fig10_1.png. Family: Architecture / layering (family 1). Method: HTML → render.mjs → crop.
- One idea: The three instruments of Java immutability (records, immutable collections, defensive copies) each close a specific gap — composed together they achieve the complete guarantee Item 17 Rule 5 requires.
- Verified against Java code quality pinned authority set (00-strategy/SOURCE-PIN.md).

| Label in diagram | Claim | Pinned source (SOURCE-PIN.md) |
|---|---|---|
| "Records" / "JEP 395 · GA Java 16" | Records are the language-level immutable data carrier, a GA language feature in Java 16 | JEP 395 (openjdk.org/jeps/395); SOURCE-PIN §1 "JEP index — JEPs shipped through 21 and 25" |
| "Private final fields per component; compiler-derived canonical constructor, accessors, equals, hashCode, toString; implicitly final class" | Records generate private final per-component fields, a canonical constructor, per-component accessors (no get prefix), and component-by-component equals/hashCode/toString; the record type is implicitly final | JEP 395 §"Semantics" — spec text for generated members; draft §"Records (JEP 395, GA in Java 16)" |
| "Makes the component reference final — does not copy or freeze a mutable component (shallow only)" | A record makes the component reference final but does not deep-copy or freeze a mutable component — it is shallowly immutable | JEP 395; draft §"The three instruments, and the gap each leaves" ("a record makes the component reference final, but does not deep-copy or freeze a mutable component"); 02-research/10_immutability_value_design/10_immutability_value_design_RESEARCH.md §2.1 |
| "Immutable collections" / "List/Set/Map.of · copyOf · JDK 9+" | List.of/Set.of/Map.of factory methods (JDK 9+) and copyOf (JDK 10+) return conventionally immutable collections | JDK 21 API — java.util.List/Set/Map Javadoc; SOURCE-PIN §1 "OpenJDK (HotSpot) JDK 21.0.11"; draft §"Immutable collections (JDK 9+)"; RESEARCH §2.2 |
| "Throws UnsupportedOperationException on any structural mutation; copyOf produces a snapshot no caller can reach through" | List.of/copyOf collections throw UnsupportedOperationException on add/set/remove; copyOf returns an immutable snapshot | JDK 21 API — java.util.List.of/copyOf Javadoc (spec: "Attempts to modify the returned list … result in an UnsupportedOperationException"); draft §"Immutable collections (JDK 9+)" |
| "Freezes the collection, not its elements; unmodifiableList(x) is a view — still reflects mutation of the source x" | An immutable collection of objects is not a collection of immutable objects; Collections.unmodifiableList is a view, not a copy | JDK 21 API — java.util.Collections.unmodifiableList Javadoc ("Returns an unmodifiable view of the specified list"); draft §"Immutable collections (JDK 9+)" quoting JDK doc; RESEARCH §2.2 |
| "Defensive copies" / "Effective Java Item 50 · compact constructor" | Manual defensive copying (copy mutable parameters on construction; return copies from accessors) is prescribed in Effective Java Item 50; the compact constructor is the record seam for this | Effective Java 3e Item 50 "Make defensive copies when needed" (SOURCE-PIN §7); JEP 395 §"Compact canonical constructor"; draft §"Defensive copies (Item 50)" |
| "A private snapshot no caller can reach; copy before validate; List.copyOf(items) in the compact constructor seals Rule 5" | The technique: store a defensive copy, validate the copy (not the original), so no caller reference reaches the internal; List.copyOf in the compact constructor is the modern shortcut | Effective Java 3e Item 50; JDK 21 API — List.copyOf; JEP 395 compact constructor; draft §"Defensive copies (Item 50)" |
| "Allocation per construction; avoid clone on subclassable types; not needed if the component is already immutable" | Item 17 names object-per-value cost; Item 50 warns against clone on subclassable parameter types; copying is unnecessary for already-immutable types | Effective Java 3e Item 17 ("require a separate object for each distinct value") and Item 50 ("do not use the clone method to make a defensive copy of a parameter whose type is subclassed by untrusted parties"); draft §"Limitations & when NOT to reach for it" |
| "Using the feature ≠ getting the guarantee" (callout) | Each instrument carries immutability's intent but stops at a boundary; "using the feature is not the same as getting the guarantee" | Draft §"The three instruments, and the gap each leaves" — CONCEPT callout (exact phrasing from draft) |
| "Rules 1–4 (Item 17)" reference in callout | Item 17's five rules for an immutable class; records satisfy rules 1–4 by construction | Effective Java 3e Item 17 "Minimize mutability" (five rules enumerated); draft §"How it works — Why immutability is the highest-leverage lever" |
| "List.copyOf(items)" in callout | List.copyOf is the defensive-copy primitive for collection components | JDK 21 API — java.util.List.copyOf Javadoc; draft §"Defensive copies (Item 50)" |
| Subtitle: "Effective Java Item 17 Rule 5" | Item 17 Rule 5: ensure exclusive access to any mutable components (defensive copies in and out) | Effective Java 3e Item 17 Rule 5 (verbatim in draft §"Why immutability is the highest-leverage lever": "Ensure exclusive access to any mutable components") |

Source-trace status: YES
Verified against the pinned authority set (00-strategy/SOURCE-PIN.md) at pins dated 2026-06-20.

## Accessibility
- Alt-text: Three rows for Java's immutability instruments — records, immutable collections, defensive copies — each with the guarantee it gives and the gap it leaves.
- Long-description: The diagram has a title "Three instruments of Java immutability" and three stacked rows connected by downward arrows, under column headers Instrument, Guarantee provided, and Gap it leaves. Row 1 (Records, JEP 395, GA Java 16): Provides private final fields per component, compiler-derived canonical constructor, accessors, equals, hashCode, toString, and an implicitly final class. Gap: makes the component reference final but does not copy or freeze a mutable component (shallow only). Row 2 (Immutable collections, List/Set/Map.of, copyOf, JDK 9+): Provides UnsupportedOperationException on any structural mutation; copyOf produces a snapshot no caller can reach. Gap: freezes the collection not its elements; unmodifiableList(x) is a view that still reflects mutation of the source. Row 3 (Defensive copies, Effective Java Item 50, compact constructor), highlighted with an accent left border: Provides a private snapshot no caller can reach; copy before validate; List.copyOf(items) in the compact constructor seals Rule 5. Cost/limit: allocation per construction; avoid clone on subclassable types; not needed if the component is already immutable. A callout below reads: "Using the feature ≠ getting the guarantee. A record with a List<LineItem> field satisfies Rules 1–4 (Item 17) automatically. Rule 5 — exclusive access to mutable components — requires the compact constructor to call List.copyOf(items). Without it, the reference is final; the list is not."
- Grayscale-safe: Yes. The three instruments are distinguished by row order and labels; the highlighted defensive-copies row is set apart by an accent left border and its position as the closing row, not by hue. The Provides and Gap columns are told apart by their column headers, not by color. Reads in black-and-white print.
- Contrast: Sufficient. Dark ink on white and lightly-tinted cell fills; the inline code tokens read clearly at print size.
- Code legibility: The inline tokens (for example List.copyOf(items), UnsupportedOperationException, equals, hashCode, toString) are real authored monospace text, not a screenshot, and read without syntax color.
