# fig81_1.sources.md — source-trace for Fig 81.1

**Figure:** Fig 81.1 — The feedback-latency ladder: fast feedback at the keyboard, unbypassable enforcement at the merge
**Chapter:** 35 — Teeth and Speed (keys 81 + folds 82, Part IX) · `01-index/FINAL_INDEX.md` line 68
**One idea:** Push each check to the leftmost rung that can catch it — IDE → pre-commit → pre-push are fast, *skippable* feedback (`git commit --no-verify`, install-dependent); PR CI → main are slow, *unbypassable* enforcement (required status check + branch protection + merge queue). Feedback makes enforcement painless; enforcement makes feedback safe to skip.
**NEUTRALITY:** No tool crowned; trunk-based reported as DORA-evidenced for *delivery performance* (not decreed); branching strategy noted context-dependent. Each feedback rung names its fast-checks-only / bypassable limit. No banned comparative wording (`better than` / `unlike X` / `superior` / `beats` / `the problem with X`).

---

## Per-label source-trace table

| Label in diagram | Claim | Pinned source (SOURCE-PIN.md, pinned 2026-06-20) + draft/dossier |
|---|---|---|
| **Title / subtitle — "feedback-latency ladder … leftmost rung that can catch it"** | The ladder orders checks fastest-cheapest at the left; push each check to the leftmost rung that can catch it | draft v1 line 55 (CONCEPT "The feedback-latency ladder"); dossier 82 §6 Fig 82.1 spec; draft line 111 |
| **Cost axis: "Cheap · seconds" → "Expensive · the last line"** | Quality feedback gets more expensive the later it arrives; each rung is more expensive than the one before | draft v1 line 55 |
| **Rung 1 — IDE · "As you type" · "Developer's editor" (Ch 17)** | The IDE flags issues as the developer types; the leftmost, fastest feedback | draft v1 lines 55, 88; routing line 10 ("IDE (leftmost feedback) → Ch 17 (36)"); dossier 82 §6 (editor/IDE key 36) |
| "In-editor inspections flag issues as the code is written" | IDE in-editor checks mechanism | draft v1 line 55; line 88 (Ch 17 first line) |
| "Leftmost, fastest signal — never a gate (same principle as the hooks)" | Treating the IDE's in-editor checks as a gate is a category error; same feedback-not-enforcement principle | draft v1 line 59; line 88 |
| **Rung 2 — Pre-commit hook · "Seconds — on `git commit`" · "Developer's machine"** | Pre-commit hooks run fast targeted checks at `git commit` time, in seconds, on the dev's machine | draft v1 line 53; dossier 82 §2; SOURCE-PIN.md §5 "pre-commit / git hooks (pre-commit.com)" |
| "Spotless format" (Ch 6) | Format check via Spotless is a fast pre-commit check | draft v1 line 53; dossier 82 §2 (key 34); SOURCE-PIN.md §2 "Spotless 3.6.0"; Ch 6 (key 34) |
| "Quick Checkstyle / PMD subset" (Ch 16) | A quick Checkstyle/PMD subset is a fast pre-commit check (not the full ruleset) | draft v1 line 53; dossier 82 §2; SOURCE-PIN.md §2 "Checkstyle 13.6.0" + "PMD 7.25.0"; Ch 16 (keys 27/28) |
| "gitleaks secrets scan" (Ch 31) | A secrets scan (gitleaks) is a fast pre-commit check | draft v1 line 53; dossier 82 §2 (secrets gitleaks, key 71); routing line 10 ("secrets (gitleaks) → Ch 31 (71)"); Ch 31 (key 71) — *gitleaks named at Ch-31 dossier; no version asserted here* |
| "Trailing-whitespace / large-file" | Trailing-whitespace and large-file checks are fast pre-commit checks | draft v1 line 53; dossier 82 §2 |
| "Managed by the pre-commit framework (`.pre-commit-config.yaml`)" | The pre-commit framework manages multi-tool hooks reproducibly via `.pre-commit-config.yaml` | draft v1 line 57; dossier 82 §2; SOURCE-PIN.md §5 "pre-commit latest (2026-04 release), pre-commit.com" |
| "Feedback · fast checks only" | Pre-commit holds fast checks only; slow checks belong in CI (slow hooks get disabled) | draft v1 lines 53, 78; dossier 82 §4 |
| **Rung 3 — Pre-push hook · "Heavier — before sharing" · "Developer's machine"** | Pre-push hooks run slightly heavier checks before sharing — a middle rung on the ladder | draft v1 lines 55, 87; dossier 82 §2 |
| "Fast unit tests" (Part V) | Pre-push runs fast unit tests (heavier than commit, lighter than CI) | draft v1 lines 55, 87; dossier 82 §2 |
| "A middle rung — heavier than commit, lighter than CI" | Pre-push is the heavier-than-commit, lighter-than-CI rung | draft v1 line 87 |
| **Rung 4 — PR's CI · "Minutes — the full gate" · "CI — every pull request" (Ch 33)** | The PR's CI runs the full gate; it is the enforcement that runs on every pull request | draft v1 lines 55, 59; routing line 10 (pipeline design → Ch 33); SOURCE-PIN.md §5 "GitHub Actions / GitLab CI / Jenkins" |
| "The full quality gate re-runs everything: compile, full SpotBugs, the test suite, Sonar" (Ch 33) | Slow checks — full SpotBugs, the test suite, Sonar — belong in CI, not the commit hook; CI re-runs everything regardless | draft v1 lines 53, 59; SOURCE-PIN.md §2 "SpotBugs 4.10.2" + "SonarQube Server 2026.1 LTA"; Ch 33 (keys 75/79) |
| "Required status check + required review" (Ch 37) | The PR-fast result is a required status check; required review is the *other* required check | draft v1 lines 43, 94; routing line 10 ("human review (required-review) → key 84"); Ch 37 (key 84) — *Ch 35 draft routes human review to "Chapter 84/key 84"; FINAL_INDEX places code-review at Ch 37* |
| "Enforcement · blocks merge" | The required CI check blocks the merge when not passed | draft v1 lines 43, 100; dossier 81 §2 |
| "Parity: same `./mvnw -B verify` + pinned versions as local" (Ch 27, 29) | Local↔CI parity: the wrapper command `./mvnw -B verify` + pinned tool versions run identically local and in CI | draft v1 line 57; dossier 82 §2 (key 62); SOURCE-PIN.md §4 "Apache Maven 3.9.16" + runtime baseline (wrapper); Ch 27 (key 62) + Ch 29 (key 67) |
| **Rung 5 — main · "Kept always-green" · "Protected trunk"** | main is kept always-green / always-shippable; the protected trunk | draft v1 lines 47, 104; dossier 81 §1–2 |
| "Branch protection: gate is a required check; restrict force-push / deletion" | Branch protection requires the quality gate as a required status check, requires review, requires up-to-date branches, restricts force-push/deletion → unbypassable | draft v1 line 43; dossier 81 §2; SOURCE-PIN.md §5 "GitHub Actions docs 2026-06" (docs.github.com) |
| "Merge queue: re-validate each PR vs the latest base + preceding queued PRs" | The merge queue serializes merges and re-validates each PR against the latest base + preceding queued PRs, so colliding green PRs can't break main; GitHub merge queue is GA | draft v1 line 47; dossier 81 §2; SOURCE-PIN.md §5 "GitHub Actions" (docs.github.com — merge queue GA, ⚠ specifics pinned at use) |
| "Trunk-based: small frequent merges; incomplete work behind feature flags" (Ch 36) | Trunk-based development = small frequent merges to a stable main on short-lived branches; incomplete work behind feature flags (the trunk-based companion, → release Ch 36) | draft v1 lines 45, 85, 96; dossier 81 §2; SOURCE-PIN.md §5 "DORA / Accelerate (dora.dev)"; Ch 36 (key 83) |
| "Enforcement · unbypassable" / "No path to main that skips the gate" | Required status check + branch protection + merge queue means no path to main that skips the gate | draft v1 lines 43, 67, 69; dossier 81 §2 |
| **Boundary — "Rungs 1–3 · SPEED · Feedback — deliberately skippable"** | The left rungs (IDE, pre-commit, pre-push) are feedback, deliberately fast and deliberately skippable | draft v1 lines 59, 69 (honest center); dossier 82 §2, §4 |
| "Bypassable on purpose: `git commit --no-verify` skips a hook, and hooks run only where installed" | A pre-commit hook is bypassable (`git commit --no-verify`) and runs only on machines that installed it → feedback, not the gate | draft v1 lines 59, 77; dossier 82 §2, §4; SOURCE-PIN.md §5 "pre-commit / git hooks (git-scm.com/docs/githooks)" |
| "Slow checks here get disabled — keep them fast" | Slow hooks get disabled; put only fast checks in the commit hook | draft v1 line 78; dossier 82 §4 |
| **Boundary — "Rungs 4–5 · TEETH · Enforcement — deliberately unbypassable"** | The right rungs (PR CI, main) are enforcement, deliberately unbypassable; the required CI check + branch protection + merge queue re-run everything regardless | draft v1 lines 59, 65, 69; dossier 81 §2 |
| "This is the gate; a hook never is." | CI + branch protection are the actual enforcement; a hook can never be the gate (category error) | draft v1 lines 59, 77, 100 |
| **Note — "Feedback makes enforcement painless; enforcement makes feedback safe to skip."** | The feedback layer makes the enforcement layer painless; the enforcement layer makes the feedback layer safe to skip | draft v1 line 69 |
| "Local↔CI parity … 'green locally' reliably predict 'green in CI' … not a ten-minute CI round-trip" | Parity via wrapper + pinned versions makes green-locally predict green-in-CI; avoids the ten-minute CI round-trip | draft v1 lines 22, 55, 57; dossier 82 §2–3 |
| "Trunk-based development is reported by DORA as correlating with delivery performance, given a trustworthy gate, reliable tests, and feature flags" | DORA reports trunk-based as correlating with delivery performance; precondition = trustworthy gate (Parts IV–VIII) + reliable tests (Ch 20) + feature flags; not "just commit to main" | draft v1 lines 45, 73; dossier 81 §2–4; SOURCE-PIN.md §5 "2025 DORA report + Accelerate (2018), dora.dev" |
| "branching strategy stays context-dependent and no single model is crowned" | Branching strategy is context-dependent (OSS/regulated/release-train, big risky migration); DORA favors trunk-based for delivery but crowns no single model | draft v1 lines 35, 49, 74; dossier 81 §4 (HONEST-LIMITATIONS) |

---

Source-trace status: **YES**

Verified against the pinned authority set (00-strategy/SOURCE-PIN.md) at the pins in SOURCE-PIN.md (2026-06-20). Every rung, latency label, check atom, boundary claim, and conceptual claim traces to the Chapter 35 draft v1 (keys 81 + 82), the two banked dossiers (`02-research/81_branch_protection_trunk`, `02-research/82_precommit_local_ci_parity`), and the pinned rows in SOURCE-PIN.md (pre-commit / git hooks; DORA / dora.dev; GitHub Actions; Spotless; Checkstyle; PMD; SpotBugs; SonarQube; Maven wrapper). No atom is invented: gitleaks is named (no version asserted) per its Ch-31 home (key 71); GitHub merge-queue specifics and DORA wording carry the dossier's `⚠ verify-at-pin` note and are stated only at the mechanism level the draft uses. NEUTRALITY: no tool crowned; honest limits (fast-checks-only, bypassable, context-dependent branching) on the figure face; no banned comparative wording.
