# Fig 13.1 sources — JMM happens-before edges and safe publication

Chapter 13 (dossier key 20, folds 21+23) · draft v1 · `03-drafts/20_thread_safety_jmm/20_thread_safety_jmm_v1.md`

| Label / element in diagram | Claim / atom | Pinned source path |
|---|---|---|
| Title "Fig 13.1 — Java Memory Model: happens-before edges and safe publication" | Identifying label | Draft §"Happens-before: the definition of 'correct'" + §"The edges you can rely on" + §"Safe publication: the four idioms" |
| Subtitle "JLS SE 21 §17.4.5 · six edges that establish cross-thread visibility · four idioms for safely publishing an object reference" | Framing | JLS SE 21 §17.4.5 (pinned: docs.oracle.com/javase/specs, SE 21 edition) |
| Column header "HB edge (JLS §17.4.5)" | Section framing | JLS SE 21 §17.4.5 (verbatim section reference) |
| Edge "Program order" and description "Within one thread: earlier action hb later action (single-thread as-if-serial)" | JMM edge + as-if-serial rule | JLS SE 21 §17.4.5: "Within a thread, an earlier action happens-before a later action" (verbatim structure); draft line: "the as-if-serial rule" |
| Edge "Monitor lock" and description "unlock on a monitor hb every subsequent lock on it — exclusion + visibility" | JMM edge | JLS SE 21 §17.4.5 (draft table: "an unlock on a monitor happens-before every subsequent lock on it (`synchronized`: exclusion **and** visibility)") |
| Edge "Volatile write/read" and description "write to volatile field hb every subsequent read of it — visibility only, no exclusion" | JMM edge | JLS SE 21 §17.4.5 (draft table: "a write to a volatile field happens-before every subsequent read of it (visibility, **no** mutual exclusion)") |
| Edge "Thread.start()" and description "Thread.start() hb any action in the started thread" | JMM edge | JLS SE 21 §17.4.5 (draft table: "`Thread.start()` happens-before any action in the started thread") |
| Edge "Thread.join()" and description "every action in thread T hb another thread's return from T.join()" | JMM edge | JLS SE 21 §17.4.5 (draft table: "every action in a thread happens-before another thread's successful return from `join()` on it") |
| Edge "java.util.concurrent" and description "actions before put into concurrent collection hb actions after take; before executor submit hb task execution" | JMM edge | `java.util.concurrent` package summary "Memory Consistency Properties" (JDK 21); draft table: "actions before putting into a concurrent collection happen-before actions after taking; before submitting to an executor happen-before the task's execution" |
| Transitivity row: "If hb(A, B) and hb(B, C), then hb(A, C) — edges compose" | JMM transitivity rule | JLS SE 21 §17.4.5 (draft table: "if `hb(x,y)` and `hb(y,z)`, then `hb(x,z)`") |
| Guarantee: "Single-thread ordering only — Does not cross thread boundaries" | Clarification | Draft §"Happens-before: the definition of 'correct'": the as-if-serial rule applies within a single thread |
| Guarantee: "Mutual exclusion + full visibility — All writes by the releasing thread are visible to the acquiring thread. The heavyweight, complete tool." | Monitor guarantee | Draft §"Three levers: synchronized, volatile, final": "**`synchronized`** … provides *both* mutual exclusion … *and* visibility" |
| Guarantee: "Visibility — not atomicity — Latest write is visible… Does NOT make read-modify-write atomic. count++ on a volatile field still loses updates" | Volatile semantics | Draft §"Three levers": "`volatile` does **not** make compound actions atomic. `count++` on a `volatile` field is a read-modify-write and can still lose updates" |
| Guarantee: "SpotBugs VO_VOLATILE_INCREMENT" | Tool rule ID | SpotBugs 4.10.2 MT_CORRECTNESS category: `VO_VOLATILE_INCREMENT` (draft: "SpotBugs flags it as `VO_VOLATILE_INCREMENT`") |
| Guarantee: "Publication at thread birth — Everything the parent thread did before start() is visible inside the new thread" | Thread.start() edge semantics | JLS SE 21 §17.4.5; j.u.c package summary "Memory Consistency Properties" (JDK 21) |
| Guarantee: "Publication at thread death — Everything the joined thread did is visible to the joining thread after join() returns" | Thread.join() edge semantics | JLS SE 21 §17.4.5; j.u.c package summary "Memory Consistency Properties" (JDK 21) |
| Guarantee: "Library-encoded edges — Every j.u.c utility documents its happens-before guarantee" | j.u.c design principle | Draft: "every utility documents which happens-before edge it provides"; j.u.c package summary "Memory Consistency Properties" (JDK 21) |
| Column header "Safe-publication idiom" | Framing | Draft §"Safe publication: the four idioms"; *Java Concurrency in Practice* §3.5 (the catalogue) |
| Idiom: "static final (class-init lock)" spacer label | Safe-publication idiom 1 | Draft table: "**Static initializer** · `static final X x = new X();` · JVM class-initialization locking publishes it fully" |
| Idiom card "Lock-guarded publish — publish + read inside synchronized — monitor unlock → lock edge" | Safe-publication idiom 2 | Draft table: "**Lock-guarded** · publish and read inside `synchronized` · monitor unlock happens-before subsequent lock" |
| Idiom card "volatile / AtomicReference — volatile X x; /\* then publish \*/ — volatile write → read edge" | Safe-publication idiom 3 | Draft table: "**`volatile` / `AtomicReference`** · `volatile X x;` then publish · volatile write happens-before subsequent read" |
| Idiom: "final field (§17.5 freeze)" spacer label | Safe-publication idiom 4 | Draft table: "**`final` field** (of a properly-constructed object) · `final X x;` set in the constructor · the §17.5 final-field freeze"; JLS SE 21 §17.5 |
| Idiom: "(join: used for shutdown / await, not publication)" spacer label | Clarification that join is not a publication idiom | Draft content: join is listed as a happens-before edge but does not appear in the four safe-publication idioms |
| Idiom card "Concurrent collection / executor — BlockingQueue / ExecutorService — j.u.c put-before-take edge" | j.u.c as safe-publication channel | Draft: "reach for a j.u.c construct first"; j.u.c package summary "Memory Consistency Properties" (JDK 21) |
| Rule box "final-field freeze (§17.5)" text: "A thread that sees a reference to a fully-constructed object is guaranteed to see correctly initialized values for that object's final fields" | JLS verbatim guarantee | JLS SE 21 §17.5 (draft: verbatim: "A thread that can only see a reference to an object after that object has been completely initialized is guaranteed to see the correctly initialized values for that object's final fields.") |
| Rule box "final-field freeze": "A record (all-final components) is thread-safe to share for free" | Record / final connection | Draft: "why a `record` (all-`final` components, canonical constructor) is thread-safe to share for free"; JEP 395 (records, Java 16) |
| Rule box "final-field freeze": "Trap: if this escapes during construction, the freeze is voided" | this-escape defect | Draft: "Publishing `this` from inside a constructor … defeats the freeze … **`this`-escape** defect" |
| Rule box "Unsafe publication" text: "Handing a reference through a plain (non-final, non-volatile, un-locked) field lets a reader observe default values (0 / null), even after the constructor returned. JMM permits this." | Unsafe publication hazard | Draft §"Safe publication": "the JMM genuinely permits it: handing a reference through an ordinary, non-`final`, non-`volatile`, non-locked field lets a reader observe the object's fields at their *default* (zero/null) values, even though the constructor 'finished.'" |
| Rule box "volatile ≠ atomic": "Use volatile for a flag or a published reference, never for a counter. Use AtomicInteger / LongAdder for read-modify-write." | volatile semantics rule | Draft §"Three levers": "Use `volatile` for a flag or a published reference, never for a counter"; §`java.util.concurrent`: "`AtomicInteger`… gives lock-free single-variable updates… `LongAdder`… trades space for throughput" |
| Rule box "volatile ≠ atomic": "SpotBugs VO_VOLATILE_INCREMENT catches volatile++ at build time" | SpotBugs rule | SpotBugs 4.10.2 MT_CORRECTNESS: `VO_VOLATILE_INCREMENT` (draft: "SpotBugs flags it as `VO_VOLATILE_INCREMENT`") |
| Caption reference: "JLS SE 21 §17.5 + §17.5.1 (final-field freeze guarantee + freeze action)" | Source citation | JLS SE 21 §17.5 and §17.5.1 (draft: "Formally (§17.5.1), a *freeze* action on each final field happens when the constructor exits") |
| Caption reference: "Java Concurrency in Practice §3.5 (the four-idiom catalogue)" | Secondary source citation | *Java Concurrency in Practice* (Goetz et al., 2006) §3.5; SOURCE-PIN.md §7 named-book canon |
| Caption reference: "SpotBugs 4.10.2 MT_CORRECTNESS category" | Tool version | SOURCE-PIN.md §2: SpotBugs **4.10.2** |
| Caption reference: "JLS SE 21 §17.7" (non-atomic long/double) | JLS section | JLS SE 21 §17.7 (draft: verbatim: "a single write to a non-volatile `long` or `double` value is treated as two separate writes") — the section is referenced in caption only |

---

Source-trace status: YES

Verified against the pinned authority set (00-strategy/SOURCE-PIN.md) at the pins in SOURCE-PIN.md.

Every edge name, guarantee text, idiom label, code fragment, rule ID, JLS section reference, and tool
name in Fig 13.1 traces to an explicit passage in `03-drafts/20_thread_safety_jmm/20_thread_safety_jmm_v1.md`
or to a named primary source pinned in `00-strategy/SOURCE-PIN.md` (JLS SE 21, JDK 21 j.u.c package summary,
SpotBugs 4.10.2, JEP 395, *Java Concurrency in Practice* 2006 §3.5). No atom was invented by the diagram.
The `this`-escape trap, the volatile-vs-atomic distinction, and the four safe-publication idioms are sourced
from the draft, which cites JLS §17.4.5 / §17.5 / §17.5.1 and the j.u.c "Memory Consistency Properties"
section verbatim. The SpotBugs rule ID `VO_VOLATILE_INCREMENT` is the SpotBugs 4.10.2 MT_CORRECTNESS
category name (cited in the draft from the tool's own documentation).
