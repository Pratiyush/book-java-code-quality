# Fig 5.2 sources — Records serve, not retire, the immutability principle

Chapter 5 (dossier key 08, folds 13) · draft v1 · `03-drafts/08_effective_java/08_effective_java_v1.md`

| Label / element in diagram | Claim / atom | Pinned source path |
|---|---|---|
| Title "Fig 5.2 — Records serve, not retire, the immutability principle" | Identifying label | Draft v1 §Deep dive: "The honest framing…is nuance, not replacement: records serve the common case (a plain immutable data carrier) and shrink the boilerplate; they do not retire the principle." |
| Subtitle "Choose the right form — records cover the common case; the principle (EJ Items 15–17) applies to every form." | Framing | Draft v1 §When to use: "Reach for a record for a transparent, immutable data carrier — the common case the immutability principle covers." |
| **Start node — "The class under review / Apply the immutability principle (EJ Items 15–17)"** | | |
| "Apply the immutability principle (EJ Items 15–17)" | Principle reference | Draft v1 §How it works: "Minimize mutability"; back-matter Key takeaways: "Records serve, not retire, the immutability principle" |
| **Q1 — "Is the data transparent and immutable with no identity semantics?"** | | |
| "transparent and immutable" | Record definition term | Draft v1 §Deep dive: "A record carries transparent, immutable data; its components are its API." |
| "no identity semantics" | Exclusion criterion | Draft v1 §Deep dive: "types with invariants, validation, or a hidden representation still need the hand-written form" — identity-bearing classes implied |
| **YES branch → Q2 — "Needs validation or normalization?"** | | |
| "Needs validation or normalization?" | Decision point | Draft v1 §Deep dive: "a record with a compact constructor that validates" and §When to use: "Add a compact constructor for validation" |
| **Q2-NO leaf — "Plain record"** | | |
| "Plain record" | Outcome label | Draft v1 hook: "record Point(int x, int y, String label) {}" — plain record, no constructor body needed |
| `record Point(int x, int y) {}` | Monospace example | Draft v1 hook: "on Java 21 the whole class is one line: record Point(int x, int y, String label) {}" (simplified to two components for clarity) |
| "equals / hashCode / toString auto-generated from components" | Generated methods claim | Draft v1 §How it works table row 2: "Records generate all three from the components" |
| "JEP 395 · final Java 16" | JEP number + version | Draft v1 back-matter Reference: "Records JEP 395 (Java 16)" |
| **Q2-YES leaf — "Record + compact constructor"** | | |
| "Record + compact constructor" | Outcome label | Draft v1 §Deep dive: "a record with a compact constructor that validates"; §When to use: "Add a compact constructor for validation" |
| "Validate in canonical form; components remain immutable; invariant enforced at construction" | Compact-constructor semantics | Draft v1 §Deep dive: "The honest framing…records serve the common case…; they do not retire the principle" + §When to use: "hand-write the class when invariants, identity, or a hidden representation are required" — compact constructor is the middle path |
| "JEP 395 · Java 16 / EJ Item 17 (validation)" | JEP + item | Draft v1 back-matter Reference: "Records JEP 395 (Java 16)"; Item 17 is the EJ item on minimizing mutability with validation |
| **NO branch — reason note** | | |
| "Has identity, hidden representation, behaviour, or mutable state — a record does not fit this shape." | Exclusion reasons | Draft v1 §Deep dive: "A record carries transparent, immutable data; its components are its API. But the Item-on-minimizing-mutability covers more: types with invariants, validation, or a hidden representation still need the hand-written form" |
| Draft v1 §Back matter WARNING: "A record exposes all components and is for data; a class with behaviour, encapsulated state, or validation beyond a compact constructor is not a record candidate." | Reinforcing exclusion | Draft v1 §Deep dive WARNING box |
| **Q3 — "Needs encapsulated state or custom equals / identity?"** | | |
| "Needs encapsulated state or custom equals / identity?" | Decision point | Draft v1 §How it works table row 2: "Stands for classes with identity or custom equality" |
| **NO-branch leaf — "Hand-written immutable class"** | | |
| "Hand-written immutable class" | Outcome label | Draft v1 §Deep dive: "still need the hand-written form" |
| "Private final fields; no setters; defensive copies where needed" | Immutable class recipe | Draft v1 §How it works: "Minimize mutability — immutable objects…"; EJ Items 15–17 (this recipe is the canonical EJ treatment) |
| "Override equals / hashCode / toString per the contract" | Object contract | Draft v1 §How it works: "Obey the equals/hashCode/Comparable contracts — violate them and collections and sorting silently misbehave" |
| "EJ Items 15–17 (minimize mutability) / EJ Item 10 (equals contract)" | Item citations | Draft v1 §How it works table rows 1–2 |
| **Bottom principle bar** | | |
| "Principle: minimize mutability — stands regardless of which form is chosen above (EJ Items 15–17)" | Principle persistence | Draft v1 §Deep dive: "records serve the common case…they do not retire the principle." Key takeaways: "Records serve, not retire, the immutability principle" |
| **Folklore note** | | |
| "'Records replace the immutability item.' They do not." | Counter-claim | Draft v1 §Deep dive: "A tempting over-claim has emerged: records replace Effective Java's immutability item. They do not." |
| "A record exposes all components and is for transparent data." | Scope of records | Draft v1 §Deep dive and WARNING box: "A record exposes all components and is for data" |
| "A class with behaviour, encapsulated state, or validation beyond a compact constructor is not a record candidate." | Exclusion rule | Draft v1 §Deep dive WARNING: verbatim |
| "Use the feature where it fits the principle — not as a default." | Anti-reflex warning | Draft v1 §Deep dive WARNING: "Reaching for a record reflexively for any small class is its own anti-pattern." |
| "Draft v1 §Deep dive; traced to JEP 395 + EJ Item 15." | Source trace in note | Draft v1 §Deep dive references JEP 395 and EJ items throughout |
| **Caption** | | |
| "JEP 395 (records, final Java 16)" | Caption JEP | Draft v1 back-matter Reference: "Records JEP 395 (Java 16)" |
| "Effective Java 3e Items 10–12, 15–17 (Bloch 2018)" | Caption item range | Draft v1 §How it works (items cited in rows 1–2 of canon-dating table) |
| "'Compact constructor' term from JEP 395 §Compact constructors" | Term origin | JEP 395 §Compact constructors (pinned JDK 21.0.11 / 25.0.3) |

---

Source-trace status: YES

Verified against the pinned authority set (00-strategy/SOURCE-PIN.md) at the pins in SOURCE-PIN.md.

Every decision node, branch label, leaf outcome, and note in Fig 5.2 traces to an explicit passage in `03-drafts/08_effective_java/08_effective_java_v1.md`. JEP 395 is verified at the pin (JDK 21.0.11 / 25.0.3). The monospace example `record Point(int x, int y) {}` is a simplified form of the hook's own one-liner (`record Point(int x, int y, String label) {}`); the simplification removes one field for diagram legibility but changes no claim. No atom was introduced by the figure that is not present in the draft. The folklore counter-claim ("do not retire the principle") matches the draft's §Deep dive verbatim.
