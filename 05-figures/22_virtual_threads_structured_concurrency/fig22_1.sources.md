# Fig 14.1 — Source trace sidecar
## Virtual thread mounting, unmounting, and the pinning trap

Chapter 14 (dossier key 22) · JEP 444 (GA, Release 21) · JEP 491 (Release 24)

| Label / atom in diagram | Claim | Pinned source path |
|---|---|---|
| "Normal path — blocking inside ReentrantLock" (track header) | ReentrantLock does not pin; is the documented mitigation at Java 21 | Draft §22 "The pinning trap — and its version boundary"; JEP 444 pinning section |
| "carrier stays free" (badge) | Carrier OS thread is freed when virtual thread unmounts on blocking I/O | JEP 444: "when [a virtual thread] blocks on a blocking I/O … the Java runtime automatically unmounts the virtual thread from its carrier" |
| "Runs virtual thread VT-1" → "I/O blocks → VT-1 unmounts" → "Picks up & runs virtual thread VT-2" → "I/O done → VT-1 remounts" (carrier lane) | Mounting/unmounting/remounting lifecycle sequence | JEP 444: mounting, unmounting, work-stealing FIFO ForkJoinPool scheduler |
| "Parked (off carrier)" / "Still parked" (VT-1 lane) | Virtual thread is suspended off the carrier during a blocking operation | JEP 444: virtual thread unmounted from carrier during blocking; carrier reused |
| JEP 444 verbatim annotation: "when [a virtual thread] blocks on a blocking I/O … the Java runtime automatically unmounts the virtual thread from its carrier" | Direct verbatim quote | JEP 444 (GA, Release 21) — quoted in draft §22 back matter |
| "Pinning path — blocking inside synchronized" (track header) | synchronized causes pinning | JEP 444: "When it executes code inside a synchronized block or method … its carrier and the underlying OS thread are blocked for the duration" |
| "carrier blocked (Java 21–23)" (badge) | Pinning applies on Java 21–23 | JEP 444 (Release 21); JEP 491 (Release 24) removes synchronized pinning |
| "fixed in Java 24 (JEP 491)" (badge) | JEP 491 removes synchronized pinning at Java 24 | JEP 491: "the synchronized keyword no longer pins virtual threads" |
| "PINNED — VT-1 cannot unmount; carrier OS thread is blocked" | Verbatim characterisation of the pinning behaviour | JEP 444: "its carrier and the underlying OS thread are blocked for the duration of the operation" |
| "Blocking call inside synchronized — cannot unmount" | The precise failure mode | JEP 444 pinning definition |
| "-Djdk.tracePinnedThreads" (annotation text) | JVM flag for diagnosing pinning events | JEP 444 diagnostics section; draft §22 back matter |
| "JFR event" (annotation text) | JDK Flight Recorder pinned-thread event | JEP 444: "jdk.tracePinnedThreads" / JFR pinned-thread event — draft §22 back matter |
| "java:S6906" (annotation text) | Sonar rule flagging synchronized in virtual-thread tasks | Draft §22: "Sonar java:S6906 flags exactly 'virtual threads should not run tasks that include synchronized code'" |
| "Java 21–23 / synchronized pins. Mitigation: replace with ReentrantLock … (Sonar java:S6906)" (version table) | Version-bound advice: true on 21–23, unnecessary on 24+ | Draft §22 §"The pinning trap — and its version boundary"; JEP 444; JEP 491 |
| "Java 24+ (JEP 491) / synchronized no longer pins. JEP 491: 'the synchronized keyword no longer pins virtual threads.' Native methods and foreign-function calls still pin." | Direct verbatim quote of JEP 491 | JEP 491 (Release 24): "the synchronized keyword no longer pins virtual threads, but we will retain it for other pinning situations" |

Source-trace status: YES
Verified against the pinned authority set (00-strategy/SOURCE-PIN.md) at the pins in SOURCE-PIN.md.
Every label, arrow, version badge, and verbatim quote in Fig 14.1 traces to JEP 444 (GA, Release 21), JEP 491 (Release 24), Sonar java:S6906, or the chapter draft §22 which itself cites those JEPs.
