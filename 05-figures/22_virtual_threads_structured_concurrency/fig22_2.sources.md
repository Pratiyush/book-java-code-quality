# Fig 14.2 — Source trace sidecar
## Three-layer concurrency verification stack

Chapter 14 (dossier key 22) · JCStress 0.16 · Error Prone · SpotBugs · Checker Framework · JLS SE 21 §17.4.5

| Label / atom in diagram | Claim | Pinned source path |
|---|---|---|
| "Layer 1 — Static Analysis / Build time" (column header) | Static tools operate at build time | Draft §22 §"Catching it at build time: three static approaches" |
| "Error Prone / Source AST" | Error Prone reasons over source AST | Draft §22 static detection table |
| "@GuardedBy(\"lock\")" | Error Prone GuardedBy annotation spelling | Draft §22: "com.google.errorprone.annotations.concurrent.GuardedBy"; Error Prone source |
| "ON_BY_DEFAULT ERROR" (severity) | Error Prone GuardedBy severity | Draft §22: "GuardedBy (ON_BY_DEFAULT ERROR)"; Error Prone docs |
| "Gives: build-failing check on annotated fields; lock-discipline at every access site" | GuardedBy gives | Draft §22 static detection table row for Error Prone |
| "Cannot: races on un-annotated fields; non-lock visibility races" | GuardedBy limitation | Draft §22 static detection table |
| "SpotBugs MT_CORRECTNESS / Bytecode / Heuristic" | SpotBugs MT_CORRECTNESS category, bytecode-level heuristic analysis | Draft §22 §"Catching it at build time" + static table |
| "IS2_INCONSISTENT_SYNC" | SpotBugs bug pattern ID | Draft §22 back matter: "IS2_INCONSISTENT_SYNC '≤⅓ accesses unsync, writes ×2'" |
| "DC_DOUBLECHECK" | SpotBugs bug pattern ID | Draft §22 back matter: "DC_DOUBLECHECK/DC_PARTIALLY_CONSTRUCTED" |
| "VO_VOLATILE_INCREMENT" | SpotBugs bug pattern ID | Draft §22 back matter |
| "AT_OPERATION_SEQUENCE_ON_CONCURRENT_ABSTRACTION" | SpotBugs bug pattern ID | Draft §22 back matter |
| "docs note 'inaccuracy'" (SpotBugs limit) | SpotBugs FP/FN documentation statement | Draft §22: "heuristic FPs/FNs (its own docs note 'inaccuracy')" |
| "Checker Framework Lock Checker / Pluggable Types / Sound" | Checker Framework Lock Checker approach | Draft §22 static detection table |
| "@GuardedBy / @Holding / @EnsuresLockHeld / @GuardSatisfied" | Checker Framework Lock Checker annotations | Draft §22 back matter: "@GuardedBy/@Holding/@EnsuresLockHeld/@GuardSatisfied" |
| "if it type-checks without error, no data races on typed fields" (soundness statement) | Checker Framework soundness claim | Draft §22: "if the Lock Checker type-checks your program without errors, then your program will not have data races caused by unsynchronized accesses to shared mutable fields" |
| "Gives: a guarantee for the checked lock-race property" | Checker Framework gives | Draft §22 static detection table |
| "Cannot: annotation effort; scoped to lock races only" | Checker Framework limitation | Draft §22 static detection table |
| "Layer 2 — Stress Sampling / Verify time (separate build target)" | JCStress is a separate build target, verify-time tool | Draft §22: "a separate build target, and not a merge-blocking gate" |
| "JCStress / OpenJDK / Probabilistic" | JCStress is OpenJDK, probabilistic | Draft §22: "OpenJDK Java Concurrency Stress harness … experimental and probabilistic" |
| "@State" | JCStress annotation | Draft §22: "@State class" |
| "@Actor" | JCStress annotation; "each run by one thread, exactly once per state instance, with inter-actor order deliberately unspecified" | Draft §22 §"Testing the race: stress, deterministic, and the anti-pattern" |
| "@Result" | JCStress annotation | Draft §22 |
| "@Outcome(expect=…)" | JCStress annotation | Draft §22 |
| "ACCEPTABLE — legal outcome" | JCStress Expect value | Draft §22: "a legal outcome (not required to appear)" |
| "ACCEPTABLE_INTERESTING — legal reordering worth surfacing" | JCStress Expect value | Draft §22: "legal but highlighted — the reordering worth surfacing" |
| "FORBIDDEN — must never appear; if observed, defect confirmed" | JCStress Expect value | Draft §22: "must never appear; if observed, the JMM was violated or the code is broken" |
| "UNKNOWN — un-graded (test-completeness gap)" | JCStress Expect value | Draft §22: "an un-graded outcome — a test-completeness gap" |
| "-m quick" (flag) | JCStress reduced-pipeline-run flag | Draft §22: "The -m quick flag exists for a reduced pipeline run" |
| "org.openjdk.jcstress:jcstress-core:0.16" (GAV) | JCStress Maven coordinates and version | Draft §22 header: "org.openjdk.jcstress:jcstress-core 0.16 latest" |
| "can prove a bug exists — observe FORBIDDEN = real defect" (Gives) | JCStress proves presence | Draft §22: "the rare kind of test that can prove a bug exists — observe a FORBIDDEN tuple and a real defect is demonstrated" |
| "probabilistic; green run ≠ proof of absence; hardware-dependent; slow" (Cannot) | JCStress limitation | Draft §22: "it is, by its own README, experimental and probabilistic … results are hardware-dependent" |
| "'experimental' per its own README" | JCStress self-description | Draft §22: "It is a design-correctness microscope, not a CI gate" / JCStress README |
| "Layer 3 — Deterministic Reproduction / Regression time (once bug understood)" | Deterministic tools used as regression lock after bug found | Draft §22: "they are best as the regression lock once a bug is found and understood" |
| "CountDownLatch — release all threads at the same instant; maximize overlap" | CountDownLatch use in deterministic concurrency testing | Draft §22: "a CountDownLatch to release all threads at the same instant and maximize overlap" |
| "CyclicBarrier / Phaser — rendezvous at the exact check-then-act window" | CyclicBarrier/Phaser use | Draft §22: "a CyclicBarrier/Phaser to rendezvous threads at the exact check-then-act window" |
| "Single-thread executor — pin a specific ordering" | Single-thread executor use | Draft §22: "a single-thread executor to pin an ordering" |
| "Clock.fixed — make time-dependent logic deterministic" | Clock.fixed use | Draft §22: "an injected Clock.fixed to make time-dependent logic deterministic" |
| "Gives: forces one interleaving; bug becomes a repeatable regression test" | Deterministic tools give | Draft §22: "they force one interleaving … They prove the bug for the schedule engineered" |
| "Cannot: proves only the engineered schedule; says nothing about other interleavings" | Deterministic tools limitation | Draft §22: "say nothing about others, so they are best as the regression lock once a bug is found" |
| "jqwik — property-based; checks operation sequences against a sequential spec; linearizability-style" | jqwik characterisation | Draft §22: "Property-based tools like jqwik check operation sequences against a sequential spec, strong for linearizability-style checks" |
| "Lincheck — model-checks / enumerates interleavings vs. sampling real hardware" | Lincheck characterisation | Draft §22: "Lincheck takes a model-checking approach that enumerates interleavings rather than sampling real hardware" |
| "JLS §17.4.5 'correctly synchronized' / data-race-free (spec property all three approximate)" | The undecidable spec property that all static/dynamic layers approximate | Draft §22: "All three approaches approximate the same spec property (JLS §17.4.5's 'correctly synchronized' / data-race-free)" |
| "Static builds catches a different subset than runtime sampling" (note) | Complementary, not substitutes | Draft §22: "static and dynamic are complementary detection-times, not substitutes" |
| "What none of them catches: arbitrary races, deadlock, liveness" (note) | Honest limitation of all three layers | Draft §22 §"Limitations & when NOT to reach for it" |
| "Anti-pattern to retire: Thread.sleep(100)-timed tests" | Thread.sleep anti-pattern | Draft §22: "Thread.sleep(100)-timed 'concurrency tests' neither force a specific interleaving nor sample the space … Replace them with latches/barriers … or JCStress" |

Source-trace status: YES
Verified against the pinned authority set (00-strategy/SOURCE-PIN.md) at the pins in SOURCE-PIN.md.
Every label, tool name, annotation, GAV coordinate, Expect-value, flag, and limitation in Fig 14.2 traces to the chapter draft §22 (which itself cites JCStress README, Error Prone source, SpotBugs docs, Checker Framework docs, and JLS SE 21 §17.4.5). No atom was invented.
