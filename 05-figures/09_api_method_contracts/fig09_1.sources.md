# fig09_1.sources.md — Source-trace sidecar for Fig 09.1

**Figure:** Fig 09.1 — The two halves of a method contract
**Chapter:** 7 — A Method Is a Promise (dossier key 09, folds 60) · Part II
**HTML source:** `05-figures/09_api_method_contracts/fig09_1.html`
**Rendered artifact:** `05-figures/09_api_method_contracts/fig09_1.png`

---

## Per-label source-trace table

| Label / atom in diagram | Claim asserted | Pinned source (SOURCE-PIN.md) |
|---|---|---|
| Figure title: "The two halves of a method contract" | A method's contract has exactly two halves with different enforcement homes | Draft v1 §"The two halves of a contract": "Every method makes a promise with two parts, and they are enforced in completely different places." |
| Figure subtitle: "Push as much of the contract into the type system as possible — the cheapest feedback on a broken contract is 'it didn't compile'" | The design directive and reason | Draft v1 §"The two halves of a contract" closing: "The one idea worth holding: push as much of the contract into the type system as possible, because the cheapest possible feedback on a broken contract is 'it didn't compile.'" |
| Spectrum label: "The left-shift spectrum — move promises leftward" | The discipline is shifting promises toward the type system | Draft v1 §"The two halves of a contract": "The whole skill is moving promises leftward — from a comment nobody reads, to a runtime check that fails fast, to a type that will not compile when broken." |
| Spectrum segment: "Comment / doc only" | Rightmost (costliest) position on the enforcement spectrum | Draft v1 §"The two halves of a contract" table row: doc/runtime-carried half, Javadoc |
| Spectrum segment: "Fail-fast runtime check" | Middle-right position | Draft v1 §"The two halves of a contract" table: "Cost of a violation: runtime exception — later, costlier" |
| Spectrum segment: "Static analyzer" | Middle-left position | Draft v1 §"Where each rule is enforced" table: column "Analyzer" |
| Spectrum segment: "Type system" | Leftmost (cheapest) position | Draft v1 §"The two halves of a contract" table: "Type-carried … compile error — cheapest" |
| Arrow: "design goal: shift leftward" | The design directive is leftward movement | Draft v1 §"The two halves of a contract": "The whole skill is moving promises leftward" |
| "later / costlier feedback →" | Moving right = later, costlier violation | Draft v1 §"The two halves of a contract" table: "Cost of a violation: runtime exception — later, costlier" |
| Left half heading: "Type-carried half" | Name of the first half | Draft v1 §"The two halves of a contract" table: column header "Half: Type-carried" |
| Left half badge: "compile error — cheapest" | Cost of a type-carried violation | Draft v1 §"The two halves of a contract" table: "Cost of a violation: compile error — cheapest" |
| Left half enforcer: "compiler · static checker · IDE" | What enforces the type-carried half | Draft v1 §"The two halves of a contract" table: "Enforced by: compiler / static checker" |
| Rule 1: "Return Optional<T>, not null (Item 55)" | Item 55 design rule and its mechanism | Draft v1 §"Design the signature, return type, and parameters (Items 51–55)": "Return Optional judiciously (Item 55): Optional<T>, used as a return type only, puts 'a result may be absent' into the type"; *Effective Java* 3e (2018) Item 55, SOURCE-PIN §7 |
| Rule 1 detail: "Never Optional for parameters, fields, or boxed primitives (OptionalInt/OptionalLong/OptionalDouble instead)" | Bloch's named caveats | Draft v1 §"Design the signature…": "Bloch's caveats: never Optional of a boxed primitive (use OptionalInt/OptionalLong/OptionalDouble); never for collection element types (return empty); avoid Optional fields and parameters"; *Effective Java* 3e (2018) Item 55, SOURCE-PIN §7 |
| Rule 2: "Nullness as a signature fact — JSpecify 1.0.0" | JSpecify puts null contract into the signature | Draft v1 §"Encode the contract in the type system": "JSpecify standardizes @Nullable, @NonNull, @NullMarked, @NullUnmarked"; SOURCE-PIN §2: "JSpecify 1.0.0 (stable; @Nullable/@NonNull/@NullMarked/@NullUnmarked)" |
| Rule 2 annotation names: `@Nullable` / `@NonNull` / `@NullMarked` | JSpecify 1.0.0 annotation names | Draft v1 §"Encode the contract in the type system": "JSpecify standardizes @Nullable, @NonNull, @NullMarked (everything in scope is non-null unless marked otherwise), and @NullUnmarked"; JSpecify 1.0.0, SOURCE-PIN §2 |
| Rule 2 detail: "JSpecify is a specification, not a checker" | JSpecify's nature | Draft v1 §"Encode the contract in the type system": "Crucially, JSpecify is a specification, not a checker: it gives a tool-agnostic vocabulary" |
| Rule 2 checker tag: "JSpecify 1.0.0 · NullAway 0.13.4" | Pinned versions of specification and a conforming checker | SOURCE-PIN §2: "JSpecify 1.0.0"; "NullAway 0.13.4 (requires JDK 17 + Error Prone 2.36.0+)" |
| Rule 3: "Minimize accessibility (Items 15–17)" | Items 15–17 design rules | Draft v1 §"Minimize the surface (Items 15–17)": "Effective Java Item 15 — 'Minimize the accessibility of classes and members'"; *Effective Java* 3e (2018) Items 15–17, SOURCE-PIN §7 |
| Rule 3 detail: "Every public / protected member is a promise kept forever (binary-compat burden)" | Accessibility as a permanent promise | Draft v1 §"Minimize the surface (Items 15–17)": "every public or protected member is a promise that must be kept forever (the binary-compatibility burden covered at the end of this chapter)" |
| Rule 4: "Real override — @Override" | @Override annotation and its enforcement | Draft v1 §"Encode the contract in the type system": "Real overrides (@Override). Error Prone MissingOverride flags a method that overrides a supertype method without the annotation; with it, the compiler verifies the override is genuine, catching signature drift." |
| Rule 4 checker tag: "EP MissingOverride" | Error Prone rule ID | Draft v1 §"Encode the contract in the type system": "Error Prone MissingOverride"; Source-PIN §2: Error Prone |
| Rule 5: "Un-ignorable return values — @CheckReturnValue" | CheckReturnValue pattern | Draft v1 §"Encode the contract in the type system": "When a return value is the result (a check, a new immutable value), dropping it is a bug. Error Prone CheckReturnValue (error) makes the result un-ignorable" |
| Rule 5 detail: "@CanIgnoreReturnValue exempts builders that return this" | The exemption annotation | Draft v1 §"Encode the contract in the type system": "CanIgnoreReturnValue exempts builders that return this" |
| Rule 5 checker tags: "EP CheckReturnValue · SpotBugs RV_RETURN_VALUE_IGNORED · Sonar java:S2201" | Three tool rule IDs covering the same ground | Draft v1 §"Encode the contract in the type system": "Error Prone CheckReturnValue (error) … SpotBugs RV_RETURN_VALUE_IGNORED and Sonar java:S2201"; SOURCE-PIN §2 |
| Rule 6: "No parameter reassignment — final params" | final parameters as type constraint | Draft v1 §"Encode the contract in the type system": "PMD AvoidReassigningParameters and Sonar java:S1226 flag overwriting a parameter before reading it; final parameters make it a compile constraint" |
| Rule 6 checker tags: "PMD AvoidReassigningParameters · Sonar java:S1226" | PMD and Sonar rule IDs | Draft v1 §"Encode the contract in the type system": same sentence; SOURCE-PIN §2: PMD 7.25.0, SonarQube 2026.1 LTA |
| Right half heading: "Doc / runtime-carried half" | Name of the second half | Draft v1 §"The two halves of a contract" table: column header "Half: Doc/runtime-carried" |
| Right half badge: "runtime exception — later, costlier" | Cost of a doc/runtime violation | Draft v1 §"The two halves of a contract" table: "Cost of a violation: runtime exception — later, costlier" |
| Right half enforcer: "Javadoc (states it) · fail-fast check (enforces it)" | What enforces the doc/runtime half | Draft v1 §"The two halves of a contract" table: "Enforced by: Javadoc + fail-fast runtime check" |
| Rule A: "Fail fast on bad input (Item 49)" | Item 49 design rule | Draft v1 §"Fail fast on bad input (Item 49)": "check parameters at the top of the method and throwing immediately"; *Effective Java* 3e (2018) Item 49, SOURCE-PIN §7 |
| Rule A detail: "Converts a wrong result far away into a clear exception at the call site" | Stated consequence of fail-fast | Draft v1 §"Fail fast on bad input (Item 49)": "This converts a wrong result far away into a clear exception at the call site." |
| `requireNonNull(T)` / `(T, String)` / `(T, Supplier<String>)` — since 1.7 / 1.7 / 1.8 | java.util.Objects method signatures and since-versions | Draft v1 §"Fail fast on bad input (Item 49)" table: "requireNonNull(T) / (T, String) / (T, Supplier<String>) — Since: 1.7 / 1.7 / 1.8"; "Signatures verified against the JDK 21 API"; SOURCE-PIN §1: JDK 21.0.11 |
| `checkIndex(int, int)` — since 9; `checkIndex(long, long)` — since 16 | java.util.Objects checkIndex signatures and since-versions | Draft v1 §"Fail fast on bad input (Item 49)" table: "checkIndex(int, int) (and long overload, since 16) — Since: 9 / 16"; SOURCE-PIN §1: JDK 21.0.11 |
| `checkFromToIndex` / `checkFromIndexSize` — since 9 | java.util.Objects method signatures and since-versions | Draft v1 §"Fail fast on bad input (Item 49)" table: "checkFromToIndex / checkFromIndexSize — Since: 9"; SOURCE-PIN §1: JDK 21.0.11 |
| Rule B: "Document what the type cannot carry (Item 56)" | Item 56 design rule | Draft v1 §"Document the part types cannot carry (Item 56)": "@param per parameter (its preconditions), @return for every non-void method, @throws for each exception with its triggering condition, and a first sentence that stands alone as the summary"; *Effective Java* 3e (2018) Item 56, SOURCE-PIN §7 |
| Rule B detail: "@param / @return / @throws / @implSpec" | Named Javadoc tags | Draft v1 §"Document the part types cannot carry (Item 56)": "@param … @return … @throws … @implSpec documents the contract a subclass may rely on" |
| Rule C: "Defend against representation exposure (Item 50)" | Item 50 design rule | Draft v1 §"Do not leak the representation (Item 50)": "copy it on the way in and on the way out"; *Effective Java* 3e (2018) Item 50, SOURCE-PIN §7 |
| Rule C detail: "When the stored type is already immutable, skip the copy" | Named caveat for defensive copy | Draft v1 §"Limitations": "When the stored type is already immutable, copies are pure overhead; reach for immutability instead." |
| Rule C checker tags: "SpotBugs EI_EXPOSE_REP / EI_EXPOSE_REP2 · PMD MethodReturnsInternalArray / ArrayIsStoredDirectly" | Tool rule IDs for representation exposure | Draft v1 §"Do not leak the representation (Item 50)": "SpotBugs EI_EXPOSE_REP (a getter returns a mutable internal field) and EI_EXPOSE_REP2 … PMD MethodReturnsInternalArray and ArrayIsStoredDirectly"; SOURCE-PIN §2: SpotBugs 4.10.2, PMD 7.25.0 |
| Rule D: "Exception conventions" — NullPointerException / IndexOutOfBoundsException / IllegalArgumentException / IllegalStateException | Named exception types and their triggering conditions | Draft v1 §"Fail fast on bad input (Item 49)": "NullPointerException for null, IndexOutOfBoundsException for a bad index, IllegalArgumentException for other bad values, IllegalStateException for bad object state" |
| Rule E: "Return empty, not null — collections & arrays (Item 54)" | Item 54 design rule | Draft v1 §"Design the signature…": "Return empty, not null (Item 54): a method that can return 'no elements' returns an empty collection or array, never null, so callers need no guard"; *Effective Java* 3e (2018) Item 54, SOURCE-PIN §7 |
| Rule E detail: "(Distinct from Optional, which is for a single absent value.)" | Distinction between Item 54 and Item 55 | Draft v1 §"Design the signature…": Item 54 and 55 are presented as adjacent but distinct |
| Cost cell (left): "Type-carried violation → compile error" | Nature and cost of a type-carried violation | Draft v1 §"The two halves of a contract" table: "Cost of a violation: compile error — cheapest" |
| Cost cell (right): "Doc/runtime-carried violation → runtime exception" | Nature and cost of a doc/runtime violation | Draft v1 §"The two halves of a contract" table: "Cost of a violation: runtime exception — later, costlier" |
| Cost cell right detail: "Fail-fast (top of method) moves the exception earlier" | Effect of fail-fast on runtime-violation cost | Draft v1 §"Fail fast on bad input (Item 49)": "This converts a wrong result far away into a clear exception at the call site." |
| Version note: "japicmp 0.25.6 or revapi 0.15.1 in CI to compute the required semver bump" | Pinned tool versions and their function | Draft v1 §"Deep dive: the contract across versions": "japicmp … revapi"; SOURCE-PIN §6: "revapi 0.15.1 / japicmp 0.25.6" |
| Version note: "MAJOR.MINOR.PATCH" | Semver scheme | Draft v1 §"Deep dive: the contract across versions": "Semantic versioning is the contract that communicates change: MAJOR.MINOR.PATCH"; semver.org, SOURCE-PIN §6 |
| Version note: "@Deprecated(forRemoval=true, since="…")" | Deprecation-with-removal annotation syntax | Draft v1 §"Deep dive: the contract across versions": "@Deprecated(forRemoval=true, since=...) with a migration path rather than deletion"; JDK 21, SOURCE-PIN §1 |
| Version note: "binary-incompatible break (JLS ch. 13)" | JLS chapter reference for binary compatibility | Draft v1 §"Deep dive: the contract across versions": "The JLS (ch. 13) defines binary compatibility precisely"; JLS SE 21 ch. 13, SOURCE-PIN §1 |
| Caption tool versions: SpotBugs 4.10.2 / PMD 7.25.0 / Sonar 2026.1 LTA / NullAway 0.13.4 / JSpecify 1.0.0 / japicmp 0.25.6 / revapi 0.15.1 | Pinned tool versions used in analyzer tag labels | SOURCE-PIN §2: SpotBugs 4.10.2, PMD 7.25.0, SonarQube Server 2026.1 LTA, NullAway 0.13.4, JSpecify 1.0.0; SOURCE-PIN §6: japicmp 0.25.6, revapi 0.15.1 |
| Caption: "JDK 21" / "since 9" / "since 16" / "since 1.7" / "since 1.8" | JDK since-versions for java.util.Objects methods | Draft v1 §"Fail fast on bad input (Item 49)" table; SOURCE-PIN §1: JDK 21.0.11 |

---

## Source-trace status

**Source-trace status: YES**

Every label, arrow, atom, and number in Fig 09.1 traces to either:
- The pinned chapter draft `03-drafts/09_api_method_contracts/09_api_method_contracts_v1.md` (which itself cites the authority set and carries its own source verification header), or
- A named pinned authority in SOURCE-PIN.md:
  - *Effective Java* 3e (Bloch, 2018) — SOURCE-PIN §7
  - JDK 21.0.11 (`java.util.Objects` signatures; JLS SE 21 ch. 13) — SOURCE-PIN §1
  - JSpecify 1.0.0 — SOURCE-PIN §2
  - NullAway 0.13.4 — SOURCE-PIN §2
  - Error Prone (2.x line) — SOURCE-PIN §2
  - SpotBugs 4.10.2 — SOURCE-PIN §2
  - PMD 7.25.0 — SOURCE-PIN §2
  - SonarQube 2026.1 LTA — SOURCE-PIN §2
  - japicmp 0.25.6 — SOURCE-PIN §6
  - revapi 0.15.1 — SOURCE-PIN §6
  - semver.org — SOURCE-PIN §6

No label, rule ID, API signature, version number, or tool flag was invented. All since-versions for `java.util.Objects` methods match the draft's source-verified table. All tool rule IDs (EP MissingOverride, EP CheckReturnValue, EP CanIgnoreReturnValue, SpotBugs EI_EXPOSE_REP, EI_EXPOSE_REP2, RV_RETURN_VALUE_IGNORED, PMD AvoidReassigningParameters, MethodReturnsInternalArray, ArrayIsStoredDirectly, Sonar java:S2201, java:S1226) match the draft text which carried its own verify-at-pin flags for exact severities. The diagram depicts the two-halves framing the draft is built around — no diagram-specific claims were added beyond the draft.

**Verified against the pinned authority set (00-strategy/SOURCE-PIN.md) at the pins in SOURCE-PIN.md — 2026-06-20.**
