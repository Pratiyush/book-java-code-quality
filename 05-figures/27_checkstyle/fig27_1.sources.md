# Figure source-trace — Fig 16.1 The detection-time axis (Ch 16)

- Files: `fig27_1.html` → `fig27_1.png`. Family: Architecture / layering. Method: HTML → render → crop.
- One idea: Each analyzer's vantage point in the build pipeline (source text → source AST → type-attributed AST inside javac → emitted bytecode) determines what it can and cannot see, making the four tools complementary by construction.
- Verified against the pinned authority set (00-strategy/SOURCE-PIN.md) at the pins in SOURCE-PIN.md.

| Label in diagram | Claim | Pinned source (SOURCE-PIN.md) |
|---|---|---|
| Pipeline stage "Source text / .java file as written" | First stage of the Java build; Checkstyle reads here | Draft v1 §How it works organizing table row 1: "Reads: source text → AST (one file)" |
| Pipeline stage "Source AST / build step (before compile)" | Checkstyle and PMD read the source AST before compilation | Draft v1 §How it works table rows 1–2; §Checkstyle: "parses each source file to an AST" |
| Pipeline stage "Type-attributed AST / inside javac (during compile)" | Error Prone hooks the compiler and reads the type-attributed AST during compilation | Draft v1 §How it works table row 4: "type-attributed AST inside `javac`"; §Error Prone: "runs as a `javac` plugin, examining the type-attributed AST during compilation" |
| Pipeline stage "Emitted bytecode / .class files (after compile)" | SpotBugs analyzes compiled bytecode after javac completes | Draft v1 §How it works table row 3: "compiled bytecode"; §SpotBugs: "its defining choice is what it reads: compiled `.class` bytecode, in a separate pass after `javac`" |
| "Checkstyle / 13.6.0" | Tool identity and pinned version | SOURCE-PIN.md §2: "Checkstyle 13.6.0 (2026-06)" |
| Checkstyle "Has types? No" | Checkstyle has no type information | Draft v1 §How it works table: "no (single-file, no type/inheritance)"; Checkstyle docs verbatim in draft v1: "You cannot determine the type of an expression" |
| Checkstyle "Reads: Source text → AST (one file; build step)" | Checkstyle parses one source file at a time | Draft v1 §Checkstyle: "parses each source file to an AST"; §How it works table: "source text → AST (one file)" |
| Checkstyle reach: "Style, naming, layout, Javadoc presence, size limits" | Checkstyle's characteristic scope | Draft v1 §How it works table: "style, naming, layout, Javadoc, size"; §Checkstyle: "style and convention layer — naming, layout, import hygiene, Javadoc presence, and size limits" |
| Checkstyle blind spot: "cannot determine the type of an expression" | Verbatim from Checkstyle docs | Draft v1 §Checkstyle (verbatim quote): "You cannot determine the type of an expression … You cannot determine the full inheritance hierarchy of type" — checkstyle.org pinned 13.6.0 |
| Checkstyle examples: `LineLength`, `ConstantName`, `JavadocMethod` | Named Checkstyle Check modules | Draft v1 §Checkstyle: "Check submodules (`LineLength`, `ConstantName`, `JavadocMethod`, …)" |
| "PMD + CPD / 7.25.0" | Tool identity and pinned version | SOURCE-PIN.md §2: "PMD (+ CPD) 7.25.0" |
| PMD "Has types? Partial" | PMD has partial type information | Draft v1 §How it works table: "partial" |
| PMD reach: "Configurable rule catalogue (8 categories); complexity metrics; CPD token-based copy-paste detection" | PMD's distinctive scope and CPD capability | Draft v1 §PMD: "eight categories (`bestpractices`, `codestyle`, `design`, `documentation`, `errorprone`, `multithreading`, `performance`, `security`)"; "CPD … tokenizes source and runs Karp-Rabin matching" |
| PMD blind spot: "Whole-program flow; semantic (non-textual) duplication" | PMD's characteristic limitation | Draft v1 §How it works table: "whole-program flow; semantic (non-textual) duplication"; §PMD CONCEPT: "CPD finds textual clones, never semantic ones" |
| PMD examples: `CyclomaticComplexity`, `GodClass`, `CPD --minimum-tokens` | Named PMD metric rules and CPD flag | Draft v1 §PMD: "metric rules (`CyclomaticComplexity`, `NPathComplexity`, `GodClass`…)"; CPD: "`--minimum-tokens` … required with no engine default" |
| "Error Prone / 2.x (JDK 21+)" | Tool identity and JDK requirement | SOURCE-PIN.md §2: "Error Prone (+ Refaster) latest 2.x (requires JDK 21+)"; draft v1 §Error Prone: "must run on JDK 21 or newer" |
| Error Prone "Has types? Yes (full)" | Error Prone has full compiler type information | Draft v1 §How it works table: "yes (full)"; §Error Prone: "has the compiler's full type information" |
| Error Prone "Reads: Type-attributed AST inside javac (during compile)" | Error Prone runs as a javac plugin | Draft v1 §Error Prone: "runs as a `javac` plugin, examining the type-attributed AST during compilation" |
| Error Prone reach: "Type-aware bug patterns as build-failing errors; automated fixes; Refaster rewrites" | Error Prone's distinctive capability | Draft v1 §Error Prone: "many checks are ON_BY_DEFAULT ERROR, so a violation fails the build"; "many checks ship a suggested fix"; "Refaster lets a team author a rewrite" |
| Error Prone blind spot: "Runs on every compile; requires JDK 21+; build-failing defaults need on-ramp on legacy code" | Error Prone's characteristic cost | Draft v1 §Error Prone limitations: "runs on every compile, couples the build to the JDK"; "its build-failing defaults can flood a legacy codebase until ramped with `-XepAllErrorsAsWarnings` or patch mode" |
| Error Prone examples: `CollectionIncompatibleType`, `DeadException`, `ReturnValueIgnored` | Named ON_BY_DEFAULT ERROR checks | Draft v1 §Error Prone: "`CollectionIncompatibleType` … `DeadException` … `ReturnValueIgnored` (ERROR)" |
| "SpotBugs / 4.10.2 + FindSecBugs 1.14.0" | Tool identity and pinned versions | SOURCE-PIN.md §2: "SpotBugs (+ FindSecBugs, fb-contrib) 4.10.2"; draft v1 back matter: "FindSecBugs 1.14.0 (144 patterns / 826 signatures)" |
| SpotBugs "Has types? Yes (bytecode)" | SpotBugs has type information from bytecode | Draft v1 §How it works table: "yes (from bytecode)" |
| SpotBugs reach: "Bug patterns in compiled output: impossible casts, null-on-path, volatile misuse, security signatures" | SpotBugs and FindSecBugs capability | Draft v1 §SpotBugs: "`EC_UNRELATED_TYPES`, `BC_IMPOSSIBLE_CAST`, `VO_VOLATILE_INCREMENT`, `NP_NULL_ON_SOME_PATH`"; FindSecBugs: "SQL injection, weak crypto, `PREDICTABLE_RANDOM`" |
| SpotBugs blind spot: "Formatting/comments gone by bytecode; source-distance in messages; heuristic detectors" | SpotBugs limitation of the bytecode vantage point | Draft v1 §SpotBugs limitations: "SpotBugs cannot see formatting or comments (they are gone by bytecode), and its messages sit one step from the source. Several detectors are heuristic and say so." |
| SpotBugs examples: `EC_UNRELATED_TYPES`, `NP_NULL_ON_SOME_PATH`, `EI_EXPOSE_REP` | Named SpotBugs bug patterns | Draft v1 §SpotBugs: "`EC_UNRELATED_TYPES` … `NP_NULL_ON_SOME_PATH` … `EI_EXPOSE_REP`" |
| Axis-rule note verbatim quote: "cannot determine the type of an expression" | Verbatim from Checkstyle docs | Draft v1 §Checkstyle (verbatim): "You cannot determine the type of an expression" — checkstyle.org pinned 13.6.0 |
| Caption: Checkstyle verbatim quote block | Multi-sentence verbatim from Checkstyle docs | Draft v1 §Checkstyle back matter verbatim: checkstyle.org pinned 13.6.0, 2026-06-20 |
| Caption: Error Prone "must be run on JDK 21 or newer" | Verbatim from Error Prone docs | Draft v1 §Error Prone: "its own docs state it 'must be run on JDK 21 or newer'" — errorprone.info |
| Caption: FindSecBugs "144 patterns / 826 signatures" | Quoted figure from FindSecBugs docs | Draft v1 back matter: "FindSecBugs 1.14.0 (144 patterns / 826 signatures)" — spotbugs.github.io |
| Caption: PMD "8 categories" | PMD category count | Draft v1 §PMD: "grouped into eight categories" — pmd.github.io pinned 7.25.0 |
| "No tool is crowned" | Neutrality statement | 00-strategy/NEUTRALITY.md; GUIDELINES §2 rule 1 |

---

Source-trace status: YES

Verified against the pinned authority set (00-strategy/SOURCE-PIN.md) at the pins in SOURCE-PIN.md.
Every label, tool name, version, rule ID, verbatim quote, and category count in Fig 16.1 traces to an
explicit passage in `03-drafts/27_checkstyle/27_checkstyle_v1.md` (draft v1 §How it works organizing
table and per-tool sections) or the pinned source rows in `00-strategy/SOURCE-PIN.md` §2
(Checkstyle 13.6.0, PMD 7.25.0, SpotBugs 4.10.2, Error Prone 2.x). No atom was introduced by
the figure that is not present in the draft. The "cannot determine the type" and "must be run on
JDK 21 or newer" quotes are marked as verbatim in the draft and sourced to each tool's own
pinned official documentation.

## Accessibility

- Alt-text: Pipeline rail of four build stages over a table placing Checkstyle, PMD, Error Prone, SpotBugs at where each reads the program.
- Long-description: The figure is titled "Fig 16.1 — The detection-time axis: where each analyzer reads the program," subtitled that four tools have four vantage points and where a tool stands determines what it can see. It has two sections. The top section is a horizontal pipeline with four boxes connected by rightward arrows. From left to right: "Source text (.java file as written)" → "Source AST (build step, before compile)" → "Type-attributed AST (inside javac, during compile)" — this box is shaded darker — → "Emitted bytecode (.class files, after compile)". Below the pipeline is a six-column table with columns: Tool, Reads at, Has types?, Distinctive reach, Characteristic blind spot, Example rules/patterns. Row 1 — Checkstyle 13.6.0: reads Source text → AST (one file, build step); Has types: No; Reach: style, naming, layout, Javadoc presence, size limits; Blind spot: anything needing types, flow, or cross-file facts — "cannot determine the type of an expression"; Examples: LineLength, ConstantName, JavadocMethod. Row 2 — PMD+CPD 7.25.0: reads Source → AST + metrics (build step); Has types: Partial; Reach: configurable rule catalogue (8 categories), complexity metrics, CPD token-based copy-paste detection; Blind spot: whole-program flow, semantic (non-textual) duplication; Examples: CyclomaticComplexity, GodClass, CPD --minimum-tokens. Row 3 — Error Prone 2.x (JDK 21+): reads Type-attributed AST inside javac (during compile); Has types: Yes (full); Reach: type-aware bug patterns as build-failing errors, automated fixes, Refaster rewrites; Blind spot: runs on every compile, requires JDK 21+, build-failing defaults need on-ramp on legacy code; Examples: CollectionIncompatibleType, DeadException, ReturnValueIgnored. This row has a warm left-border accent matching the highlighted pipeline stage. Row 4 — SpotBugs 4.10.2 + FindSecBugs 1.14.0: reads Compiled bytecode (.class files, after compile); Has types: Yes (bytecode); Reach: bug patterns in compiled output — impossible casts, null-on-path, volatile misuse, security signatures; Blind spot: formatting/comments gone by bytecode, source-distance in messages, heuristic detectors; Examples: EC_UNRELATED_TYPES, NP_NULL_ON_SOME_PATH, EI_EXPOSE_REP. Below the table is a callout note stating the axis rule: a tool can only catch what its vantage point lets it see, the four tools are complementary by construction, and none stands smarter than the others — each stands somewhere different. A legend at the bottom distinguishes the Error Prone row (warm accent, runs inside javac) from the other three (a separate build step) and explains the Yes / No / Partial type-information badges. A source line closes the figure.
- Grayscale/contrast/legibility note: Grayscale-safe. In the pipeline rail the "Type-attributed AST" stage is set apart by a darker tint and a heavier border weight, not by hue, and it is restated in text. The "Has types?" column is the one place color is used (a red "No", an amber "Partial", green "Yes" badges), but each badge carries its word — "No", "Partial", "Yes (full)", "Yes (bytecode)" — so the answer reads in black-and-white, and the legend spells the mapping out. The single warm accent marks only the Error Prone row and its matching rail stage; it is redundant with the row's tool name, its "inside javac" text, and its fixed position, so it survives grayscale. Rows are also separated by horizontal rules. Dark text on light tints reads at print size; every tool name, version, rule ID, and bug-pattern name is authored selectable text, understandable without syntax color. The figure is a wide six-column table — a NOTE to the figure-designer, not a contrast failure: confirm the rendered PNG keeps the monospace example-rule column legible at the final printed page width.
