# Fig 55.1 sources — Architecture-enforcement spectrum

Chapter 26 (dossier key 55, folds 33+56) · draft v1 · `03-drafts/55_enforcing_architecture_fitness_functions/55_enforcing_architecture_fitness_functions_v1.md`

| Label / element in diagram | Claim / atom | Pinned source path |
|---|---|---|
| Title "Fig 55.1 — Architecture-enforcement spectrum" | Identifying label | Draft v1 §"Enforcement: from documented to executable" — enforcement-spectrum table (lines ~51–58); placement before §ArchUnit |
| Subtitle "Convention → ArchUnit rule → JPMS compiler boundary: enforcement strength rises with adoption cost. Choice is a trade-off, not an upgrade path." | Framing | Draft v1 line ~51: "a spectrum of increasing strength and cost"; dossier line 7: "Enforcement spectrum (strength × cost): convention → ArchUnit test → JPMS compiler boundary"; trade-off-not-upgrade per §"deep dive" line ~105 |
| Level 0 "Naming convention — agreed in the wiki" / "enforced by: discipline (nobody)" | Mechanism + enforcer | Draft v1 enforcement-spectrum table line ~55: "Naming convention | discipline | none (documentation) | free, and ignored"; line ~49: "A decision written in a wiki…is invisible to the compiler and the build" |
| Level 0 badges "strength: none" / "cost: free" | Strength + cost | Draft v1 line ~55: "none (documentation) | free, and ignored" |
| Level 0 "what holds it: Team memory and good intentions; shared agreement recorded in a wiki or ADR." | Detail | Draft v1 line ~49: "A decision written in a wiki ('the domain layer must not depend on the web layer')"; line ~23: "the architecture lived in a wiki diagram with no teeth" |
| Level 0 "honest limit: Invisible to the compiler and the build — a single import under deadline pressure violates it silently; the architecture erodes one import at a time." | Honest limit | Draft v1 line ~49: "a single import added under pressure violates it silently and nothing notices"; line ~23: "It eroded one deadline-pressured import at a time" |
| Connector "make the decision executable → test phase" | Transition label | Draft v1 line ~49: "**Enforcement** closes that gap: it turns the architectural decision into an *executable rule* that fails the build on violation" |
| Level 1 "ArchUnit rule — architecture as a JUnit test" / "enforced by: JUnit / CI (test phase)" | Mechanism + enforcer | Draft v1 line ~56: "**ArchUnit rule** | a JUnit test in CI | fails the build on violation | a test dependency; post-compile"; line ~64: "assert architecture facts as plain-Java JUnit tests" |
| Level 1 badges "strength: fails the build on violation" / "cost: one test dependency; post-compile" | Strength + cost | Draft v1 line ~56 (table row); line ~85: "runs post-compile" |
| Level 1 "what it catches: Layer-direction violations (domain → web import); cycles (slices().beFreeOfCycles()); naming, annotation constraints, coding rules (NO_CLASSES_SHOULD_ACCESS_STANDARD_STREAMS)." | Detail | Draft v1 lines ~78–81: layeredArchitecture, `slices().matching(..).should().beFreeOfCycles()`, `GeneralCodingRules` constants like `NO_CLASSES_SHOULD_ACCESS_STANDARD_STREAMS`; dossier line 8 |
| `domain → web` (Level 1 catch) | Layer-direction example | Draft v1 §"deep dive" line ~101: "the domain layer must not depend on the web layer"; line ~71–73 code block (`..domain..` should not depend on `..web..`) |
| `slices().beFreeOfCycles()` (Level 1 catch) | ArchUnit DSL identifier | Draft v1 line ~80: "`slices().matching('..storefront.(*)..').should().beFreeOfCycles()`"; line ~135; `00-strategy/SOURCE-PIN.md` §2 ArchUnit 1.4.2 (archunit.org) |
| `NO_CLASSES_SHOULD_ACCESS_STANDARD_STREAMS` (Level 1 catch) | GeneralCodingRules constant | Draft v1 line ~81: "`GeneralCodingRules` constants like `NO_CLASSES_SHOULD_ACCESS_STANDARD_STREAMS` (no `System.out`)"; back-matter line ~148; `00-strategy/SOURCE-PIN.md` §2 ArchUnit 1.4.2 (constant set ⚠ @pin per dossier line 10) |
| Level 1 "honest limit: Bytecode-only: dependencies via reflection, Class.forName, DI wiring, or ServiceLoader are not edges ArchUnit sees — a routed violation passes. Only catches what is encoded; runs post-compile (slower than the compiler)." | Honest limit | Draft v1 line ~85: "Because it reads **bytecode**, dependencies expressed *outside* bytecode — reflection (`Class.forName`), string-based DI wiring, classpath scanning, `ServiceLoader` — are not edges ArchUnit sees, so a layering violation routed through reflection passes the rule…It **only catches what is encoded**"; dossier line 8 |
| Level 1 API block: `@AnalyzeClasses(packages = "org.acme.storefront", importOptions = DoNotIncludeTests.class)` / `noClasses().that().resideInAPackage("..domain..")` / `.should().dependOnClassesThat().resideInAPackage("..web..")` / `// FreezingArchRule.freeze(rule) → ratchet for legacy codebases` | ArchUnit DSL / API | Draft v1 code block lines ~66–74 (`@AnalyzeClasses(packages = "org.acme.storefront", importOptions = DoNotIncludeTests.class)`, `noClasses().that().resideInAPackage("..domain..").should().dependOnClassesThat().resideInAPackage("..web..")`); FreezingArchRule line ~83; `00-strategy/SOURCE-PIN.md` §2 ArchUnit 1.4.2 |
| `org.acme.storefront` (package in API block + JPMS block) | Companion-module package | Draft v1 line ~67 (`@AnalyzeClasses(packages = "org.acme.storefront"`); companion-module spec line ~153 (`org.acme.storefront` domain in layered packages); dossier line 8 |
| `DoNotIncludeTests` / `@AnalyzeClasses` (API block) | ArchUnit import-option / annotation | Draft v1 line ~67 + ~76: "driven by `@AnalyzeClasses` + `@ArchTest`"; back-matter line ~148: "`ImportOption.Predefined.DO_NOT_INCLUDE_TESTS/JARS`"; `00-strategy/SOURCE-PIN.md` §2 ArchUnit 1.4.2 |
| Connector "move boundary into the compiler → compile + runtime" | Transition label | Draft v1 line ~87: "Where ArchUnit checks a boundary in a *test*, the **Java Platform Module System** (JPMS, since Java 9) checks it in the *compiler*" |
| Level 2 "JPMS module-info.java — compiler-enforced boundary" / "enforced by: javac + runtime (Java 9+, JEP 261)" | Mechanism + enforcer | Draft v1 line ~57: "**JPMS `module-info`** | the compiler + runtime | `javac` itself forbids the access | a real modularization migration"; line ~89: "`javac` and the runtime *enforce* it"; back-matter line ~149: "JPMS (JEP 261, Java 9)" |
| "JEP 261" / "Java 9+" (Level 2) | JEP + version identifier | Draft v1 back-matter line ~149: "**JPMS** (JEP 261, Java 9; ⚠ confirm @pin)"; `00-strategy/SOURCE-PIN.md` §1 JEP index (confirm JEP # at use — ⚠ @pin) |
| Level 2 badges "strength: javac refuses to compile the violation" / "cost: real modularization migration" | Strength + cost | Draft v1 line ~57 (table); line ~89: "code in another module cannot access a package that was not exported, full stop…it comes at a real cost: modularizing a legacy application…is a genuine migration" |
| Level 2 "what it catches: Any access to a package not declared exports in the module-info.java fails javac and the runtime — structural, not after-the-fact. Strong encapsulation at the module level." | Detail | Draft v1 line ~89: "A `module-info.java` declares `exports`…and `javac` and the runtime *enforce* it: code in another module cannot access a package that was not exported, full stop. That is a stronger guarantee than a test, because it is structural rather than after-the-fact" |
| Level 2 "honest limit: Modularizing a legacy application with non-modular dependencies is a real migration; many teams rationally stay on the classpath. Coarser-grained than ArchUnit (module boundary, not package/annotation); ArchUnit for fine-grained rules, JPMS for coarse boundaries that warrant the cost." | Honest limit | Draft v1 line ~89: "modularizing a legacy application, especially with non-modular dependencies, is a genuine migration, and many teams rationally stay on the classpath and use ArchUnit for finer-grained rules. JPMS is the right lever for…coarse module boundaries; ArchUnit is the right lever for finer package/annotation rules" |
| Level 2 API block: `module org.acme.storefront.domain { exports org.acme.storefront.domain.model; // explicit; web pkg not exported / requires org.acme.storefront.persistence; }` | JPMS module-info syntax | Draft v1 line ~89 (`module-info.java` declares `exports`…and `requires`); §"deep dive" line ~103 ("the domain module does not `require` the web module"); `00-strategy/SOURCE-PIN.md` §1 JEP index (⚠ exports/requires semantics @pin) |
| Trade-off axis "↑ strength" / "↑ cost" ramps (Level 0/1/2) | Strength × cost claim | Draft v1 line ~51: "a spectrum of increasing strength and cost"; dossier line 7: "(strength × cost)" |
| Axis note "Level 0: free & ignored. / Level 1: one dependency; runs with CI. / Level 2: structural; migration investment." | Per-level summary | Draft v1 enforcement-spectrum table (lines ~55–57) |
| Axis note "A team may run both Level 1 (fine-grained rules) and Level 2 (coarse module boundaries)." | Neutrality / both-not-upgrade | Draft v1 line ~89: "A team may run both. Neither is crowned" |
| When-to-use "Stop a documented architecture from eroding: Level 1 — ArchUnit rule in CI." | Guidance | Draft v1 §"When to use what" line ~133: "To stop documented architecture from eroding: make it executable — an ArchUnit rule in CI, not a wiki diagram" |
| When-to-use "Cardinal-sin check (no cycles): slices().beFreeOfCycles() — cheapest, enable first." | Guidance | Draft v1 §"When to use what" line ~135: "For the cardinal-sin check (no cycles): `slices().should().beFreeOfCycles()` — cheap, high-value, enable it first" |
| When-to-use "Legacy codebase adoption: FreezingArchRule.freeze(rule) then drive baseline down." | Guidance | Draft v1 §"When to use what" line ~136: "To adopt a rule on a legacy codebase: `FreezingArchRule.freeze(...)`, then drive the baseline down"; CONCEPT line ~83 |
| `FreezingArchRule.freeze(rule)` / `ViolationStore` | ArchUnit API identifiers | Draft v1 line ~83: "`FreezingArchRule.freeze(rule)` records all *current* violations to a `ViolationStore`; subsequent runs report **only new** ones"; back-matter line ~148; `00-strategy/SOURCE-PIN.md` §2 ArchUnit 1.4.2 |
| When-to-use "Coarse structural encapsulation that warrants migration cost: Level 2 — JPMS." | Guidance | Draft v1 §"When to use what" line ~137: "For compiler-enforced coarse boundaries that warrant the migration cost: JPMS modules" |
| When-to-use "What cannot be expressed as a rule: code review & architectural judgment." | Guidance | Draft v1 §"When to use what" line ~139: "For what cannot be expressed as a rule: code review and architectural judgment — do not force taste into a gate" |
| Alternatives "jQAssistant — Cypher queries over a Neo4j graph of the codebase; suited to ad-hoc exploration." | Named alternative (neutral) | Draft v1 line ~60 + line ~124: "**jQAssistant** — scans the codebase into a Neo4j graph and enforces rules as Cypher queries; suited to ad-hoc graph exploration"; dossier line 7 |
| Alternatives "Spring Modulith — module verification for Spring apps." | Named alternative (neutral) | Draft v1 line ~60 + line ~126: "Spring Modulith (module verification for Spring apps)"; dossier line 7 |
| Alternatives "Deptective — compiler plugin." | Named alternative (neutral) | Draft v1 line ~60: "Deptective (a compiler plugin)"; dossier line 7 |
| Alternatives "JDepend — afferent/efferent coupling metrics; measurement, not a gate." | Named alternative (neutral) | Draft v1 line ~125: "**JDepend** — computes package dependency metrics (afferent/efferent coupling, instability) rather than pass/fail rules; a measurement complement, not a gate" |
| Alternatives "Each serves its niche; none crowned." | Neutrality marker | Draft v1 line ~36: "different approaches to dependency governance, none crowned"; line ~60: "none crowned" |
| FF strip title "The fitness-function frame — these mechanisms, and every gate in this book, are the same thing" | Unifying-frame claim | Draft v1 §"Fitness functions: the frame that unifies the gates" (lines ~91–97); CONCEPT line ~95: "the whole governance layer unifies" |
| FF strip quote "Ford / Parsons / Kua / Sadalage, Building Evolutionary Architectures: 'any mechanism that provides an objective integrity assessment of some architectural characteristic(s)' — run on every build or deploy to protect properties as the system evolves." | Definition (attributed) | Draft v1 line ~93: "*Building Evolutionary Architectures* (Ford, Parsons, Kua, Sadalage) defines an **architectural fitness function** as 'any mechanism that provides an objective integrity assessment of some architectural characteristic(s)'…automated checks that run on every build or deploy"; back-matter line ~150 (⚠ §7 canon row, definition verbatim @pin) |
| FF cell "ArchUnit cycle rule — atomic · triggered · static — guards acyclicity (Ch 26)" | Fitness-function instance | Draft v1 line ~93: "*atomic*…*triggered*…*static* (ArchUnit, metrics)"; line ~95: "an ArchUnit cycle rule"; this chapter = Ch 26 (header line 2); `00-strategy/SOURCE-PIN.md` §2 ArchUnit 1.4.2 |
| FF cell "Coverage / mutation gate — atomic · triggered · static — guards test reach (Ch 23)" | Fitness-function instance | Draft v1 line ~95: "a coverage or mutation gate (Chapter 23)"; routing line ~151: "coverage/mutation gates → Ch 23 (47/48)" |
| FF cell "JMH perf-regression check — atomic · triggered · dynamic — guards latency budget (Ch 44)" | Fitness-function instance | Draft v1 line ~95: "a JMH performance-regression check"; line ~93: "*dynamic* (latency, throughput)"; routing line ~151: "perf gates → JMH (51/105)" — ⚠ figure reads "Ch 44" (the perf-regression-gates chapter, dossier key 105) vs draft routing's dossier keys 51/105 |
| FF cell "SBOM / vulnerability gate — atomic · triggered · static — guards supply chain (Parts VII–VIII)" | Fitness-function instance | Draft v1 line ~95: "an SBOM or vulnerability gate (Parts VII–VIII)"; routing line ~151: "SBOM/security gates → Parts VII/VIII" |
| FF strip "One designed portfolio of atomic/holistic, static/dynamic, triggered/continuous guards for the characteristics a system must protect." | Portfolio claim | Draft v1 CONCEPT line ~95: "they are a *portfolio* a senior engineer designs deliberately for the specific characteristics a system must protect"; line ~93 (the three category pairs) |
| FF strip "Limits: only protects measurable characteristics; too many strict functions ossify (breed ignore-annotations); functions are code that rots — design deliberately and maintain." | Honest limits | Draft v1 line ~97: "can only protect a characteristic that can be **articulated and measured**…**Over-governance ossifies**…breed ignore-annotations…Fitness functions are *code* — they rot"; §Limitations lines ~116–118 |
| Legend "Level 0 — advisory only; no build signal / Level 1 — ArchUnit / CI gate (typical starting point) / Level 2 — JPMS / compiler + runtime (strongest; highest adoption cost)" | Level legend | Draft v1 enforcement-spectrum table (lines ~55–57); is-start marker on Level 1 per "ArchUnit and JPMS are the two main Java vehicles" (line ~60) + typical starting point |
| Legend "ArchUnit DSL → archunit.org user guide | JPMS → JEP 261 (Java 9) | Fitness functions → Ford et al., Building Evolutionary Architectures" | Source pointers | Draft v1 back-matter lines ~148–150; `00-strategy/SOURCE-PIN.md` §2 ArchUnit 1.4.2 (archunit.org), §1 JEP index, §7 canon (Building Evolutionary Architectures ⚠ not a pinned row) |
| Caption — ArchUnit DSL list (`noClasses().that().resideInAPackage().should().dependOnClassesThat()`, `layeredArchitecture()`, `slices().should().beFreeOfCycles()`, `FreezingArchRule.freeze()`, `NO_CLASSES_SHOULD_ACCESS_STANDARD_STREAMS`, `@AnalyzeClasses`/`DoNotIncludeTests`) — ArchUnit user guide, archunit.org (key 33) | Source attribution | Draft v1 back-matter line ~148 (full DSL inventory); `00-strategy/SOURCE-PIN.md` §2 "ArchUnit 1.4.2 (archunit.org + github.com/TNG/ArchUnit)" |
| Caption — JPMS `module-info.java` `exports`/`requires` compile + runtime — JEP 261, openjdk.org/jeps/261 (Java 9) | Source attribution | Draft v1 back-matter line ~149; `00-strategy/SOURCE-PIN.md` §1 JEP index (⚠ confirm JEP # at use) |
| Caption — Fitness function definition + categories atomic/holistic, triggered/continuous, static/dynamic: Ford / Parsons / Kua / Sadalage, Building Evolutionary Architectures (O'Reilly, 1e/2e), "Automated Software Governance" chapter | Source attribution | Draft v1 back-matter line ~150 (⚠ §7 canon row, taxonomy verbatim @pin); `00-strategy/SOURCE-PIN.md` §7 (Building Evolutionary Architectures NOT a pinned row — ⚠ @pin) |
| Caption — Alternatives (jQAssistant, Spring Modulith, Deptective, JDepend) cited in draft back-matter, key 55 dossier | Source attribution | Draft v1 back-matter line ~147 + lines ~124–126 |

---

Source-trace status: YES (with ⚠ canon-gap and JPMS-semantics caveats below)

Verified against the pinned authority set (00-strategy/SOURCE-PIN.md) at the pins in SOURCE-PIN.md.

Every level, mechanism, enforcer, strength/cost badge, ArchUnit DSL identifier, JPMS keyword, alternative tool, and fitness-function instance in Fig 55.1 traces to an explicit passage in `03-drafts/55_enforcing_architecture_fitness_functions/55_enforcing_architecture_fitness_functions_v1.md` (§"Enforcement: from documented to executable" spectrum table, the ArchUnit code block and rule catalogue, the "ratchet" CONCEPT, §JPMS, §"Fitness functions: the frame that unifies the gates" and its CONCEPT, §"When to use what", §Alternatives, and the back-matter sources block) and to `00-strategy/SOURCE-PIN.md` (§2 ArchUnit 1.4.2 / archunit.org, §1 JEP index). No level, ArchUnit API, JPMS construct, or fitness-function category was invented by the diagram. Neutrality respected: Level 0/1/2 are presented as a strength × cost trade-off ("not an upgrade path"), ArchUnit and JPMS each get their own case and limit, the alternatives (jQAssistant, Spring Modulith, Deptective, JDepend) each get their niche, and none is crowned. Source caveats: (1) *Building Evolutionary Architectures* (Ford / Parsons / Kua / Sadalage), the authority for the fitness-function definition and the atomic/holistic · triggered/continuous · static/dynamic taxonomy, is NOT a pinned row in `00-strategy/SOURCE-PIN.md` §7 — both the draft and the figure mark it `⚠ @pin` (verbatim definition + taxonomy to be confirmed when that canon row is pinned). (2) The JPMS `exports`/`requires` semantics under JEP 261 (Java 9) are marked `⚠ @pin` in the draft; the JEP index is pinned per-feature in `00-strategy/SOURCE-PIN.md` §1, with the JEP number confirmed at use. (3) The ArchUnit version (1.4.2) and `GeneralCodingRules` constant set / `archunit.properties` defaults are pinned at the line in §2 but flagged `⚠ @pin` for exact-value confirmation in the dossier; the figure asserts only the API identity, not a version literal. (4) The "Ch 44" cross-reference on the JMH perf-regression fitness-function cell points to the performance-regression-gates chapter (dossier key 105 per the draft routing line ~151); the chapter-number resolution is editorial, not a sourced atom.
