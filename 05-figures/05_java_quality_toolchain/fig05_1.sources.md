# Fig 3.1 sources — The Java quality toolchain: lifecycle map

Chapter 3 (dossier key 05, solo) · draft v1 · `03-drafts/05_java_quality_toolchain/05_java_quality_toolchain_v1.md`

| Label / element in diagram | Claim / atom | Pinned source path |
|---|---|---|
| Title "Fig 3.1 — The Java quality toolchain: lifecycle map" | Identifying label | Draft §"Two axes: what it catches, and when it runs" (lines ~36–42) + FIGURE PLAN comment (lines ~167–168): "Fig 3.1 (THE chapter figure) — the lifecycle map: IDE → compile → build → pre-commit → PR/CI → platform → production, each tool category placed at its moment" |
| Subtitle "Seven moments · tool categories placed at the moment they run · earlier = cheaper to fix" | Framing | Draft §"Two axes…": "by *moment* (when it runs): author-time (IDE) → compile-time → local build → pre-commit → PR / CI → platform / dashboard → runtime / production" (line ~39) |
| Moment 1 "IDE / Author-time" | Moment label | Draft line ~39: "author-time (IDE)" in the moment axis |
| Moment 2 "Compile-time" | Moment label | Draft line ~39: "compile-time" |
| Moment 3 "Local build" | Moment label | Draft line ~39: "local build" |
| Moment 4 "Pre-commit" | Moment label | Draft line ~39: "pre-commit" |
| Moment 5 "PR / CI" | Moment label | Draft line ~39: "PR / CI" |
| Moment 6 "Platform / Dashboard" | Moment label | Draft line ~39: "platform / dashboard" |
| Moment 7 "Runtime / Production" | Moment label | Draft line ~39: "runtime / production feedback" |
| Latency annotation "Earlier = cheaper to fix (shift-left principle · Ch 4)" | Claim atom | Draft line ~40: "Earlier is cheaper to fix (the shift-left principle, Chapter 4)" |
| Lane 1 — SonarLint (Ch 17) | Tool placement | Draft routing table line ~59: "Platform / trend / dashboard · SonarQube / SonarLint · IDE + CI + server · 17, 38" |
| Lane 1 — Spotless (Ch 6) | Tool placement | Draft routing table line ~50: "Formatting · Spotless, google-java-format, EditorConfig · source text · author / pre-commit / CI · 6" |
| Lane 1 — google-java-format (Ch 6) | Tool placement | Draft routing table line ~50 (same) |
| Lane 2 — Error Prone (Ch 16), "runs inside javac" | Tool placement + atom | Draft lines ~51–52: "Bug patterns (source / compile) · PMD, Error Prone · source / inside `javac` · build / compile-time · 16" + line ~67: "Error Prone runs inside javac as a compiler plugin" |
| Lane 2 — NullAway (Ch 9) | Tool placement | Draft routing table line ~53: "Null-safety & types · NullAway, JSpecify, Checker Framework · source + annotations · compile-time · 9" |
| Lane 2 — Checker Framework (Ch 9) | Tool placement | Draft routing table line ~53 (same) |
| Lane 3 — Checkstyle (Ch 16) | Tool placement | Draft routing table line ~51: "Style & conventions · Checkstyle, PMD · source (AST) · build / CI · 16" |
| Lane 3 — PMD / CPD (Ch 16) | Tool placement | Draft routing table line ~51 (same) |
| Lane 3 — SpotBugs (Ch 16) | Tool placement | Draft routing table line ~52: "Bug patterns (bytecode) · SpotBugs (+ FindSecBugs) · compiled bytecode · after compile / CI · 16" |
| Lane 4 — Spotless pre-commit (Ch 6, Ch 35) | Tool placement | Draft routing table line ~50 (Spotless: author/pre-commit/CI) + line ~81: "same checks run at pre-commit (Chapter 35) and in CI" |
| Lane 4 — Checkstyle fast (Ch 35) | Tool placement | Draft line ~81: "The same checks run at pre-commit (Chapter 35) and in CI (Chapter 34) so developers are not surprised at the gate" |
| Lane 4 — EditorConfig (Ch 6) | Tool placement | Draft routing table line ~50 (same) |
| Lane 5 — SpotBugs + FindSecBugs (Ch 16, 30) | Tool placement | Draft routing table line ~52 (SpotBugs) and line ~56: "Security — SAST · FindSecBugs, Semgrep, CodeQL · source / bytecode / dataflow · CI · 30, 31" |
| Lane 5 — OWASP Dep-Check / Grype (Ch 27, 28) | Tool placement | Draft routing table line ~58: "Security — SCA / deps · OWASP Dependency-Check, Grype, Renovate · dependency tree · build / CI / scheduled · 27, 28" |
| Lane 5 — Semgrep / CodeQL (Ch 30, 31) | Tool placement | Draft routing table line ~56 (same) |
| Lane 5 — JaCoCo / PITest gate (Ch 23) | Tool placement | Draft routing table line ~55: "Test adequacy · JaCoCo (coverage), PITest (mutation) · test runtime · test / CI gate · 23" |
| Lane 5 — ArchUnit (Ch 25, 26) | Tool placement | Draft routing table line ~54: "Architecture / boundaries · ArchUnit, JPMS · tests / bytecode graph · test / CI · 25, 26" |
| Lane 6 — SonarQube (Ch 17, 38) | Tool placement | Draft routing table line ~59: "Platform / trend / dashboard · SonarQube / SonarLint · aggregates many · IDE + CI + server · 17, 38" |
| Lane 6 — Renovate / Dependabot (Ch 28) | Tool placement | Draft routing table line ~58: "Security — SCA / deps · OWASP Dependency-Check, Grype, Renovate · dependency tree · build / CI / scheduled · 27, 28" |
| Lane 7 — DORA metrics (Ch 38) | Tool placement | Draft routing table line ~61: "Delivery outcomes · CI + VCS data (DORA) · the process · continuous · 38" |
| Lane 7 — OpenRewrite on-demand (Ch 40) | Tool placement | Draft routing table line ~60: "Refactoring / modernization · OpenRewrite, Refaster · source (recipes) · on-demand / CI · 40" |
| Layer strip "Source analysis: Checkstyle · PMD · Spotless" | Distinction atom | Draft lines ~67–68: "Source vs bytecode vs compile-integrated. Checkstyle and PMD read **source**…" |
| Layer strip "Compile-integrated: runs inside javac; fastest; can auto-fix — Error Prone · NullAway · Checker Framework" | Distinction atom | Draft line ~67: "Error Prone runs **inside `javac`** as a compiler plugin (fastest feedback, and it can auto-fix)" |
| Layer strip "Bytecode analysis: reads .class files — SpotBugs" | Distinction atom | Draft line ~67: "SpotBugs reads **bytecode** (so it sees what the compiler actually produced, catching things invisible in source)" |
| Layer strip "Tool vs. Platform: SonarQube / SonarLint" | Distinction atom | Draft line ~74: "Checkstyle, PMD, and SpotBugs are analyzers; **SonarQube** is a *platform* that can run its own analyzers and aggregate results over time with quality gates (Chapter 17)" |
| Layer strip "Lint vs. Test: JaCoCo · PITest · ArchUnit" | Distinction atom | Draft line ~73: "Lint/analyze vs test. Static analysis reasons about code *without running it*; tests run it. They are complementary, not substitutes (Part V)" |
| Shift-left note text | Claim atom | Draft line ~40: "Earlier is cheaper to fix (the shift-left principle, Chapter 4)" + line ~39 axis definition |
| Caption "FindBugs is dead — its successor is SpotBugs (spotbugs-maven-plugin)" | Claim atom | Draft line ~94–95: "The clearest example: **FindBugs is dead** — its successor is **SpotBugs**, and the old `findbugs-maven-plugin` is replaced by `spotbugs-maven-plugin`" |
| All pinned tool versions (Checkstyle 13.6.0, PMD 7.25.0, SpotBugs 4.10.2, Error Prone 2.x, NullAway 0.13.4, JSpecify 1.0.0, Checker Framework 4.2.0, ArchUnit 1.4.2, Spotless 3.6.0, google-java-format 1.35.0, SonarQube 2026.1 LTA, Semgrep 1.163.0, CodeQL rolling, JaCoCo 0.8.15, PITest 1.25.3, OWASP Dep-Check 12.2.2, Grype ~0.108–0.110, Renovate rolling, OpenRewrite 8.81.0) | Version pins | `00-strategy/SOURCE-PIN.md` §§2–6, pinned 2026-06-20 |

---

Source-trace status: YES

Verified against the pinned authority set (00-strategy/SOURCE-PIN.md) at the pins in SOURCE-PIN.md.

Every moment label, tool name, chapter reference, and distinction claim in Fig 3.1 traces to an
explicit passage in `03-drafts/05_java_quality_toolchain/05_java_quality_toolchain_v1.md` — the
routing table (lines ~48–62), the two-axis narrative (lines ~36–42), the three distinctions
(lines ~65–75), and the shift-left principle (line ~40). The figure plan comment in the draft
(lines ~167–168) specifies the lifecycle-map design verbatim. No atom was invented by the diagram.

---

## Accessibility

- Alt-text: Lifecycle timeline of seven moments from IDE to production, with the Java quality tools that run at each moment; earlier is cheaper to fix.
- Long-description: A left-to-right timeline arrow across seven numbered moments, each with a node, a label, and a typical feedback latency. From left to right: 1 IDE / author-time (seconds) — the first moment, marked as the focus node; 2 compile-time (seconds); 3 local build (seconds to minutes); 4 pre-commit (under 30 seconds); 5 PR / CI (minutes); 6 platform / dashboard (continuous); 7 runtime / production (hours to days). A latency bar beneath reads "Earlier = cheaper to fix (shift-left principle, Ch 4)" on the left and "Later = slower feedback, higher fix cost" on the right. Below the axis, one tool card sits under each moment. Moment 1 (the accent-bordered card): SonarLint (Ch 17), Spotless (Ch 6), google-java-format (Ch 6) — format and style on the fly. Moment 2: Error Prone (Ch 16), NullAway (Ch 9), Checker Framework (Ch 9) — runs inside javac. Moment 3: Checkstyle (Ch 16), PMD / CPD (Ch 16), SpotBugs (Ch 16) — source AST and bytecode. Moment 4: Spotless (Ch 6), Checkstyle fast (Ch 35), EditorConfig (Ch 6) — fast cheap checks only. Moment 5: SpotBugs + FindSecBugs (Ch 16, 30), OWASP Dependency-Check / Grype (Ch 27, 28), Semgrep / CodeQL (Ch 30, 31), JaCoCo / PITest gate (Ch 23), ArchUnit (Ch 25, 26) — SAST, SCA, coverage, architecture. Moment 6: SonarQube (Ch 17, 38), Renovate / Dependabot (Ch 28) — aggregates analyzers, tracks trends. Moment 7 (the dashed card): DORA metrics (Ch 38), OpenRewrite on-demand (Ch 40) — delivery outcomes, modernize. A strip below draws five distinctions: source analysis reads .java source AST (Checkstyle, PMD, Spotless); compile-integrated runs inside javac and can auto-fix (Error Prone, NullAway, Checker Framework); bytecode analysis reads .class files (SpotBugs); tool vs platform, where a platform aggregates many analyzers (SonarQube, SonarLint); and lint vs test, static analysis reasoning without running code versus tests that run it (JaCoCo, PITest, ArchUnit). A closing shift-left note states the same check run earlier costs less to fix. The caption notes FindBugs is dead and its successor is SpotBugs (spotbugs-maven-plugin).
- Grayscale-safe: Yes. The focus first moment is set apart by an inset ring on its node and an accent bar on its card, plus its leading position; the runtime card is set apart by a dashed border. These are border-treatment and position cues, not hue. Reads in black-and-white print.
- Contrast: Sufficient. Dark ink on light card and node fills throughout; small chapter-reference text remains legible at the 3x render. Note: this is a dense reference figure — the chapter-reference text is the smallest type in the book's figures, so confirm it holds at the final print trim size at Step 15.
- Code legibility: The inline tool tokens (javac, spotbugs-maven-plugin) are real authored monospace text, not a screenshot, and are readable without syntax color.
