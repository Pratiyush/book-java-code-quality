# OS / editor
.DS_Store
*.swp
*~
.idea/
.vscode/

# Build output (companion code modules under 08-companion-code/)
target/
build/
*.class
.gradle/
bin/

# Scratch / ephemeral
temp/
tmp/
*.log

# Java tool local caches
.checkstyle

# Copyrighted reference corpus — NEVER commit (CLAUDE.md / LEGAL-IP-RULES.md):
# close-paraphrase-avoidance checks only; never quoted into the manuscript.
_ref/
pre-final/figures/*.png
