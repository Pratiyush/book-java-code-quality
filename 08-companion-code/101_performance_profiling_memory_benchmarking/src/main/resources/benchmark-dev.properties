# Benchmark execution profile — DEV (a fast local run for a quick signal).
# Externalized so a quick local run and a thorough CI run differ by profile, not by editing the
# benchmark source. A dev run trades statistical confidence for speed; never quote a dev-profile
# number as a result.
benchmark.warmupIterations=2
benchmark.measurementIterations=3
benchmark.forks=1
