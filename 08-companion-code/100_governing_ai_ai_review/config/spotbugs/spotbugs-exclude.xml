<?xml version="1.0" encoding="UTF-8"?>
<!--
  SpotBugs exclude filter for the Chapter 100 companion. The model types are immutable records and a
  sealed verdict, and the gate exposes no mutable state beyond its own atomic counters, so the slice is
  clean at threshold=Medium / effort=Max and this filter is wired and intentionally empty of <Match>
  rows — the place a reviewed false positive or a baselined legacy finding would live (the Chapter 39
  discipline), kept inert here because this module carries no such finding.
-->
<FindBugsFilter>
</FindBugsFilter>
