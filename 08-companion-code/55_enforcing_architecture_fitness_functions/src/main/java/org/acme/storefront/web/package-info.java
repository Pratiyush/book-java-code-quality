/**
 * The web layer: {@link org.acme.storefront.web.OrderController} and a small
 * {@link org.acme.storefront.web.HealthEndpoint}. It is the topmost layer — it calls down into
 * {@code ..service..} and, in the layered rule, is accessed by no other layer.
 */
package org.acme.storefront.web;
