<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
        "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
        "https://checkstyle.org/dtds/configuration_1_3.dtd">
<!--
  The same small, curated house ruleset the flagship modules use (Chapter 16: curate a ruleset,
  don't enable everything and flood the build). High-signal checks the code is held to: naming
  conventions, import hygiene, braces, and a few coding/structure rules. Deliberately omits the
  finicky, high-noise checks (Indentation, MagicNumber, whitespace, mandatory Javadoc on every
  member) so the gate stays credible. The column limit (120) is this team's cited choice, not a
  universal truth (Chapter 6, style-value neutrality).

  Note for this chapter: this module's whole subject is what an analyzer detects, so it carries
  deliberate analyzer-target shapes (an empty catch, a type-misuse call, a resource leak, a taint
  flow). They are written so the module's own gate stays green — an AST shape an analyzer would
  match is shown in a region the house EmptyBlock check tolerates (a comment inside the block) and
  the one finding the house SpotBugs gate genuinely raises (the resource leak) carries a single
  reviewed suppression with a reason. The point is to show what each technique sees, not to flood
  this build.
-->
<module name="Checker">
  <property name="severity" value="error"/>
  <property name="charset" value="UTF-8"/>
  <property name="fileExtensions" value="java"/>

  <!-- File-level (no AST needed) -->
  <module name="LineLength">
    <property name="max" value="120"/>
    <property name="ignorePattern" value="^(package|import) .*"/>
  </module>

  <module name="TreeWalker">
    <!-- Naming conventions (Chapter 6) -->
    <module name="PackageName"/>
    <module name="TypeName"/>
    <module name="MethodName"/>
    <module name="MemberName"/>
    <module name="ParameterName"/>
    <module name="LocalVariableName"/>
    <module name="ConstantName"/>
    <module name="StaticVariableName"/>
    <module name="LambdaParameterName"/>
    <module name="PatternVariableName"/>
    <module name="RecordComponentName"/>
    <module name="CatchParameterName"/>

    <!-- Import hygiene -->
    <module name="AvoidStarImport"/>
    <module name="RedundantImport"/>
    <module name="UnusedImports"/>

    <!-- Blocks and braces -->
    <module name="NeedBraces"/>
    <module name="LeftCurly"/>
    <module name="RightCurly"/>
    <module name="EmptyBlock">
      <property name="option" value="text"/>
    </module>

    <!-- Modifiers -->
    <module name="ModifierOrder"/>
    <module name="RedundantModifier"/>

    <!-- Coding / structure -->
    <module name="OneTopLevelClass"/>
    <module name="EqualsHashCode"/>
    <module name="SimplifyBooleanExpression"/>
    <module name="EmptyStatement"/>
    <module name="UpperEll"/>
    <module name="ArrayTypeStyle"/>
    <module name="OuterTypeFilename"/>
  </module>
</module>
